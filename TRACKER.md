# TRACKER — tekton (code paths still tekton / `rvt`) work queue

Display name is now **tekton** (prose/docs/skills); directories, the python
package and the plugin manifest name stay `tekton`/`rvt` until the one
scripted rename after trademark clearance — see `RENAME.md`. Do not rename
piecemeal.

## *** P0 — SHIPPING GATES (from docs/product/content-strategy.md, 2026-08-03) ***

Nothing built on the current pipeline is a DELIVERABLE. Every .rvt in this
tree is an internal proof. The cutover gates, in order:
- [x] **G1 GENESIS — ACHIEVED (verdict #24, 2026-08-04): GENESIS LOADS.**
      The composed genesis project base `experiments/genesis/subst_k4/
      compose/G_ABPD.rvt` — settings / catalog / palette / datum / views /
      residue layers ALL our constructors' output (2,840 landed slots +
      240 lawful maxgc deletions, composed by `tools/genesis_compose.py`,
      anchor byte-exact), NO Autodesk-authored base content supplied —
      LOADS in Autodesk's reader as a browsable model (3D + sheet
      GEN-101, not empty). Ledgered: docs/coverage/viewer-certified.json.
      The user's day-one target ("no base file required") is MET.
      RESIDUALS carried forward (P0-adjacent, tracked below):
        (r1) RENDER gate — LOAD is not RENDER; every certification to
             date is a LOAD pass. Baked geometry = the seq-103 GElement
             B-rep (grammar decoded; `rvt.render.wallgeom` reproduces
             native walls with ZERO differing leaves) — being wired for
             created elements. See genesis-11 in Epic D below.
        (r2) WALLS + LOADED FAMILY DOCUMENTS combination bug — walls alone
             PASS, families alone empty-pass, together FAIL; mechanism
             under bisection (render-instances stream). Blocks the full
             IFC electrical room on the genesis base.
        (r3) ~260 remaining Autodesk-authored elements + 4 named stragglers
             (link symbol, DataStorage blob, one topology, link instance)
             — each = author a constructor + an in-place rung.
        (r4) counsel C1 (authoring string), C4 (the two shipped product
             corpora: Formats/Latest class schema + ESSchemaStorage unit
             schemas, byte-identical in EVERY Revit file), C5 (the "Data
             generated by Autodesk® Revit®" format-signature token) — see
             docs/product/COUNSEL-BRIEF.md.
        (r5) trademark clearance for the tekton name (blocks RENAME.md).
      History (2026-08-03 sub-gates G1a ADocument encoder / G1b own
      default content / G1c resource ids / G1d multi-baseline ledger, the
      G0/G1-candidate lineage, identity leaks V30/V31/V32) is SUPERSEDED by
      the in-place substitution + composition mechanism that reached #24.
- [~] G2 OWN THE IDENTITY BLOCK — the writer emits OUR BasicFileInfo, never
      the template's (today it inherits Autodesk's build string, an
      Autodesk employee's local path and the sample GUIDs). Mechanism =
      engineering; what the authoring string may lawfully assert while
      Revit still opens the file = COUNSEL question C1.
- [ ] G3 COUNSEL — meeting booked; agenda = docs/product/COUNSEL-BRIEF.md
      (C1 authoring string, C4 the two shipped product corpora, C5 the
      format-signature token, format/interoperability posture per
      docs/legal/provenance-memo.md, the AI policy-layer flags on the
      genesis charters stated factually, and the tekton trademark
      clearance). Plus the standing items: manufacturer-EULA read (P0
      HUMAN TASK: open the actual download packages/EULAs), customer-
      supplied content, product terms / IP indemnity.
- [ ] G4b FAMILY-GENESIS RESIDUAL (asset-assay, 2026-08-03): generated .rfa
      element layers are genuinely constructed (0 identical records), but each
      carries the DONOR family's Global/Latest ADocument (25% of non-format
      bytes; 1,727 dangling donor refs) + a partition-footer token reading
      'Data generated by Autodesk'. FIX = apply the rvt.adocument codec at
      the family level (purge donor registries, repopulate to our elements)
      = project genesis-2 in miniature; footer string -> counsel C5
      (format-token/trademark question). Geometry proof files predate the
      identity fix (regenerate).
- [ ] G4 CONTENT — generate-our-own parametric families from manufacturer
      FACTS (dims/ratings) per the strategy; no third-party geometry.
Until G2-G3 clear and (r1)/(r2) close: no external delivery. Proof files
stay proofs; every manifest stamps PROOF-ONLY, NOT-DELIVERABLE.

## *** Epic P — PACKAGING & THE MULTI-SURFACE FRONT DOOR  ← CURRENT ***

USER REQUIREMENT (2026-08-04): tekton is drivable from ANY AI surface
(Claude Design / Chat / Cowork / Code, ChatGPT Work, Gemini) with any of
THREE inputs — a prompt, an IFC, or an existing Revit file. PACKAGING RULE:
all code/scripts ship INSIDE the plugin as skills + reference docs, synced
by tools/sync_plugin.py (drift guard + DENY list); a hosted MCP server is
the DOCUMENTED FUTURE path (docs/product/MCP-PATH.md), not built now.
- [~] P1 UNIFIED FRONT DOOR `tools/frontdoor.py` — LANDED 2026-08-04
      (`author --prompt / --ifc / --rvt --edit`; `--strict` emits two
      coordinated files under the walls+families open bug; `--base`
      refuses an Autodesk sample; pinned hash-verified genesis base by
      default; output `experiments/frontdoor/<name>-<stamp>/`).
      `tools/rvt_job.py` stays the tested gated engine under it. Front-
      door stream still ACTIVE at handoff — its own docs/inbox record
      closes P1; README documents the real `--help` surface.
- [ ] P2 PROMPT PATH proven end-to-end: prompt -> spec.json (schema-
      validated against spec/building.schema.json) -> author; and the
      user's own alternate path prompt -> Three.js <three-d-stage> ->
      IFC4 -> P3. Both must reach a validated .rvt.
- [~] P3 IFC PATH: inputs/ifc/electrical-room-2500a.ifc end-to-end via
      rvt.ifc.intent (positions from world-baked vertices, Pset tagging-
      contract join, feeder tree, our family mapping) -> project on the
      genesis base. Built (docs/inbox/ifc-room.md); FULL room blocked on
      residual (r2). Fixture path: chicago-plenum-downlight.ifc -> our .rfa
      (L_downlight_loaded PASS).
- [ ] P4 RVT PATH: an uploaded project as base — ops authoring
      (rvt_job.py edit / rvt.manipulate, M2-M4 certified) and use as the
      seed/template for a new job (seed_audit gate). Prove on a file the
      tools were not built against (the 132 MB German-locale project
      already loads and edits).
- [ ] P5 SKILL PACKAGING SWEEP: fold frontdoor + the coverage/CRUD honest
      status into skills/tekton-native/SKILL.md (currently carries duplicated
      appended sections — a sync/authoring drift to clean), keep
      skills/tekton-ifc for the IFC leg; every executable reachable
      from a skill's scripts/; `claude plugin validate` + a cold-load in a
      fresh Cowork session (F8 reality test) as the gate.
- [ ] P6 SYNC DISCIPLINE: `tools/sync_plugin.py --check` green as a
      standing gate; extend the mappings for frontdoor.py + any new
      skill docs; DENY list keeps quarantined/third-party data out.
- [ ] P7 CRUD / COVERAGE HONESTY (from coverage-critic H1-H3): headline
      = certified % first (13.2%, 20 cells, 2026-08-04 run) with
      mutating-verbs-proven separate (42.7%, 53/124, free reads stripped);
      retire "53.3% proven" as a headline everywhere it is quoted.
- [ ] P8 MCP READINESS (documentation-only, no build): keep every CLI's
      --json report / exit-code / manifest contract stable — MCP-PATH.md
      §7 depends on wrapping exactly those. Trigger to build = the first
      non-Claude surface (ChatGPT Work / Gemini / Design) or Cowork's
      sandbox failing F8 on a large model.
- [ ] P9 RENAME (blocked on counsel trademark clearance): execute the one
      scripted sweep in RENAME.md; Step 0 (de-bake the 9 absolute-path
      ROOT constants in src/) is worth doing early and independently.


Only the orchestrator edits this file. Agents propose work in their final
report or via `docs/inbox/<slug>.md`.

Legend: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked

## Epic F — Design→Revit bridge & the `tekton-ifc` SKILL  ← PRIMARY

The deliverable is a reusable SKILL (SOPs + canonical assets) any fresh
Claude Design / Cowork session can load cold. Ground truth:
`docs/design-ground-truth.md`.

- [x] F1 Obtain & dissect real Design-authored IFC + the exporter + models
      (`samples/design-ifc/bs-area-e-electrical-room.ifc`; ifc-export.js v1,
      panel-meta.js, panelboard-model.js, three-d-stage.js analysed)
- [x] F2 `skills/tekton-ifc/SKILL.md` — master SOP (cold-loadable)
- [x] F3 Canonical `assets/ifc-export.js` v2 (drop-in `toIfc`; R1–R10)
- [x] F4 References: tagging contract, authoring rules, import fidelity,
      MEP class map, shared-parameter mapping, versions (~11.5k words)
- [x] F5 Sandbox engine: validate (Tier score) + harden (31.4→89.0 on the
      real sample: extrusions + insertion points recovered) + generate
- [x] F6 Node harness runs the exporter headless + ifcopenshell validation
- [ ] F7 Deliver `ifc-export-v2.js` into the user's Chicago-plenum Design
      project (approved: "do it") — sourced from the skill's canonical asset
- [ ] F8 Sandbox reality test in a live Cowork session: pip install
      ifcopenshell, large-model limits, APS egress (decides skill vs MCP)
- [ ] F9 `.mcpb` / MCP escalation ONLY if F8 finds a hard blocker

## Epic G — Productization & licensing (plan of record: docs/product/architecture.md)

- [x] G0 Architecture decision draft: engine server-side behind Remote MCP;
      plugin ships only the knowledge layer; open-core exporter; `.rvt` via
      APS for the paid product; native writer stays research + legal-gated
- [ ] G1 User decisions: pricing unit, open-core vs server-side exporter,
      APS `.rvt` vs IFC-only tiers, offline SKU need, multi- vs single-tenant
- [ ] G2 Remote MCP server design (OAuth, entitlements, metering,
      signed-URL file transport, tenant isolation)
- [ ] G3 Plugin packaging (skills + hooks + agent templates + .mcp.json)
- [ ] G4 Legal review before any `.rvt`-write commercial offering

## Epic A — Decompose & decode (`.rvt` internals)

- [x] A0 Container inventory (OLE) — `tools/extract.py`
- [x] A1 Corpus, CORRECTED for page framing — `tools/build_corpus.py`;
      `container.py` de-pages; 13,583/13,583 members CRC-valid
- [x] A2a Schema grammar (Formats/Latest) — schema-a + schema-b + rvt-rs
      corroboration; grammar solved
- [x] A2b Full schema decode: 4,690 classes to EOF, 0 gaps, 16/16 anchors
      (`src/rvt/schema.py`, `schema.json`, `mep_classes.txt`)
- [x] A3 Metadata streams — `src/rvt/meta.py` (all six files, byte-exact)
- [x] A4 `Global/ElemTable` — decoded (class 0x5c9, 40-B ElemRec)
- [x] A5 `Global/Latest` framing — primitives, prologue, ordinal 0x1c
- [x] A6 `Global/PartitionTable` (workset table) + `Partitions/N` fully
      framed to record level (seq 101/102/103, 100% record walk)
- [x] A7 `Global/History` decoded; ContentDocuments = object array
- [x] A8 Cross-file constancy — PAGE FRAMING discovered; 8-byte prefixes,
      Contents wrapper resolved
- [x] A9 Strings/GUID orientation index — `src/rvt/strings_scan.py`
- [x] A10 Object decoder: 85,814/85,814 records decode 100% (racbasic);
      corpus 99.69% (only Extensible-Storage blobs remain) — `objects.py`
- [x] A11 Ground-truth oracle harness wired (magnetar 2023/2024 files +
      shipped exports/CSVs) — `tests/oracle/`, `docs/streams/11-oracle.md`
- [ ] A12 Unified `rvtinspect` over all decoders; `docs/FORMAT.md` merge
- [x] A13 ECC SOLVED (`ecc.py`): 255-lane bit-interleaved CRC-11 (poly
      x^11+x^2+1) + 8 size classes; corpus-wide byte-exact

## Epic B — Open-format generator

- [x] B1/B2 spec JSON schema drafted + IfcOpenShell 0.8.5 installed; valid
      IFC4 house generated & EXPRESS-validated (`experiments/gen_ifc_min.py`)
- [ ] B3 Fold spec→IFC generator into the skill engine (parameterized entry)

## Epic C — [REMOVED] APS / Design Automation

USER DECISION (firm, twice): NO Autodesk APS. The writer is our own native
binary writer (Epic D). Do not re-propose APS. Prior research remains in
docs/generation-paths.md as history only.

## Epic D — NATIVE `.rvt` WRITER  ← THE PRIMARY GOAL

Mandate: take inputs / an IFC and produce a `.rvt` that opens (Autodesk
Viewer as the fast oracle, run by the user) and is editable. Milestone
ladder — each isolates one unknown:

- [x] D0 Container round-trip stream-identical (`cfb_writer.py`, 15 tests)
- [x] D1 Stream re-writer (`writer.py`): valid gzip members (sync-flush+
      finish, correct CRC/ISIZE), page re-framing with pluggable trailers
- [x] D2 THE GATE — ECC CRACKED (`src/rvt/ecc.py`, bit-interleaved CRC-11
      from Utility.dll CRCIO.cpp; 3,502 full + 55 partial pages byte-exact)
- [x] D3 WHOLE-FILE re-write PASSES the Autodesk Viewer (V15, 2026-08-03):
      every framed byte ours — 3D + full sheet set render. WRITER PROVEN.
- [x] D4 Object codec byte-exact: encode.py inverse of objects.py —
      1,153,554 records, 397 classes, 100.000%, whole segments identical;
      + 6 small-stream encoders 36/36 (`stream_encoders.py`)
- [x] D5 FIRST AUTHORED CHANGE ACCEPTED + VISIBLE (V18, 2026-08-03): our
      title text renders on the cover sheet; stamps proven not validated
- [x] D6/D7 CREATION PROVEN: family instance (V20), batch of 4 (V21), wall via
      add_wall with SerializedDummy rep (V22) — all translate. Visual
      confirmation deferred to a WebGL browser.
- [x] D7 ELEMENT CREATION ACCEPTED (V20, 2026-08-03): new family instance
      (concrete column, id 1,472,525) via mutate+encode+commit translates;
      visual eyeball deferred to a WebGL browser; V21 batch-of-4 in flight
- [x] D9 PORTABILITY (2026-08-03): Document.from_file(any.rvt) — no corpus;
      spec_to_rvt --template-rvt auto-discovers content; plugin validated
      by `claude plugin validate` and load-tested in a real Claude Code
      session (5 skills enumerate). Sync via tools/sync_plugin.py + drift
      test. (V27 pass)
- [x] D10 CONNECTOR HYGIENE: created equipment clones the exact symbol and
      starts UNCONNECTED (scrubbed refs). (V28 pass)
- [x] D11 CIRCUITS PROVEN (V29 pass, 2026-08-03): Document.add_circuit —
      real-model recipe (loads connType 1, panel = base on a 50000-series
      slot, mutual back-links); 3-circuit electrical room translates.
      Circuiting EXISTING elements = phase 2.
- [x] D8 SPEC/IFC -> NATIVE .rvt PROVEN + VISUALLY CONFIRMED: V23 (spec),
      V25 (from IFC, zero hand-authoring via ifc_to_spec.py), V26 (+ walls
      synthesized from the room-shell footprint) all translate; V23-vs-V24
      {3D} comparison proves created geometry renders. GOAL MET.
- [!] D9 Legal review before any commercial `.rvt`-write sale (flagged; not
      a technical blocker for the beta)

## Epic E — Verification & loop

- [ ] E1 Local verify harness (parse-all-samples regression, CRC asserts,
      IFC validate) as project `/verify` skill
- [ ] E2 Review→fix→verify loop until green
- [ ] E3 Revit/APS acceptance path for generated output

## Open questions

- CRITICAL: which Revit version do the brothers' firm run? (target ceiling;
  Revit cannot open newer files). Need one small file from that version
  to capture its schema blob if < 2026.
- The 353-byte page trailer (ECC): reproducible? Or does Revit tolerate
  zeroed/absent trailers via auto-repair? Needs Revit/APS test.
- Do serialized instances reference classes by (definition-order + 0x0c)
  ids, with the ±1 drift the partitions agent observed? Confirm in A10.
- Sandbox: does a live Cowork sandbox pip-install ifcopenshell and permit
  APS egress? (F8)

## Coverage harness hardening (from the coverage critic, 2026-08-03)
- [ ] H1 per-cell SEMANTIC CHECK: every non-read cell gets a `check` callable
      (Document.from_file assertion of the specific fact — diameter changed,
      instance moved, element gone) and VALIDATES requires validator 0-errors
      AND check passed; ingest streams' manifest.json readbacks.
- [ ] H2 strict READ probes: decode >= N records with 0 dirty AND assert the
      fields downstream verbs need (host, level, type, curve/point,
      connectors) — no free greens.
- [ ] H3 HEADLINE = certified % first, proven-uncertified separately.
      Honest baseline REFRESHED (2026-08-04 12:09, `coverage.py run
      --validate-only`): 13.2% CERTIFIED (20/152); 42.7% mutating-verbs
      proven (53/124, all 28 free read cells stripped); 0 REGRESSED,
      0 FAILS; "53.3% proven" (81/152) merges VALIDATES with CERTIFIED
      and is retired as a headline (still printed by the tool — fix in
      the harness per H3).
- [ ] H4 columns/beams category-id probe contradiction (probe reported 0
      structural columns vs note "131") — reconcile constants.
