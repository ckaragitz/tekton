# inbox — asset-assay (hostile provenance + fitness critic for the asset factory)

Stream: THE ASSAYER (2026-08-03). Charter: independently re-run the certified
validator and a raw byte/string/record scan over every generated family and
project under `experiments/families/{genesis,factory}/`, quantify what is
COPIED versus CONSTRUCTED versus a FACT, test the fitness claims (catalog
dimensions, tagging-contract parameters, connector electricals /
circuitability), and rank next steps. NO code touched. Evidence scripts +
raw outputs in the session scratchpad (`assay/`: `validate_rfa.json`,
`validate_rvt.json`, `scan.py|json`, `donor_cmp.py`, `records_cmp.py`,
`host_cmp.py`, `connectors.py`, `fitness.py`).

## Verdict in one screen

* **The element layer of every from-scratch family is genuinely
  CONSTRUCTED.** 0 of the 41/39/35/21/25 records in the panel / xfmr /
  troffer / S0 / S0e documents is byte-identical to ANY record of the rme
  panelboard (u243, 551 recs), the rme plain-recessed light (u271, 605 recs)
  or the Autodesk donor .rfa (1,992 recs). No record-count identity (41 vs
  551/605/1,992 — the ~500-record template ballast of a real family is
  ABSENT). Best-match leaf identity vs the specimens is 43–51 % overall
  (0.05 UnitsElem, 0.17–0.22 self-Family, 0.41 VarSketch, 0.36–0.86
  ExtrusionElem, 0.83–0.85 ConnectorElem, 0.90 SketchPlane — the high scores
  are formulaic datum/view records that are near-identical in any two
  families). No specimen GUID reuse (the only shared GUID string is the null
  GUID); document GUIDs are ours (the load's unit 306 GUID `7552b43f-…` is
  new). Manufacturer / model / catalog numbers ride as parameter VALUES with
  our own prose descriptions ("… generated from catalog facts"). This is NOT
  the G0 element-clone trap.
* **But every .rfa the factory / skeleton emits carries Autodesk BYTES in its
  container:** `Global/Latest` (68,508 B raw, ~1.38 MB inflated) is the
  DONOR `racbasicsamplefamily-2026.rfa`'s serialized ADocument, byte-for-
  byte — the document object of Autodesk's 4-leg TABLE family, whose
  element-tracking registries reference **1,727 donor element ids that do
  not exist in our file** (0 references to any of our 41 elements). Plus
  `Formats/Latest` (165,553 B, the per-release schema — a legitimate format
  constant, present in every 2026 file) and the opaque partition unit footer
  + end record (the source of the literal string **"Data generated by
  Autodesk"** at tail-257 of `Partitions/0` in all five). Byte tally for the
  flagship `eaton_prl_400A_42sp_480Y.rfa` (270,336 B): 234,061 B (86.6 %)
  byte-identical to the Autodesk donor stream-for-stream; excluding the
  schema constant, **68,765 B (25.4 %) are Autodesk-authored document /
  format bytes riding inside our "generated" family** — Global/Latest is the
  one that is CONTENT, not format. The factory's own `provenance_scan`
  declares this honestly (`dangling_references_to_donor_ids: 1727`,
  "the one honest carried non-constant") — it is disclosed, not hidden, but
  it means the standalone .rfa is our elements wrapped in Autodesk's table
  family's document object.
* **The four `G_*` geometry proofs are the OLD TRAP, undisguised:** 11 of 13
  streams byte-identical to the donor — Autodesk's `BasicFileInfo` with the
  employee Windows path `C:\Users\hansonje\Desktop\Downloadable
  Files\racbasicsamplefamily.rfa` and "Author: Autodesk Revit", Autodesk's
  `PartAtom` (`<title>racbasicsamplefamily</title>`), Autodesk's
  `RevitPreview4.0` thumbnail (their rendered table image), `TransmissionData`
  with donor `ElementId`s, the donor's History / DIT / ContentDocuments /
  PartitionTable / Global/Latest, and the donor's 1,992-element table
  document in `Partitions/69` (ours appended). The family-geometry record
  frames them as "the sample .rfa + our form" (a mechanism proof), and a
  clean composition already exists (`G_box_on_S0.rfa` = our S0 + our box:
  from-scratch container class). They must be labelled QUARANTINE — never
  shippable, never a viewer submission that could be mistaken for product.
* **The certified gate is NOT met by any .rfa on disk.** Under the actual
  `tools/rvt_validate.py` (the brief's "every emitted file must pass with 0
  errors — .rfa included"): factory panel / xfmr / troffer, S0, S0e →
  **INVALID, 3 errors each** (missing `ProjectInformation`; `PartAtom` not
  CRCIO-framed / no gzip member); the four `G_*` → **INVALID, 5 errors**
  (the same 3 + the family-file ElemTable graveyard tail + a DIT layout the
  decoder rejects) = exact PARITY with the untouched Autodesk donor .rfa
  (5 errors). The streams' "VALID 0 errors, 0 warnings" claims are all under
  a runtime monkeypatch (`famgen.skeleton.validate_family`: `UNFRAMED_STREAMS
  += PartAtom`, `ProjectInformation` dropped) that lives OUTSIDE the
  certified validator; the 6-line `validate.py` family-mode diff has been a
  recorded request across three streams and is still unmerged. Honest
  reading: the 3 residual findings are validator-calibration gaps for the
  .rfa shape (an authentic Autodesk .rfa scores WORSE), not evidence of a
  broken file — but the certification sentence in the records ("VALID 0
  errors, family mode") is a shimmed verdict, not the arbiter's.
* **The three project files DO pass the certified gate:**
  `project_with_generated_panel.rvt`, `_dummy.rvt`, `project_with_loaded_
  panel.rvt` → **VALID, 0 errors**, 1 warning each = the pre-existing
  decoder gap of the untouched rme sample (1,171 FamilyInstance E-storage
  blobs), 427,623 / 427,620 records, 2.57 M references.
* **The load-into-project proof is structurally REAL, not faked.** The
  load adds exactly 19 host records (1 Family, 14 ParamElemFamily twins,
  FamilySurrogate, FamilySymbol, FamSymSurrogate, 1 FamilyInstance) and one
  new embedded save unit (306, our 41-record document, our GUID); **0/19
  added records are byte-identical to any baseline rme record**; the host
  Family 888055 is only 0.34 leaf-identical to the closest of the 44 baseline
  Families, the FamilySymbol 888071 only 0.27 vs specimen 455409 (its
  transform reference) — i.e. constructed via the decoded transform, not
  cloned. The FamilyInstance 888073 is 0.54 identical to a baseline instance:
  its placement SCAFFOLDING is deliberately cloned from a host instance of
  the category and re-pointed at OUR symbol + OUR rebuilt one-slot connector
  manager (the accepted `rvt.mutate` pattern). The embedded ADocument for
  our unit is 3,017 authored bytes with ZERO strings (all-null registries),
  and the loader RE-ENCODES the host `Global/Latest` (+49 B, three
  registrations) — proof that an ADocument encoder EXISTS today, which makes
  the donor `Global/Latest` in the standalone .rfa a retirable debt, not a
  hard block. What the proof does NOT establish is Revit ACCEPTANCE (the
  forge's own H1–H6 ledger stands): all-null AppInfoManager (H1), the symbol-
  geometry id space inferred from ONE specimen (H2), no style/category copies
  in big2small (H3), `symbol == masterSymbol` on a face host (H5).

## Fitness of the generated Eaton panel (the product sentence)

- **(a) Catalog dimensions — 2 spot-checks PASS with honest flags.** Box
  width 20.0 in and depth 5.75 in are `provenance: fact` sourced to the
  PRL2X row of `facts/eaton/pow-r-line-panelboards.json`
  (`variants[*].dims_in.w = 20.0`, `.d = 5.75`; `line_facts.box_width_
  standard_in = 20.0`, `box_depth_standard_in = 5.75`; sources S1/S2 =
  Eaton CA08100003E Vol 2 Tab 3 June 2020 + RP01400001E, both `VERIFIED`
  reads of Eaton's own catalogs via an authorized distributor). Height 60 in
  is correctly flagged `assumed` (borrowed from the PRL1X 42-circuit sizing
  table — "SURFACED AS UNVERIFIED"), as is SCCR 14 kA. The description string
  carries the true W × H × D. The geometry generator receives these values
  (`add_box_form(width_ft, depth_ft, …)`); I did not re-measure the emitted
  B-rep vertices — the family-geometry stream's own byte-exact box
  reproduction from dimensions covers that mechanism.
- **(b) Tagging-contract parameter NAMES: PASS by name, FAIL by identity.**
  All 11 names of `usecases/eaton-panelboard/panelboard-shared-
  parameters.txt` (PanelName, Voltage, Phases, Wires, BusRating, MainsType,
  MainsRating, ShortCircuitRatingkA, Mounting, NumberOfCircuits,
  NeutralRating) are present as `ParamElemFamily`s (+ Width/Height/Depth =
  14). BUT they are LOCAL family parameters
  (`revit.local.family:41b011b7…-1.0.0` spec ids), NOT the SHARED
  parameters the contract file defines (GUIDs `d2cce9ee-…`, `002b1533-…`,
  … — none of those GUIDs appears anywhere in the file). A Revit schedule
  or tag keyed on the SHARED parameter binds by GUID and will show these
  columns EMPTY for our family; only a NAME-keyed extraction (our own
  tooling) binds. Also: no `Manufacturer` / `Model` identity BUILT-INS are
  present as records — "Eaton" / "PRL2X" ride as VALUES of user text
  parameters (the panel .json's grep shows no `-1010104` / `-1010103`
  identity BIPs); a schedule column "Manufacturer" (the built-in) is empty.
- **(c) Connector electricals — SANE; circuitability — LOAD yes, PANEL no.**
  Panel `ConnectorElem` 1040: internal `m_dVoltage` 5166.677 ⇒ 480.0 V
  (÷ 0.3048², the verified unit rule) ✓, 3 poles ✓, PF 1.0, load
  classification our own 'Power' (resolved by name to the host's 'Power' on
  load). Transformer: primary 480 V / 3-pole ✓, secondary 208 V / 3-pole ✓
  with 807,293 internal ⇒ 75,000 VA = 75 kVA ✓ (lumped on phase 1, not
  25/25/25 — a modelling wrinkle). Troffer: 120 V / 1-pole ✓, 409.03
  internal ⇒ 38 VA, PF 0.95 ✓. **But `rvt.mutate.Document.add_circuit`
  would NOT find valid supply slots on our panel:** our placed instance
  888073's connector manager holds ONE connector at nIndex 1 (its incoming
  feed), whereas a real panelboard instance (rme 471580) carries index 1 +
  42 slot connectors at 50000..50041 (one per circuit space). `add_circuit`
  allocates the next free ≥50000 slot; on ours the set is empty, so it
  falls back to index 1 — every branch circuit would be wired onto the
  panel's own SUPPLY connector, an invalid graph. (`add_circuit` also only
  circuits NEW elements minted in the same commit, so a real test would
  place a fresh instance via `add_family_instance(symbol_id=888071)` and
  circuit that — the slot deficiency is inherited by the clone.) Our panel
  is a well-formed LOAD (feedable from an upstream panel via index 1) but
  NOT a circuitable PANEL for its own 42 spaces.

## Findings (evidence)

- **Certified validator, .rfa (raw, no shim) [V]:** factory panel/xfmr/
  troffer, S0, S0e = INVALID 3 errors {`ProjectInformation` missing;
  `PartAtom` final block not CRCIO-framed; `PartAtom` no gzip member};
  G_box_solid/dummy, G_cyl_solid/dummy = INVALID 5 errors (the 3 + ElemTable
  graveyard-tail decode + DIT decode); Autodesk donor .rfa itself = INVALID
  the SAME 5. Warnings 0 everywhere. `validate_rfa.json`.
- **Certified validator, .rvt [V]:** all three factory projects VALID 0
  errors, warnings=1 (baseline's known FamilyInstance ES-blob gap), 507
  pages / 8 gzip members / 2,053 partition blocks / 427,623 records /
  142,234 elements / 2,571,702 refs / 15,339 connector edges checked.
  `validate_rvt.json`.
- **Stream-level donor identity [V]** (`donor_cmp.py`, raw + logical
  compare vs `vendor/phi-ag-rvt/examples/Autodesk/racbasicsamplefamily-
  2026.rfa`, which is byte-identical to `experiments/families/F0_rfa_
  container_roundtrip.rfa`): factory + S0 + S0e ⇒ ONLY `Formats/Latest`
  (165,553 B) and `Global/Latest` (68,508 B) identical to the donor; every
  other stream ours. G_box/G_cyl ⇒ ONLY `Global/ElemTable` and
  `Partitions/69` differ from the donor; the other 11 streams (incl.
  `RevitPreview4.0`, `BasicFileInfo`, `PartAtom`, `TransmissionData`,
  `Global/Latest`) are the donor's bytes.
- **Inflated string / byte scan of every stream + every save-unit record
  [V]** (`scan.py`, `scan.json`): NO Autodesk/ADSK/autodesk.com string, NO
  employee username, NO Windows path, NO rme sample-family name, NO .fbx
  in ANY factory / S0 record EXCEPT (i) the format vocabulary
  (`autodesk.spec.* / autodesk.unit.* / autodesk.parameter.group:*` Forge
  ids — identifiers, not content), (ii) `urn:schemas-autodesk-com:partatom`
  XML namespace in OUR PartAtom (interop vocabulary; values ours), (iii)
  `assetlibrary_base.fbx` + `SunAndSky-002` + render-settings keys inside
  `SunAndShadowSettings` (the per-document rendering DEFAULT block that
  every Revit document carries — a format default from the project
  skeleton's view constellation, not manufacturer content), (iv) the
  literal UTF-16 "Data generated by Autodesk" inside the copied partition
  END RECORD of `Partitions/0` (offset −257) in all five. The category
  labels our transformer/troffer PartAtoms carry ("Dry Type Transformer",
  "Lighting Fixture") are Revit CATEGORY names that happen to overlap the
  rme sample-family names in the grep list — generic vocabulary, and OUR
  family names differ ("Dry Type Transformer 75kVA 480-208Y120" ≠ the
  sample's "M_Dry Type Transformer - …").
- **Record-level provenance [V]** (`records_cmp.py`): per-file totals
  above; class histograms show NONE of the specimen ballast (no
  CategoryElem / GStyleElem / FontElem / DimensionStyle / LinearDimString /
  SketchGrid rows — the ~510 template rows of a real family document are
  absent from ours by design). Best-match leaf identity our-panel vs rme
  panelboard 0.483 (2,847/5,891 leaves), our-troffer vs rme plain-recessed
  0.505, our-panel vs donor 0.477. GUID strings inside our documents: only
  the null GUID; ∩ specimens = ∅ otherwise.
- **The load's host records [V]** (`host_cmp.py` vs the untouched rme
  baseline): 19 added host records, 0/19 payload-identical to baseline,
  aggregate best-match identity 0.428; new save unit 306 (41 records = the
  standalone document's exact class histogram), new content-document GUID;
  host `Global/Latest` re-encoded (338,295 → 338,344 B) and
  `ContentDocuments` extended (1,316,957 → 1,317,509 B) — the registrations
  are written INTO the real host ADocument by an encoder, not spliced bytes.
- **Facts store [V read]:** width/depth are catalog facts with cited,
  VERIFIED sources; height/SCCR honestly `assumed`; the LICENSE_NOTES
  posture (facts-not-expression, photometry referenced-not-redistributed,
  names-as-values) is implemented as written for the panel. The transformer
  carries an Eaton catalog number (`V48M28T7516`) and the troffer a
  Lithonia one (`2BLT4-38W`) + the Acuity product URL as VALUES — the
  content-strategy §5.4 trademark question (recorded NEEDS COUNSEL) applies.

## (1) Honest provenance ledger of a generated .rfa

| layer | what | provenance |
|---|---|---|
| element records (Partitions save unit) | 21–41 elements: self-Family, Ref. Level, RefPlanes, view constellation, params/types, sketch + extrusion + our B-rep, load classification, connector | **structure-we-constructed** — 0 byte-identical records, no ballast, no GUID reuse; per-class header flag words / m_designOptionId etc. are format constants read off specimens |
| dimensions, ratings, catalog no., URL | 20 × 60 × 5.75 in, 480 V, 400 A, 42 sp; 75 kVA; 38 W / 4600 lm / 4000 K | **numbers-that-are-facts** (cited Eaton / distributor / search sources, per-field `fact`/`assumed` flags carried into the file's own JSON) |
| names, descriptions, PartAtom title, BasicFileInfo, TransmissionData | ours | **ours** (author "rvt-writer", no user path, our prose) |
| Global/ElemTable, History, DIT, PartitionTable, ContentDocuments, Contents body, CRCIO framing, CFB container | ours | **ours (encoded)** |
| `Formats/Latest` (165,553 B) | per-release class schema | **format constant** (legitimate; every 2026 file carries an equivalent) |
| `Global/Latest` (68,508 B / 1.38 MB inflated) | the DONOR TABLE family's ADocument, 1,727 dangling donor element refs | **bytes-we-copied — CONTENT, not format** (declared by the factory's own provenance scan; the debt to retire) |
| partition unit footer (64 B) + end record (~92 B, "Data generated by Autodesk") | opaque | **bytes-we-copied** (opaque; accepted in prior gates) |
| G_box/G_cyl proofs | 11/13 streams = the donor | **bytes-we-copied — the whole Autodesk sample family** (mechanism proofs only; quarantine) |

## (2) Fitness gaps (ranked)

1. **The panel is not circuitable as a PANEL** — one supply connector, no
   per-circuit 50000-series slot connectors for its 42 spaces; `add_circuit`
   would wire every branch to the feed connector. Product sentence
   ("build me an Eaton panel with circuits X and Y") stops here today.
2. **Contract parameters are name-matched, not shared** — no shared-
   parameter GUIDs, no Manufacturer/Model built-in identity data; a real
   schedule/tag keyed on the shared parameter or the built-in shows empty.
3. **The certified validator rejects every .rfa** (3 residual family-shape
   findings) — the "0 errors" claims are shimmed; the family-mode
   `validate.py` diff is unmerged.
4. Transformer 75 kVA lumped onto phase 1 (not 25/25/25 across the three
   phases); apparent-load parameters otherwise 0 on the panel connector
   (fine for a distribution panel).
5. Height 60 in and SCCR 14 kA are `assumed` — correct flagging, but the
   flagship's own overall height is unverified.

## (3) Is the load-into-project proof real or faked?

REAL at every layer this project can measure — genuine embedded save unit,
genuinely constructed host records (0/19 cloned, 0.43 identity), an
ADocument ENCODER (not a splice) writing the three registrations into the
real host tree, certified validator VALID 0 errors, targeted provenance
clean — and honestly UNPROVEN at the one layer only Revit can judge (the
forge's H1–H6, above all the all-null `AppInfoManager` of our embedded
document, 3,017 authored bytes vs the specimens' 19–44 KB with ~110
registries). It is not faked; it is unaccepted.

## (4) Ranked next steps

1. **Retire the donor `Global/Latest` from the standalone .rfa.** The
   ADocument encoder that already reconciles the HOST document in the
   loader (`rvt.adocument.encode_latest`) should author the family's own
   document object (or a minimal one whose registries reference only OUR
   elements). This removes 68.5 KB / 1,727 dangling refs of Autodesk's
   table family from every asset we would ship, and turns "no cloned
   third-party content" from mostly-true into true. Same tick: replace the
   opaque partition footer/end-record with our own (or prove them content-
   free) so the "Data generated by Autodesk" string leaves our files.
2. **Give the panel its circuit slots** — one 50000-series slot connector
   per space (the rme 471580 grammar: index 1 = incoming, 50000..50041 =
   slots), so `add_circuit` and Revit's own circuiting both work; add a
   test that places a fresh instance and circuits two loads to distinct
   slots.
3. **Bind the tagging contract by identity, not name:** author the 11
   contract parameters as SHARED parameters carrying the contract GUIDs
   (`d2cce9ee-…` PanelName, `002b1533-…` Voltage, …) so schedules/tags key
   on GUID; and write Manufacturer / Model / Description into the identity-
   data built-ins (`-1010104` / `-1010103` / `-1010109`) the family-
   skeleton stream already decoded, not only as text-parameter values.
4. **Merge the 6-line family-mode diff into `src/rvt/validate.py`** and
   re-baseline: the gate must be the certified tool with 0 errors, not a
   runtime shim; until then every record's ".rfa VALID 0 errors" line
   should read "0 errors under the family-mode shim; 3 under the arbiter".
5. **Quarantine `experiments/families/genesis/G_{box,cyl}_{solid,dummy}.rfa`**
   (rename to `*.QUARANTINED.rfa` or move under a `donor-graft/` folder
   with a README): they redistribute Autodesk's file — an employee Windows
   path, "Author: Autodesk Revit", Autodesk's preview thumbnail, Autodesk's
   PartAtom and their entire 1,992-element table document. Keep
   `G_box_on_S0.rfa` (our container + our form) as the geometry proof and
   regenerate the cylinder proof onto S0 the same way.
6. Viewer/Revit-gate, unchanged order: `project_with_generated_panel.rvt`
   first (H1–H6), then the ladder, then the standalone .rfa — but note the
   standalone .rfa carries the TABLE family's document object; a "pass"
   there proves Revit tolerates a foreign ADocument, a "fail" is more
   likely THAT than our elements.

## Requests for the orchestrator

- Treat "VALID 0 errors (family mode)" in family-skeleton / family-geometry /
  asset-forge as SHIMMED verdicts; the arbiter says INVALID (3 or 5) for
  every .rfa on disk, including Autodesk's own donor (5). Book the
  `validate.py` family-mode diff (family-skeleton §13) as a core change.
- Do NOT let any `G_*` file into a viewer queue that could be mistaken for
  product; label them donor-graft mechanism proofs.
- Book an ADocument-authoring pass for the STANDALONE .rfa (asset-forge's
  encoder already writes host ADocuments) — the single largest provenance
  liability of the shippable asset.
- Book the panelboard circuit-slot connector work (`famgen` skeleton /
  factory) before anyone demos "add circuits to the generated panel".

## Verification (how to re-run this assay)

- `.venv/bin/python tools/rvt_validate.py experiments/families/genesis/*.rfa experiments/families/factory/*.rfa` → 3 or 5 errors each (above);
  `.venv/bin/python tools/rvt_validate.py experiments/families/factory/*.rvt` → VALID 0 errors ×3, 1 baseline warning each (~4 min).
- Stream identity vs donor: hash each stream of a factory .rfa against
  `vendor/phi-ag-rvt/examples/Autodesk/racbasicsamplefamily-2026.rfa` —
  `Formats/Latest` + `Global/Latest` match; on `G_*` 11/13 match.
- Record-level: `rvt.families.FamilyIndex` on each .rfa (unit 0), the donor
  (unit 0), rme units 243 / 271, and the loaded project (added ids = host
  records absent from the rme baseline) — payload set-intersection = ∅ in
  every case; best-match leaf identities as tabulated.
- Fitness: `rvt.mutate.Document.from_file(project_with_generated_panel.rvt)`
  → symbol 888071 'Panelboard 480Y/277 400A MCB 42ckt Surface' / '400A MCB
  42ckt', instance 888073 connector indexes = [1] (specimen 471580 =
  [1, 50000..50041]).
- Full suite: `.venv/bin/python -m pytest tests/ -q --ignore=tests/oracle`
  → **708 passed, 3 failed** (12:43): `test_plugin_sync` (the known
  plugin drift awaiting `tools/sync_plugin.py`) + two `test_provenance.py`
  G0 tests (parallel provenance / genesis streams) — none in famgen. A
  separate `-x` run first stopped on `test_genesis_settings.py::
  test_reproduce_small_singletons` (GCSTracker leaf diff), which then
  PASSED in the full run: order-dependent / flaky, worth its owner's look.
- Tests added by this stream: NONE (read-only assay charter). Proposed gate
  test for the factory's owner: `tests/test_famgen_provenance_gate.py` —
  assert 0 payload-identical records vs the donor .rfa + rme units 243/271
  for every `experiments/families/factory/*.rfa`, no user path / employee
  name in `BasicFileInfo`, and an EXPLICIT allow-list of donor-carried
  streams (`Formats/Latest`, `Global/Latest`, unit footer, end record) that
  shrinks to `Formats/Latest` once step 1 lands — so a future donor graft
  fails loudly instead of shipping quietly.

BRANCH STATE: no git repo (working tree); deliverable = this memo
docs/inbox/asset-assay.md (READ-ONLY assay, no code or asset touched; full
suite 708 passed / 3 failed = the known plugin-drift + two provenance-G0
cross-stream failures, none in famgen);
verdicts — element layer of all from-scratch families CONSTRUCTED (0
cloned records, no ballast, no GUID reuse), dimensions are cited FACTS,
BUT every emitted .rfa carries the Autodesk table family's document object
(Global/Latest, 68.5 KB, 1,727 dangling donor refs) + the "Data generated
by Autodesk" partition end record, the four G_* proofs are the Autodesk
sample family wholesale (employee path, Autodesk preview) and must be
quarantined, NO .rfa passes the certified rvt_validate (3–5 errors; the
"0 errors" claims are under a runtime shim), the three .rvt DO pass (0
errors), the load proof is structurally REAL (19 constructed host records,
authored embedded ADocument, real host-ADocument re-encode) but
Revit-unaccepted, and the flagship panel is a valid LOAD but not a
circuitable PANEL (one supply connector, no 50000-series slots) with
name-matched-not-shared contract parameters; READY.
