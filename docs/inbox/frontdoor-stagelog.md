# frontdoor-stagelog — ONE quiet-stage log helper for the three front-door routes

Stream record for issue #373 (Refs #312 / #188 / #330; PG3 / O10). Territory used:
`src/rvt/frontdoor/stagelog.py` (new), `src/rvt/frontdoor/router.py` + `build.py` (their two log
helpers only), `src/rvt/frontdoor/edit.py` (the quiet capture in `run_edit`), `src/rvt/frontdoor/manifest.py`
(`edit_manifest` / `write_manifest` naming + the MD line), `tests/test_frontdoor.py`, `tests/test_router.py`,
`tests/test_stagelog.py` (new) + its shard drop-in `tests/ci_shard.d/373-stagelog.txt`, this record; mirrors
under `plugin/lib/**` regenerated by `tools/sync_plugin.py`. Hot files untouched (`tools/frontdoor.py` already
prints every `manifest_paths` key, so `edit.log` shows up in the human summary for free); `src/rvt/frontdoor/__init__.py`
untouched (the edit route's log path rides through `run_edit`'s return dict → `edit_manifest` → `write_manifest`).

## eng #373 — 2026-08-10 (fresh cloud clone, no `samples/`, `main` @ 6d5b82b)

### The gap

Three copies of one idiom, two semantics. `router.py::_stage_stdout` (#188/#330) and `build.py::_stage_stdout`
(#312, a deliberate copy) each opened `<out>/{route,build}.log` line-buffered utf-8/backslashreplace, caught
`OSError` into an in-memory sink + a note (`res.errors` for the router, `res.degradations` for the build), and
named the path. `edit.py::run_edit` still ran the job runner under `redirect_stdout(io.StringIO())` and stuffed
the text into `run["log"]` → `manifest.edit.log_tail[-2000:]` — so `manifest.build.log` was a *path* and
`manifest.edit.log_tail` was *content*, and a quiet edit wrote no log file at all. And the #374 review's edge:
the build's log opens **after** `standalone.forbid_research_inputs` is armed, whose audit hook raises
`StandaloneError(RuntimeError)` — not `OSError` — from inside `open()`, so `--out …/samples/x --json` gave
**rc 1 + a 1,492 B traceback ending `StandaloneError: … samples/x/build.log`, 0 B on stdout, no manifest**
(reproduced on `main` @ 6d5b82b before touching anything).

### What was built

* `src/rvt/frontdoor/stagelog.py` (66 lines, stdlib only, imports nothing from the rest of `rvt.frontdoor`) —
  `stage_stdout(out_dir, name, *, quiet, on_open=None, on_degrade=None) -> ContextManager`: not quiet →
  `nullcontext()`; quiet → `open(<out_dir>/<name>, "w", buffering=1, encoding="utf-8", errors="backslashreplace")`
  **at call time**, `on_open(path)`, `redirect_stdout` into it for the block; any exception from `open()`
  (`OSError`, or whatever an audit hook raises — the tripwire's `StandaloneError`) → `on_degrade("<name> not
  writable (<Exc>: …); stage output not logged")` + an `io.StringIO()` sink. The `except` is deliberately
  `Exception`, not `(OSError, StandaloneError)`: importing `standalone` here would drag the front door into a
  leaf module (`tools/rvt_job.py` wants this helper without `rvt.frontdoor` on its hot path, see follow-ups), and
  the law is about the *log*, not about which layer refused it — an observer that cannot open never costs the run.
* `router.py::_stage_stdout` / `build.py::_stage_stdout` are now 3-line wrappers (`on_open` = name the path where
  each result keeps it, `on_degrade` = the result's non-fatal list). Behaviour unchanged for the build. **Router:
  the unwritable-dir note moved from `res.errors` to `res.caveats`** — the router's existing non-fatal channel
  (matrix cell caveats and every absorbed `build.degradations` entry already land there, `tools/route.py run`
  prints them as `caveat :` lines); `errors` stays for stage failures. `tests/test_router.py::test_quiet_route_into_readonly_dir_still_runs_its_stage`
  adjusted accordingly (asserts the caveat, and that no `route.log`/`route crashed` line is in `errors`; the
  pre-existing `route manifest write failed: PermissionError` error for a wholly read-only dir is untouched).
  `build.py` drops its now-unused `import io`.
* `edit.py::run_edit(quiet=True)` runs `J.main(argv)` under `stage_stdout(out_dir, "edit.log", …)` and returns
  `log_path` + `degradations` instead of `log` (content). `manifest.edit` gains `log` (the path, `_relp`'d like
  `build.log`) and `degradations` (the one note when the log could not open, else `[]`); **`log_tail` is gone** —
  the DONE allowed keeping it one release as a view of the file, `/simplify` found no consumer of it anywhere in
  `src/ tools/ plugin/ skills/` (only this PR's own tests), and keeping it meant a whole-file read at manifest time plus
  up to 2 KB of the log duplicated into every `manifest.json` (10,092 B → 8,368 B on the pinned-base edit); the path is
  the contract now, exactly as for the build. `write_manifest` names the log beside `json` / `md` for whichever of
  `build` / `edit` recorded one, keyed by **the recorded path's own basename** (no second literal `"edit.log"` in
  `manifest.py` to drift from the one passed to `stage_stdout`); `MANIFEST.md`'s Edit section prints
  `- **degradation**: …` lines and `- job log (plan / commit / gate progress): <path>`.
* `tests/test_router.py::test_frontdoor_json_stdout_stays_one_document` deleted (the weaker twin of
  `tests/test_frontdoor.py::test_cli_json_is_one_document_on_stdout_and_nothing_on_stderr`; ~3.5 s no longer paid twice
  in the shard).
* New tests: `tests/test_stagelog.py` (5: verbose null context; quiet streams into the named file, opened at call
  time, tail-able mid-block, a lone surrogate escaped not raised; read-only dir → block runs, ONE note, no file,
  nothing on stdout; **tripwire armed + `…/samples/x` → identical degradation with `StandaloneError` in the note**;
  callbacks optional / missing dir silent) — fresh-clone safe, in the shard via `tests/ci_shard.d/373-stagelog.txt`.
  `tests/test_frontdoor.py` §8c (3): quiet edit on the pinned 2025 base writes `edit.log`, `manifest_paths["edit.log"]`,
  `manifest.edit.{log,degradations}` (and no `log_tail` key), `--verbose` writes none and leaks progress live as before; an
  unopenable `edit.log` (squatted by a directory — an `OSError` under any uid, so it runs as root too) → edit
  **delivered**, ONE degradation, no log named, stdout clean; `--out <tmp>/samples/x --json` at the CLI → rc 3, stderr
  `""`, ONE JSON, manifest written, no traceback — the envelope only; today that JSON says `NO-OUTPUT` with a
  `build.log not writable (StandaloneError…` degradation, and whether it should instead deliver is #425 (below).

### Evidence (this VM, repo `.venv` = CPython 3.11.15; "as nobody" = `runuser -u nobody`, so `chmod 0555` bites)

**The three routes, `--json`, uid nobody, this head:**

| call | rc | stdout | stderr | `manifest` keys in the JSON | log |
|---|---|---|---|---|---|
| `frontdoor.py author --prompt "an electrical room with 6 panels" --out X --json` | 0 | one JSON 2,530 B | 0 B | `json, md, build.log` | `X/build.log` 2,250 B, 17 `[ifc_intent]` lines (unchanged vs main) |
| `route.py run --output rfa --prompt "a 225 A panelboard" --out X --json` | 0 | one JSON | 0 B | `ROUTE.md, route.json, route.log` | `X/route.log` 105 B (`[ifc_intent] F PP-1 -> … validate=VALID`) |
| `frontdoor.py author --rvt plugin/assets/genesis/G_ABPD_2025.rvt --edit "set level 311 elevation to 5 ft" --out X --json` | 0 | one JSON 1,309 B (main: 1,183 B — the one `edit.log` key + path) | 0 B | `json, md, edit.log` (main: `json, md`) | `X/edit.log` 1,626 B, 18 lines (7 `[rvt_job]` + gate lines) — the exact text main put in `log_tail` (diffed: only the out-dir path differs); `manifest.json` 8,368 B (main 10,092 B: `log_tail` gone) |

**Log alone unopenable (`X/<name>.log` pre-created as a directory), uid nobody:** all three rc 0, stderr 0 B, ONE JSON,
output **delivered** (`files` = `families_dir, combined` / `intent, families_dir, rfa:PP-1` / `edited`), no log key
in `manifest`, exactly one note: `build.degradations` ∋ `build.log not writable (IsADirectoryError…`,
`caveats` ∋ `route.log not writable (…`, `edit.degradations == ["edit.log not writable (IsADirectoryError…"]`.

**`--out` under a quarantined name (`…/samples/x`), uid nobody:**

| route | main @ 6d5b82b | this head |
|---|---|---|
| prompt (`--prompt "…1 panel" --out …/samples/x1 --json`, 3 runs) | **rc 1**, stdout 0 B, stderr 1,492 B traceback `StandaloneError: … samples/x/build.log`, no manifest | **rc 3**, stdout ONE JSON `NO-OUTPUT (see build.degradations / errors)`, stderr 0 B, `manifest` = `json, md`, `build.degradations` ∋ `build.log not writable (StandaloneError: standalone build touched a research-machine input: …/samples/x1/build.log …); stage output not logged`; 1.39 / 1.36 / 1.35 s |
| edit (`--rvt G_ABPD_2025.rvt --edit … --out …/samples/e --json`) | rc 0 (the edit route arms no tripwire) | rc 0, `manifest` = `json, md, edit.log`, `files` = `edited` |
| router (`route.py run --output rfa … --out …/samples/r --json`) | rc 0 | rc 0, `route.log` written |

NO-OUTPUT is what `main` gave before #374 (the regression fixed here was only the envelope), but it is **not** a law
worth keeping: the tripwire's documented rule is about *reading research inputs*, yet its hook also refuses the job's own
output *writes* under such a name (the same manifest carries four more `StandaloneError … touched a research-machine
input` notes about `_stages/stage_P_identity.rvt`, `stage_L_loaded.rvt.load.json`, `prompt_room.rvt`), and the arming
call in `build.py` already has an `allow=` list that could exempt the out dir — with the catch that a bare prefix
exemption would let `--out samples` unguard the real quarantine dir. That decision (deliver vs refuse up front with one
line) is outside "the two helpers only" and is filed as #425; the test added here pins only the envelope so #425 need
not invert it. `except Exception` around the one `open()` stays as the observer's last resort either way.

**Wall time (S-2026-08-09-g) — unchanged.** Interleaved A/B/A/B on a quiet VM, fresh out dir per run:

| job | main @ 6d5b82b (runs; median) | this head (runs; median) |
|---|---|---|
| repo `.venv`: `frontdoor.py author --prompt "…6 panels" --json` | 5.87 5.33 4.99 · 4.82 4.99 5.03; **5.01 s** | 4.98 5.07 4.91 · 5.33 4.99 4.94; **4.99 s** |
| repo `.venv`: `frontdoor.py author --rvt G_ABPD_2025.rvt --edit … --json` | 1.31 1.21 1.54 · 1.19 1.21 1.39; **1.26 s** | 1.14 1.14 1.19 · 1.16 1.24 1.16; **1.16 s** |
| bare unzip of each build's `tekton-plugin.zip`, system `python3` 3.11.15, `env -u PYTHONPATH -u TEKTON_ROOT`: `go author --prompt "…6 panels" --json` | 6.40 5.36 5.50 · 5.43 5.13 5.67; **5.47 s** | 5.10 5.21 5.28 · 5.47 5.28 5.14; **5.25 s** |
| bare: `go author --rvt assets/genesis/G_ABPD_2025.rvt --edit "set level 311 elevation to 5 ft" --json` | 1.51 1.44 1.36 · 1.28 1.24 1.17; **1.32 s** | 1.24 1.21 1.31 · 1.20 1.32 1.32; **1.28 s** |
| bare: `go edit assets/genesis/G_ABPD_2025.rvt set-level --id 1351691 --elevation-ft 5 -o …` (1 run each pass) | 0.96 · 1.03 s | 0.97 · 1.04 s |

Every bare `go` call: exit 0, `go.stdout` / `go.stderr` absent from the JSON (nothing leaked), `result.manifest`
= `json, md, build.log` / `json, md, edit.log`.

**Bytes.** The edit route's output is **byte-identical** main vs this head (`cmp` clean on `G_ABPD_2025.edited.rvt`,
598,016 B, both from the repo run and from the bare `go` run). The prompt build's `prompt_room.rvt` is *not*
byte-comparable run-to-run even on one build (two consecutive runs of this head differ in 167,115 of 606,208 bytes —
compressed streams over per-run stamps; determinism is #9's subject), so the claim there is the weaker one that
holds: same size, same manifest shape, `validate` 0 errors on `combined`, and no code on the byte-writing path changed
(the diff touches only where `sys.stdout` points while the stages run).

### Gates (final code, after `/simplify` + `/verify`)

* `RVT_SKIP_LARGE=1 .venv/bin/python -m pytest tests/test_frontdoor.py tests/test_stagelog.py tests/test_router.py tests/test_go_edit.py tests/test_edit_own_release.py tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_coldstart.py tests/test_surface_perf.py tests/test_shard_list.py -q -rs`
  → **254 passed, 23 skipped** in 117 s — every skip pre-existing: `RVT_SKIP_LARGE` / `samples/` absent /
  no numpy-capable bare `python3` for `test_surface_perf.py` (5) / the three `chmod 0555` cases as root.
* As uid `nobody` (so nothing skips on `chmod`): `tests/test_stagelog.py` + the router read-only case + the five
  `test_frontdoor.py` log cases (8b/8c) → **11 passed, 0 skipped** in 4.3 s.
* `tools/sync_plugin.py` (5 files synced incl. the new `plugin/lib/src/rvt/frontdoor/stagelog.py`, deny-audit clean,
  identity scan == allowlist, zip 5,207 KB) then `--check` → "plugin in sync with source";
  `plugin/scripts/validate_plugin.py` → PASS (25 assertions); `tools/dev/check_portable_paths.py` → ok (2,886 paths);
  `tools/dev/shard_list.py --print` lists `tests/test_stagelog.py`.
* `/verify` on the final tree: `frontdoor.py author --prompt "…6 panels" --out out/verify/p --json` → rc 0, stderr 0 B,
  `PROOF-ONLY (self-checks PASS…)`, `manifest` = `build.log, json, md`, `build.log` 17 `[ifc_intent]` lines,
  `rvt_validate.py prompt_room.rvt` → `VALID (no errors); warnings=1` (the known DataStorage decoder gap);
  `… --rvt G_ABPD_2025.rvt --edit "set level 311 elevation to 5 ft" --out out/verify/e --json` → rc 0, stderr 0 B,
  `manifest` = `edit.log, json, md`, `edit.log` 18 lines, `rvt_validate.py` ok (0 errors);
  `route.py run --output rvt --prompt "…6 panels" --json` → ok, `manifest` = `ROUTE.md, author:build.log, author:json,
  author:md, route.json, route.log`. Bare unzip of the rebuilt `tekton-plugin.zip`, system `python3`, out dirs with
  spaces: `go author --prompt "…6 panels"` → `go.ready true`, exit 0, no `go.stdout`/`go.stderr`, `result.manifest` =
  `build.log, json, md`, `files` = `families_dir, combined`; `go author --rvt assets/genesis/G_ABPD_2024.rvt --edit
  "set level 1351691 elevation to 5 ft"` → exit 0, `result.manifest` = `edit.log, json, md`, `files` = `edited`; the same
  with `<out>/edit.log` squatted by a directory → exit 0, delivered, `manifest` = `json, md`,
  `edit.degradations == ["edit.log not writable (IsADirectoryError…"]`.
* Full suite NOT run (SUITE-COORDINATION).

### Findings / follow-ups (outside this territory — noted, not done)

1. **Fourth and fifth copies remain.** `tools/rvt_job.py::_open_job_log` (#267/#396: `<out>.log` for `--json`,
   same open-or-StringIO idiom) is NO-GO this wave (eng #266 holds `rvt_job.py`), and `router.py::_r_spec_on_rvt_seed`
   still buffers the spec+rvt seed job into a `StringIO` and writes `job-create.log` *after* the run (outside "the two
   helpers only"). Both can call `stage_stdout`; the one constraint (#267's note) is that `rvt_job.py` must not import
   the `rvt.frontdoor` package on its hot path — measured here: `import rvt.frontdoor.stagelog` costs **~47 ms / +59
   modules** over bare `import rvt` because the package `__init__` runs, although `stagelog.py` itself is stdlib-only.
   So the follow-up is: move the body to a leaf `src/rvt/_logsink.py`, keep `rvt.frontdoor.stagelog` as a re-export,
   switch those two call sites. Filed as **#424** (`Refs #373`).
2. **A wholly read-only `--out` still tracebacks at the CLI, before and after, identically** — not in the log helper:
   prompt route at `build.py: build_intent → os.makedirs(<out>/_stages)` (before the stage `try`), edit route at
   `__init__._route_rvt_inner → write_manifest → open(manifest.json)`; the router alone answers with ONE JSON
   (`FAILED (PermissionError: … intent.json)`, rc 3). That is #209's envelope ("an exception AFTER/around the build
   still returns one --json result"), so it is recorded there as a comment with these two sites rather than filed twice.
3. **The quarantined-name `--out` should deliver or be refused up front**, not NO-OUTPUT with misleading notes — **#425**
   (`Refs #373`, from the `/simplify` altitude lens; see the paragraph under the quarantined table above).

### `/simplify` (4 lenses) — applied / skipped

Applied: `log_tail` + `_log_tail` + `LOG_TAIL_CHARS` dropped (simplification #4 / altitude Q4 — derivable state with no
consumer); `write_manifest` names the log from the recorded path's basename instead of re-deriving `f"{block}.log"`
(reuse #1 / altitude #3); the quarantined-`--out` test loosened to the envelope (altitude #1) and #425 filed for the
real depth. Skipped, with reasons: replacing the `on_open`/`on_degrade` callbacks by a value-bearing context manager
(`cap.path` / `cap.note`; simplification #1, altitude #2) — the issue's DONE names exactly this callback signature and the
≤3-line wrappers, and both shapes are correct; inlining the two wrappers and trimming their pre-existing wrapper-level
tests (simplification #2/#3, reuse #3) — the DONE requires the wrappers and "existing tests stay green untouched"; a
shared read-only-dir/tripwire pytest fixture across three files (reuse #3) — wider than this diff; folding the
quarantined subprocess case into the existing `--json` subprocess test (reuse #4) — it costs ~1.4 s (fails at stage P)
and is the only end-to-end check of the envelope the issue asks for; the two out-of-territory copies (simplification #5)
— #424. Efficiency lens: clean (stagelog import 0.23 ms self and off the plugin cold path; one line-buffered write per
progress line ≈ +0.3 ms per edit; no retained closures).

## BRANCH STATE (eng #373)

* Branch `cam/373-stagelog` from `origin/main` @ 6d5b82b.
* Files written: `src/rvt/frontdoor/stagelog.py` (new), `src/rvt/frontdoor/{router,build,edit,manifest}.py`,
  `tests/test_stagelog.py` (new), `tests/ci_shard.d/373-stagelog.txt` (new), `tests/test_frontdoor.py`,
  `tests/test_router.py`, `docs/inbox/frontdoor-stagelog.md` (this file); regenerated mirrors
  `plugin/lib/src/rvt/frontdoor/{stagelog,router,build,edit,manifest}.py`.
* Gates: as listed above, all green; `sync_plugin.py --check` clean; nothing staged for the viewer (no format bytes
  changed; no certification claim).
* Shipped vs staged: everything in this PR ships; nothing awaits a human.
