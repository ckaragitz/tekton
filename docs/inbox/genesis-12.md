# genesis-12 — THE RESIDUE ENDGAME + THE WALLS+FAMILIES BUG — workstream record

Stream: **GENESIS-12** (2026-08-04).  Charter: (1) RESIDUE ROUND 3 on the
viewer-certified composed base `experiments/genesis/subst_k4/compose/
G_ABPD.rvt` — census the remaining Autodesk-authored elements, author
constructors for the reachable classes (`src/rvt/genesis/residue_c.py`), emit
in-place rungs + the cumulative `RC_deep`, retire the 4 named stragglers by
the reduction law (clean-header rung first); (2) name THE WALLS+FAMILIES
MECHANISM from a byte-level diff and prove it with two probes on the certified
walls-only base (with-fix / without-fix); (3) emit a wall-render probe:
G_ABPD + one created wall carrying the authored seq-103 B-rep solid
(`rvt.render.brep` in a wrapper; `rvt/mutate.py` untouched).

**Territory touched ONLY:** `src/rvt/genesis/residue_c.py` (new),
`tests/test_residue_c.py` (new, 17 pass), `experiments/genesis/subst_k4/
residue_c/**` (6 probes + control + census + reports + deletion specs +
probes.json), `experiments/render/g12/**` (3 probes + control + reports +
probes.json + the builder script), this record.  Sanctioned regenerations
run per standing rule: `tools/sync_plugin.py` (the new `src/rvt` module) and
`rvt.objlint` invariant MINING for five family classes (new data files under
`experiments/genesis/lint/invariants/{Family,FamilySymbol,FamilySurrogate,
FamSymSurrogate,ParamElemFamily}.json` — additive, generated by the linter's
own `mine_all`, no existing file edited).  No browser / viewer use; every
probe waits on the orchestrator's gate.

## Result in one screen

* **CENSUS (byte ground truth).** G_ABPD holds 3,102 host elements; **422
  carry seq-102 object bytes byte-identical to K4's** (the pre-substitution
  ancestor = Autodesk's serialization).  Dispositions: **349 MACHINERY**
  (no free value — retired by reproduction in certified rungs), **35
  COINCIDENT** (our library value = the published designation: NEC wire
  insulation codes, ASME pipe schedules, 'Arial'…), **27 RC-YEAR + 2
  RC-PREVIEW** (genuine Autodesk free values still aboard — REACHED this
  round), **9 STRAGGLER** (deleted this round).  `residue_c/census.json`.
* **A residue-A2 claim is CORRECTED:** `BuildingOperatingYearSchedule` was
  reproduced as "pure machinery, no free value" — but `m_scheduleName`
  carries Autodesk's schedule names ('Off - 24 Hours', 'Common Office
  Occupancy - 8 AM to 5 PM'…) and the German corpus file has them
  LOCALIZED ('Aus - 24 Stunden') ⇒ a free value.  `RC_year` lands OUR
  names (each year schedule takes its wrapped GEN day schedule's name).
* **RC rungs, all VALID (validator 0 errors), byte-delta asserted (only the
  landed records change; Latest + ElemTable byte-identical), 37/37
  constructed records round-trip byte-exact:** `RC_year` (27), `RC_preview`
  (2 type previews, the Revit-rendered PNG cache of the formerly-Autodesk
  types NULLED — null is corpus-normal: dach 2 / rme 4), `RC_hdr` (the
  CLEAN-HEADER rung: seq-101 on our six view slots 230 / 69851 / 245443 /
  1064656 / 1454508 / 1457028, dropping the RvtLinkSymbol deletion-parent),
  `RC_area` (OUR AreaMeasureElem 9490 unwired from topology 9744, seq
  101+102), `RC_deep` (all 36 slots at once).
* **THE STRAGGLERS ARE RETIRED LAWFULLY.**  `RC_zero.rvt` =
  `tools/genesis_compose.py compose(base=G_ABPD, rung=RC_deep-delta,
  3 maxgc deletion specs)` — **COMPOSED-VALID, reduce-law EDIT-FREE
  (removed 11 / added 0 / survivors edited 0), validator 0 errors AND 0
  WARNINGS** (the standing DataStorage ES-blob warning leaves with the
  blob): RvtLinkSymbol 1250029 (+ its `G:\Dropbox\Autodesk\…` identity
  leak), DataStorage 1382860, and the room constellation {9744, RoomElem
  1004910, LevelRoomPlan 1004909, CurveElems 1004904-08, SketchPlane
  245433} atomically.  RvtLinkInstance 1250030 + CopyWatchProperties
  1250031 were ALREADY retired (D_links ⊂ D_all — verified ABSENT).
  After RC_zero: 3,091 elements, ZERO never-landed Autodesk elements.
* **THE WALLS+FAMILIES MECHANISM IS NAMED** (§2): `rvt.famgen.loader` nulls
  TWO owned-pointer slots on the host family layer that Autodesk's
  serializer NEVER nulls — `Family.m_oFamDimConstrMgr` (present 831/831
  corpus host Families) and `FamilySymbol.m_pMoveRestrictions` (present
  2710/2710 corpus symbols) — the ONLY CRITICAL `rvt.objlint` violations,
  present in EVERY audit-failed loaded-family file (stage_W 8/8+8/8,
  F_lp4, F_msb, R_inst_box, stage_L8) and ABSENT from every audit-passed
  one (L1a / L_v2 / L_downlight — `rvt.famload` writes the lawful empty
  forms).  Proof probes built: `WF_nofix.rvt` / `WF_fix.rvt` on the
  certified walls-only base, differing in EXACTLY the two seq-102 records
  (asserted byte-for-byte; objlint A/B: 4 CRITICAL findings → 0).
* **THE WALL-SOLID PROBE:** `W1_gabpd_wall_solid.rvt` = certified G_ABPD +
  one created wall (R5-specimen clone, mode 'min') whose seq-103 rep is the
  authored GElement six-face B-rep (`rvt.render.brep.wall_rep_from_object`,
  root category −1 — the RSOLID-certified form) applied as a POST-COMMIT
  WRAPPER via `regadd.substitute_elements(seqs=(103,))`; read-back
  `brep / baked / 1 solid / 6 faces / 12 edges`, validator 0 errors.
  The create-time `mutate.py` diff is in §4 (NOT applied).

Reproduce: `.venv/bin/python -m rvt.genesis.residue_c` (~25 s) and
`.venv/bin/python experiments/render/g12/build_g12_probes.py` (~16 s).

---

## §1  RESIDUE ROUND 3 (task 1)

### §1.1  The census, and what "residue" honestly means now

Method: for every host element id of G_ABPD, byte-compare its seq-102 object
payload against K4's record at the same id (the in-place ladder never moved
anything, so K4 is the byte baseline of every rung).  422/3,102 identical.
Cross-referenced against every certified rung report's `landed_slots` (Y1..Y9,
ZA_deep, ZC_deep, the 8 Group-B buckets, palette v2): **411 of the 422 were
landed** — i.e. a certified rung either REPRODUCED them as no-free-value
machinery (the Group-B finding-8 method) or landed our library value that
coincides with the published fact.  Only **11 were never landed**: the 9
stragglers + the 2 type previews.

| disposition | n | classes (top) | retirement |
|---|--:|---|---|
| MACHINERY | 349 | ParamBinding 208, CategoryElem 32, SketchPlane 21, GStyleElem 9, AreaTypeElem 8, DBDrawing 6, trackers/settings singletons ~40 | already retired by certified reproduction |
| COINCIDENT | 35 | RbsWireInsulationType 24, RbsPipe* 6, RbsWireTemp* 3, RbsWireMaterial 1, FontElem 1 | ours by authorship (Z_mepcat / ZA_deep), identical by coincidence of designation facts |
| **RC-YEAR** | **27** | BuildingOperatingYearSchedule | **RC_year (this round)** |
| **RC-PREVIEW** | **2** | LegendComponent 907404 / 1201130 | **RC_preview (this round)** |
| **STRAGGLER** | **9** | link symbol, DataStorage, topology + room curves/plan | **RC_zero deletion (this round)** |

(The 9 byte-identical stragglers + RoomElem 1004910 and SketchPlane 245433 —
which were landed/changed but belong to the room constellation — make the 11
deleted elements.)  Verdict #24's "~260 remaining" was an estimate; the
measured number is **422 Autodesk-serialization-identical, of which 38 carried
genuine Autodesk free values or were sample-placed content** — all 38 are
retired by this round's rungs + deletions.  Measured on the emitted RC_zero:
**3,091 elements, exactly 384 identical-to-K4 = the 349 MACHINERY + 35
COINCIDENT set and nothing else** — zero year schedules, zero previews, zero
stragglers.  The element-layer census after RC_zero is
**machinery-by-reproduction + coincident-facts ONLY** (and the two shipped
product corpora = counsel C4, untouched by design).

### §1.2  The evidence for the two reached classes

* **Year schedules** [V]: each of the 27 wraps exactly ONE day schedule (365
  uniform slot ids) whose name is already OURS (ZC_hvac landed 'GEN Off - 24
  Hours', …), while `m_scheduleName` on the YEAR schedule still reads
  Autodesk's ('Off - 24 Hours', 'On - 6 AM to 10 PM', …).  dach carries
  'Aus - 24 Stunden' / 'Ein - 06:00 bis 22:00 Uhr' ⇒ localized ⇒ free value,
  not an interface enum.  Constructor: name := the wrapped GEN day
  schedule's name; every other byte of the object reproduced.
* **Type previews** [V]: `LegendComponent.m_oPreviewImage` = an
  `ARasterImage` carrying a 128×128 zlib PNG **rendered by Revit from the
  type** — the types at 600634/1201129 are OURS since ZC_systype, so the
  cache is stale Autodesk-rendered imagery.  The NULL-image form appears in
  real files (dach 2, rme 4 of 308/158 LegendComponents measured) ⇒
  reader-accepted; previews regenerate.  Constructor: image := null,
  wiring (m_componentType, geom steps) reproduced.
* NOT reached, and why (census notes): `NumberingSchema.m_inInterfaceName`
  ('Rebar Numbering'…) and `RevisionNumberingSequence.m_sequenceName`
  ('Numeric'/'Alphanumeric') are **identical in the German file** ⇒
  interface constants / unlocalized shipped defaults, machinery-reproduced
  by Z_machinery / Y5 — correctly.

### §1.3  The straggler mechanics (the reduction law, applied)

The two blocking pins named by the deletion stream were both OUR OWN slots'
inherited state — fixed in place, then deleted:

1. **Clean-header rung** (`RC_hdr`): the six seq-101 headers each named
   1250029 once, at `m_parents.m_deletion[i]` (measured; the drafting/sheet
   view 1457028 included — its seq-102 pin was already emptied by
   ZC_browse).  Re-emitted as OUR headers minus that entry (all six headers
   re-encode byte-exact before the edit — the codec proof).  After RC_hdr:
   **1250029 has ZERO referrers** (typed-graph scan on RC_deep).
2. **Area-wiring rung** (`RC_area`): 9490's `m_areaSchemePlanTopologyElemId`
   9744 → −1 + header parent dropped.  After it: **9744's only referrers are
   the room constellation itself** ⇒ the maxgc seed {9744 + room content}
   closes with 0 pinned.
3. `RC_zero` composes RC_deep's in-place layer + three `maxgc` deletion
   specs (`RC_del_link` seed {1250029}, `RC_del_datastorage` seed {1382860},
   `RC_del_room` seed = the 9-element constellation) in ONE deterministic
   build from certified G_ABPD.  Deleted exactly 11; reduce-law EDIT-FREE;
   four-registry coherent; `RC_zero` validator **0 errors / 0 warnings**;
   typed-graph scan on RC_zero: no survivor references any deleted id.

### §1.4  The batch (residue_c/probes.json — gate-ADMISSIBLE)

Base = G_ABPD `[certified]` for every probe; control
`CTRL_G_ABPD_base.rvt` (byte-identical copy).  Upload order bisection-first:
**CTRL → RC_zero → RC_deep → RC_year → RC_preview → RC_hdr → RC_area.**
RC_zero PASS retires the element-layer endgame in one verdict; RC_zero FAIL
with RC_deep PASS convicts the deletion layer (per-spec evidence blocks in
`RC_zero.manifest.json`); RC_deep FAIL reads through the four singles.
md5s: RC_year `48716d09…`, RC_preview `f7d3b4ed…`, RC_hdr `c4908761…`,
RC_area `e12ac524…`, RC_deep `3547df82…`, RC_zero `c599554c…`.

Stage command (control pinned to the batch's own base, NOT the gate's
recency default):

```
.venv/bin/python tools/probe_batch.py stage \
    experiments/genesis/subst_k4/residue_c/RC_{zero,deep,year,preview,hdr,area}.rvt \
    --control-from experiments/genesis/subst_k4/compose/G_ABPD.rvt
.venv/bin/python tools/probe_batch.py stage \
    experiments/render/g12/{WF_nofix,WF_fix,W1_gabpd_wall_solid}.rvt \
    --control-from experiments/ifc_room/electrical_room_2500a_walls_only.rvt
```

---

## §2  THE WALLS+FAMILIES BUG (task 2) — the mechanism, named and instrumented

### §2.1  The diff chain that led to it

1. `F_lp4` (FAIL) vs `walls_only` (PASS) — the charter's diff — at every
   level: the delta is EXACTLY the loaded-family layer (+1 save unit,
   +ContentDocuments entry, +ContentTable record, +FamilyMgr entry,
   +ElementTrackingData symbol registration, +22 host elements, +ElemTable
   rows, 0 existing records changed).  Four-registry census: counts AND
   GUID sets coherent in every file, passing or failing — **coherence is
   not the discriminator.**
2. The same delta computed in the audit-PASSING context (`L1a` vs the rst
   sample): same structures, same shapes — ContentRec, FamilyMgr entry,
   tracking-data entry, ElemTable rows (episode 1016, owner webs) all
   pattern-identical.  **The registration layer is not the discriminator.**
3. The build-lineage cut: every audit-FAILED loaded-family file was built by
   `rvt.famgen.loader` (the room's mechanism); every audit-PASSED one by
   `rvt.famload` (L1a / L_v2 / L_downlight — on the rst base, under a real
   audit with thousands of live elements).  `R_inst_downlight`
   (famload-on-ZA_deep) was never viewer-judged — the base/loader confound.
4. Field-level A/B of the two loaders' output ON THE SAME BASE
   (`R_inst_panel` vs `R_inst_downlight`) + the Autodesk specimen (rme host
   Family 454674) + a two-sample nullability census + `rvt.objlint` mined
   invariants for the five host classes (831 Family / 2,710 FamilySymbol /
   848 + 1,285 surrogate / 4,770 ParamElemFamily specimens, six samples):

| field | corpus | famload (audit-PASSED) | famgen.loader (audit-FAILED) |
|---|---|---|---|
| `Family.m_oFamDimConstrMgr` | present **831/831** (identical empty `FamDimConstrMgrImpl`, `m_pFam` weakref 2 — 60/60) | present (empty form) | **NULL** ⇒ objlint CRITICAL ×2 |
| `FamilySymbol.m_pMoveRestrictions` | present **2710/2710** (identical empty 0×3 `Matrix`) | present (empty form) | **NULL** ⇒ objlint CRITICAL ×2 |
| `FamilySymbol.m_geomSteps`/`m_pGeomTable` | present 2710/2710 | **null — and certified-PASS** (L1a/L_downlight) ⇒ rule proven NOT load-bearing (calibration) | present (baked GElement) |
| everything else (surrogates, ParamElemFamily, FamilyInstance, headers, ElemTable rows, registries) | — | no violations | no violations (LOW-only: `m_appVersionAtLoad` −2, `m_refFaces` len 0 — both ALSO in certified famload output ⇒ non-load-bearing) |

5. Presence check across the originals: stage_W **8/8 + 8/8** null,
   F_msb 1/1+1/1, R_inst_box 1/1+1/1, stage_L8 8/8+8/8 (never audited).

### §2.2  The mechanism statement

> `rvt.famgen.loader` authors the host `Family` with
> `m_oFamDimConstrMgr = None` (`author_host_family`, loader.py:736 — it
> classified the constraint manager as "family-document-only state", but the
> corpus keeps an EMPTY one on every HOST family) and never sets
> `FamilySymbol.m_pMoveRestrictions` (`author_family_symbol` — stays null
> from `blank_object`).  Null owned-state that no Autodesk file exhibits is
> exactly what Revit's open-time document-corruption audit rejects (the
> K1-autopsy law: the reader walks owned/neutralised webs), and the audit
> only RUNS when the design is non-empty: a symbol-only file short-circuits
> at 'Design is empty' (stage_L8's non-test), walls alone carry no family
> layer (walls_only PASS), and walls + a loaded family — or an instance + a
> loaded family — forces the walk and FAILS with −1073742517.

Status: the best-evidenced candidate by corpus law + passed/failed
separation + the K1 precedent; the WF pair is the decisive viewer test, and
the branch-if-wrong is pre-staged (§2.4).

### §2.3  The proof probes (experiments/render/g12/)

Both built from the CERTIFIED `electrical_room_2500a_walls_only.rvt` in one
pipeline (`build_g12_probes.py --only fix`):

* `WF_nofix.rvt` (md5 `c2b4ae0a…`) = walls_only + ONE loaded family (the
  bare render box — no connector, no contract; `rvt.famgen.loader`,
  place=False, symbol_solid=True).  Asserted: the two nulls PRESENT
  (family 1472557, symbol 1472562).  Expected viewer verdict: **FAIL**
  (reproduces the bug at the one-family minimal delta).
* `WF_fix.rvt` (md5 `aabb8eb6…`) = the same file with ONLY those two
  seq-102 records repaired in place
  (`residue_c.fix_family_layer_records` → `substitute_elements(seqs=(102,))`
  — the certified mechanism): the corpus-lawful empty `FamDimConstrMgrImpl`
  / `Matrix` supplied.  **Asserted: the pair differs in EXACTLY the two
  records** (stream_diff_report: changed_ids == {1472557, 1472562},
  added/removed none, ElemTable + Latest + embedded units identical);
  objlint CRITICAL findings 4 → 0; both validator 0 errors.
* Reading matrix (probes.json): CTRL fail ⇒ round void.  nofix FAIL + fix
  PASS ⇒ **mechanism proven** (route the two-line fix to the famgen owner,
  §4).  nofix FAIL + fix FAIL ⇒ the null pair is insufficient — next diff
  dimensions, pre-staged: the `FamilyTypeTable` m_pairs shape (famgen 2 vs
  famload 1 rows), the `ConnectorDataCell.m_oRelevantElements` length (ours
  1 vs corpus {11,12,15,18,21}), surrogate record order (famgen: Family…
  FamilySurrogate < FamilySymbol < FamSymSurrogate; famload: surrogates
  before symbol), and the famload-vs-famgen `m_familyIds` pair-set width.
  nofix PASS ⇒ the bug is family-content- or order-dependent — rebuild the
  pair with the room's LP-4 panel product (one `build_room_product` call).

### §2.4  Corrections this stream owes the ledger prose

* Verdict #26's line "ANY embedded family unit combined with created content
  trips the audit" stays TRUE as phenomenology but now has a NAMED candidate
  cause confined to `rvt.famgen.loader`'s host records — famload's output
  has never failed an audit, and famload-on-genesis (`R_inst_downlight`) is
  still unjudged (worth a slot in any round with spare budget: its PASS
  would confirm the loader attribution independently of the WF pair).

---

## §3  WALL RENDER ON THE GENESIS BASE (task 3)

`W1_gabpd_wall_solid.rvt` (md5 `41cbbb4b…`) = certified G_ABPD + one 20-ft
wall (line (4,−12)→(24,−12) ft, height 12 ft, our wall type 600634 'GEN
Wall - Interior Partition 121mm', level 311, R5 specimen clone, mode 'min',
11 tolerated danglers — the walls_only-certified pattern) committed by
`commit_new_elements`, then the WRAPPER: `rvt.render.brep.
wall_rep_from_object(root_category_id=-1, side_material_id=600660)` →
`substitute_elements(seqs=(103,))` swaps the 2-byte SerializedDummy for the
3,060-byte GElement.  Read-back (`rvt.render.inspect`): `brep`, baked, 1
solid / 6 faces / 12 edges; validator 0 errors; `probe_batch check`
ADMISSIBLE (base G_ABPD `[certified]`).  Reading: PASS + a wall node under
'GEN L1' in the model tree + a visible shaded wall = **LOAD+RENDER proven on
the composed genesis base itself** (RSOLID proved it on the walls-only
lineage; this probe carries it onto the endgame base).  FAIL ⇒ bisect
against `RSOLID_walls_A_solid` (same grammar, walls-only base): the delta
would be the composed base's residue layers.

---

## §4  Diffs / actions proposed for files OUTSIDE this territory (NOT applied)

* **`src/rvt/famgen/loader.py` — the two-line walls+families fix** (apply on
  a WF_fix PASS; the forms are exported as
  `rvt.genesis.residue_c.famdim_constr_empty / move_restrictions_empty`):

  1. `author_host_family` (~line 736): replace
     `o["m_oFamDimConstrMgr"] = None` with the corpus empty form::

         o["m_oFamDimConstrMgr"] = _ptr("FamDimConstrMgrImpl", {
             "m_pFam": _weak(2), "m_paramExprs": [], "m_drivenDimSegs": [],
             "m_ignoredDimValueExprs": [], "m_overconstrDimSegs": [],
             "m_fixedRefs": [], "m_redundantConstrs": [],
             "m_propagatedDrivers": [], "m_dimSegDataMap": [],
             "m_additionalParamExprs": [], "m_additionalFixedRefs": []})

  2. `author_family_symbol` (the big `o.update({...})`, next to
     `"m_refs": [], "m_refFaces": []`): add::

         "m_pMoveRestrictions": _ptr("Matrix", {
             "m_nRows": 0, "m_ncols": 3, "m_bUseSingleArray": True,
             "m_arr": [], "m_rows": []}),

     (famload.py:805 already carries this exact form — the audit-passed
     reference.)

* **`src/rvt/mutate.py` — optional create-time wall bake** (task 3's
  charter diff; today the wrapper does it post-commit).  In
  `Document.add_wall`, add a keyword `bake_solid: bool = False,
  side_material_id: int = -1` and, immediately after the `el = NewElement(
  ...)` construction and before `self.new_elements.append(el)`::

      if bake_solid:
          from .render.brep import wall_rep_from_object
          rep3, _geom, _tags = wall_rep_from_object(
              obj, element_id=eid, root_category_id=-1,
              side_material_id=side_material_id, end_material_id=-1,
              with_ref_planes=False)
          el.rep = rep3          # NewElement.rep_class_id switches to GElement

  (`NewElement.records()` already emits a GElement seq-103 when `.rep` is
  set — no other change; the render-emit stream's `bake_planned_wall` is the
  same idea for callers that cannot pass the flag.)

* **`docs/coverage/viewer-certified.json` (orchestrator):** add the nine
  probes as verdicts land; on RC_zero PASS it becomes the deepest certified
  genesis base (and the future default base candidate for the front door,
  after a soak).  Note `stage_L8_lp4`'s existing "certified" entry remains
  the empty-design non-test the render-instances stream documented — avoid
  it as a control source (`probe_batch stage` currently ranks it by
  recency).
* **KNOWLEDGE.md merges:** (i) the census law — *a landed slot can still
  carry an Autodesk free value if the rung reproduced it; the byte census
  vs the ancestor + a per-class free-value check (localization in dach is
  the tell) is the honest residue instrument*; (ii) the year-schedule
  name correction; (iii) the walls+families mechanism paragraph (§2.2) with
  its status (candidate until WF verdicts); (iv) *type-preview
  LegendComponents may carry a null m_oPreviewImage (corpus-normal); a
  Revit-rendered preview cache of a substituted type is stale
  Autodesk-rendered content and is nulled, not reproduced*.
* **`tests/test_engine.py` (owner: the old revit-bridge stream):** its
  module-level `import bridge_lib` now breaks COLLECTION of the whole suite
  — `skills/revit-bridge/scripts` no longer exists after the tekton skill
  rename (skills/ carries only `tekton-ifc`).  Needs a skip-guard or
  retirement; outside this territory.  **RESOLVED between sessions** —
  another stream repaired it (mtime 20:19); this stream's close-out session
  re-ran it: **12 passed**, and collection of the un-ignored suite works
  again.
* **`tools/probe_batch.py` (gate owner, minor):** the default `--control-from`
  picks the NEWEST certified entry; after this round that could be an
  RC probe — fine — but today it suggests `RSOLID_walls_A_solid`; the two
  staged batches name their intended controls explicitly.

## §5  Open questions (the viewer decides)

* The nine verdicts, in the two probes.json orders.  Everything is
  pre-branched per probe (`if_PASS` / `if_FAIL` / the WF reading matrix).
* Whether the reader accepts our year-schedule names at the slots (the last
  value-layer probe of the HVAC web) and the nulled preview caches.
* Whether RC_zero's 11-element deletion (the first deletions ever taken
  FROM the composed base) survives the audit — reduce-law EDIT-FREE and
  danglers-zero by construction.  The room was the base's only placed
  MODEL content, but the datum layer (levels / grids) remains and datum
  draws (verdict #22), so the G_ABPD-style browsable translation — not the
  empty-design short-circuit — is expected; if the orchestrator wants the
  audit forced harder on the post-deletion state, `RC_zero + one baked
  wall` is one `build_g12_probes`-style call away.
* famload-on-genesis (`R_inst_downlight`) — still the cheapest independent
  check of the loader attribution.

## Verification (all run this session, repo root)

```
PYTHONPATH=src .venv/bin/python -m rvt.genesis.residue_c            # census + 5 rungs + RC_zero + batch (~25 s)
PYTHONPATH=src .venv/bin/python -m rvt.genesis.residue_c --demo     # 37/37 records byte-exact round trip
PYTHONPATH=src:tools .venv/bin/python experiments/render/g12/build_g12_probes.py   # WF pair + W1 (~16 s)
.venv/bin/python tools/rvt_validate.py experiments/genesis/subst_k4/residue_c/RC_zero.rvt   # VALID 0 err / 0 warn
.venv/bin/python tools/probe_batch.py check experiments/genesis/subst_k4/residue_c/RC_{zero,deep,year,preview,hdr,area}.rvt   # ADMISSIBLE
.venv/bin/python tools/probe_batch.py check experiments/render/g12/{WF_nofix,WF_fix,W1_gabpd_wall_solid}.rvt                  # ADMISSIBLE
.venv/bin/python -m pytest tests/test_residue_c.py -q               # 17 passed
.venv/bin/python -m pytest tests/ -q --ignore=tests/oracle --ignore=tests/test_engine.py   # see BRANCH STATE
```

## BRANCH STATE

* **status: DONE — READY FOR THE VIEWER QUEUE** (nothing uploaded; both
  batches gate-ADMISSIBLE with certified bases + byte-identical controls).
* **NEW files (territory only):** `src/rvt/genesis/residue_c.py`;
  `tests/test_residue_c.py` (17 pass); `experiments/genesis/subst_k4/
  residue_c/`: `RC_year.rvt`, `RC_preview.rvt`, `RC_hdr.rvt`, `RC_area.rvt`,
  `RC_deep.rvt`, `RC_zero.rvt` (+ `.inplace.rvt` intermediate) + one `.json`
  report each + `RC_zero.manifest.json` + `census.json` + three
  `RC_del_*.spec.json` + `CTRL_G_ABPD_base.rvt` + `probes.json`;
  `experiments/render/g12/`: `build_g12_probes.py`, `WF_nofix.rvt`,
  `WF_fix.rvt`, `W1_gabpd_wall_solid.rvt`, `WF_report.json`,
  `W1_report.json`, `CTRL_walls_only_g12.rvt`, `probes.json`; this record.
  Sanctioned regenerations: `tools/sync_plugin.py` (plugin bundle +
  tekton-plugin.zip, per the standing rule — validation PASS) and five new
  `experiments/genesis/lint/invariants/*.json` (rvt.objlint mine, additive).
* **No existing `src/rvt/*.py`, tool, test, sample or sibling experiment
  edited.**  The two proposed source diffs (famgen loader fix, mutate
  create-time bake) are in §4 as exact patches.
* **gates passed (machine):** every emitted `.rvt` validator VALID 0 errors
  (RC_zero also 0 warnings); every in-place rung's byte-delta assertion
  holds (only the landed records; Latest + ElemTable byte-identical); the
  RC_zero composition COMPOSED-VALID + reduce-law EDIT-FREE (11/0/0);
  straggler referrer scans zero; the WF pair's delta = exactly 2 records;
  W1 read-back baked; 37/37 constructor round trips; both batches
  ADMISSIBLE.  **NOT viewer-tested — no acceptance claim is made.**
* **full suite** (`.venv/bin/python -m pytest tests/ -q --ignore=tests/oracle
  --ignore=tests/test_engine.py`, close-out session 21:03–21:26): **1,316
  passed, 2 failed in 1371 s** — the 2 failures are the SAME two
  pre-existing cross-stream stale fixtures every sibling record lists
  (`tests/test_provenance.py::test_G0_resource_refs_are_counted` and
  `::test_G0_identity_dit_usernames_still_leak`, written against the
  pre-rebuild G0; owner: the provenance stream), **ZERO failures in this
  territory**.  `tests/test_engine.py` — whose collection breakage the first
  session had to `--ignore` — was repaired by another stream between
  sessions (mtime 20:19) and run separately: **12 passed** ⇒ net
  **1,328 pass + 17 of them this stream's + 12 engine / 2 known-stale**.
  (The suite has grown past the sibling-era ~1,222: concurrent streams
  landed famgen-factory / frontdoor-standalone / versions / electrical
  tests while this stream closed out.)
* **DONE check:** residue-C rungs + RC_deep validating ✔; the 4 stragglers
  retired lawfully (1 was already retired by D_all — stated with evidence;
  the other 3 leave in RC_zero, clean-header rung built) ✔; the
  walls+families mechanism named with the two proof probes staged ✔; the
  wall-solid probe staged ✔; manifests for the orchestrator's viewer round ✔.

## CLOSE-OUT VERIFICATION (second session, 2026-08-04 ~21:00–21:30)

The first session was cut off after writing this record but before the full
suite finished.  This close-out session INDEPENDENTLY re-verified every
load-bearing claim from the artifacts (not the stored reports), then ran the
suite:

* md5s of all 10 probes + both controls match this record exactly;
  `CTRL_G_ABPD_base` == G_ABPD and `CTRL_walls_only_g12` == the certified
  walls-only base, byte-identical (md5).
* validator re-run: RC_zero **0 errors / 0 warnings**; RC_deep / RC_year /
  RC_preview / RC_hdr / RC_area and WF_nofix / WF_fix / W1 all **0 errors**
  (1 warning = the base's standing ES-blob decode gap, which leaves with
  the blob in RC_zero).
* `probe_batch check` re-run on BOTH batches: **ADMISSIBLE**, every probe's
  declared base `[certified]`.
* WF pair re-diffed from the bytes (`rvt.regadd.stream_diff_report`):
  changed = exactly {1472557, 1472562}, added/removed none, ElemTable +
  Latest + embedded units identical.  Mechanism's corpus evidence re-read
  from the mined invariants: `Family.m_oFamDimConstrMgr.#ptr` PTR_PRESENT
  support **831/831** (6 files), `FamilySymbol.m_pMoveRestrictions.#ptr`
  PTR_PRESENT support **2710/2710** (6 files).
* W1 re-read from the file: wall 1472525 `has_baked_geometry=True,
  drawable_3d=True`, 3,060 B GElement, 1 solid / 6 faces / 12 edges.
* RC_zero re-counted from the file: G_ABPD 3,102 → RC_zero **3,091**
  elements; all 11 deleted ids absent; 1250030/1250031 confirmed absent
  from BOTH (the D_all retirement).  Identity-leak scan: 'Dropbox' +
  'rac_basic_sample' present only on 1250029 in G_ABPD, **zero hits in
  RC_zero**.
* census re-read: 3,102 hosts / 422 identical-to-K4 / dispositions
  349 MACHINERY + 35 COINCIDENT + 27 RC-YEAR + 2 RC-PREVIEW + 9 STRAGGLER.
  `RC_zero.manifest.json`: COMPOSED-VALID, reduce-law removed 11 / added 0 /
  survivors_edited 0.
* `tests/test_residue_c.py` **17 passed**; `--demo` 37/37 byte-exact;
  `tests/test_plugin_sync.py` **7 passed** (bundle carries
  `residue_c.py` + the render modules — the sync stands).
* full suite: the numbers in BRANCH STATE above (1,316 + 12 pass / 2
  known-stale cross-stream).

Nothing outside the territory was edited in either session; the two source
diffs this stream owes other owners remain §4 patches only.
