# EDIT-TEXT-OWN-RELEASE — tekton-native's `rvt_edit_text.py` performs its size-preserving text edit on a Revit 2025/2024 project under the file's own release (issue #116, Refs #70)

Stream: `edit-text-own-release` (eng #116, engineer session started by the
tech-lead session, 2026-08-10). Territory: `tools/rvt_edit_text.py` (new — the
source the plugin script now mirrors), `tools/sync_plugin.py` (one mapping
line), the regenerated `plugin/skills/tekton-native/scripts/rvt_edit_text.py`
(byte-identical mirror), `tests/test_edit_text_release.py` +
`tests/ci_shard.d/116-edit-text-release.txt`, this record. No hot file touched:
`plugin/skills/tekton-native/SKILL.md` unchanged (the documented invocation
`_bootstrap.py run rvt_edit_text.py in.rvt --old … --new … -o out.rvt` is the
same), `release_ctx.py`, `src/rvt/versions/**`, `rvt_edit.py`, `rvt_job.py`
untouched.

## 0. The defect, reproduced (fresh cloud clone, `origin/main` @ cd2d5a2, before any change)

`plugin/skills/tekton-native/scripts/rvt_edit_text.py` was a hand-authored
plugin one-off (no `tools/` source): it did `from rvt.writer import
BLOCK_TRL_TAG` at module level and ran `StreamWalker` bare, so on anything but
a 2026 file it died before the edit. Run exactly as the skill documents it, as
nobody (`env -i PATH=/usr/bin:/bin python3` = system Python 3.11.15), on the
three tracked certified bases, edit `--old "GEN Floor Plan" --new "OUR Floor
Plan" --utf16`:

| input | `main` |
|---|---|
| `G_ABPD.rvt` (2026) | `SELF-CHECK PASS`; stamps valid 3102/3102; out sha256 `e2698150…fcc87408` |
| `G_ABPD_2025.rvt` | traceback: `rvt/partitions.py:243 parse_stream_header … ValueError: unexpected Partitions header: v=9 cls=0x391` |
| `G_ABPD_2024.rvt` | traceback, same, `cls=0x37b` |

(So the failure mode on `main` is the walker refusal, not a silent 2026-tag
re-frame — the walk dies first.)

## 1. What was built

* **Moved under `tools/` + sync (the issue's suggestion), because it *is* small
  and mechanical:** `tools/rvt_edit_text.py` is the source; `tools/sync_plugin.py`
  gains one `(_tool("rvt_edit_text.py"), "skills/tekton-native/scripts/rvt_edit_text.py")`
  line beside the other tekton-native script mappings; the plugin copy is now a
  generated byte-identical mirror (`cmp` clean, and `--check` covers it from now
  on). The only non-mechanical part of the move is the import header: the
  `_bootstrap.ensure_rvt()` preamble (plugin-only) became the same three-line
  `HERE/../src` + `HERE/../lib/src` header `tools/rvt_edit.py` uses, so the one
  file runs from the repo (`tools/`) and from the plugin through the documented
  `_bootstrap.py run …` launcher (which puts the bundled engine + vendored
  olefile on the path before `runpy`). Consequence, stated honestly: a *direct*
  `python skills/tekton-native/scripts/rvt_edit_text.py …` from a bare unzip no
  longer self-bootstraps — exactly like its sibling `rvt_edit.py` in the same
  directory; the SKILL documents `_bootstrap.py run` for both, and the script's
  own usage text now says so.
* **The fix itself** (the #70 pattern, nothing reinvented): `main()` parses,
  applies the size-preserving refusal, then enters
  `rvt.frontdoor.release_ctx.enter_host_release(stack, in_path)` **once** around
  the whole body (`_edit()`: walk → edit → re-stamp → re-block → truncate →
  `ecc.frame_stream` → `write_cfb` → read-back self-checks). A native (2026)
  file enters no context; a 2025/2024 file gets its release's framing ordinals +
  codecs; an unauthor-able release prints the refusal sentence as a warning and
  fails honestly at the walker as before. The block trailer is written with
  `rvt.partitions.TRAILER_TAG` **read at call time** (the very constant the
  walker that re-checks the output parses trailers with, and one of the names
  `versions.reading` swaps) — no module-level copy of the 2026 `0x0f21`.
  `StreamWalker` is referenced through the module (`P.StreamWalker`) for the
  same reason. `gzip_member`, `ecc`, `write_cfb`, `read_entries` are
  release-agnostic and unchanged. (Review note, deliberately not taken: a bare
  `versions.reading(path)` would be ~0.1 s lighter per 2025/2024 call than the
  full host context, since this job only needs the framing ordinals — but the
  issue's DONE names `enter_host_release` and parity with `rvt_edit.py` is the
  point: one entry, one refusal sentence, one no-leak guarantee across every
  edit CLI. 0.29 s wall end to end as it is.)
* **Test** `tests/test_edit_text_release.py` (4 tests, fresh-clone safe — the
  certified pins via `conftest.CERTIFIED_YEARS` / `pinned_base`, the tool via
  `conftest.load_tool`): the edit the issue names, a same-length UTF-16 rename
  of a **level** (`"GEN B1 - Basement"` → `"OUR B1 - Basement"`, level 1351691,
  present once in seq-102 of all three bases) on 2024, 2025, then 2026 in one
  process → `SELF-CHECK PASS`, CRC/ECC/walker 0, `detect_release(out) == year`,
  standalone `validate_file` 0 errors with no context active, and the level
  **decodes** with the new name under the file's release (the edit landed in
  the element, stamps consistent); an autouse fixture snapshots the
  `rvt.partitions` framing table + `writer.BLOCK_TRL_TAG` + `active_release()`
  around every test (foreign releases run first, so a leak would break the
  following native edit); plus the length-change refusal (exit 2, nothing
  written). Mirror drift is owned by `sync_plugin.py --check` /
  `tests/test_plugin_sync.py` now that the mapping exists — not re-asserted
  here. In the shard via the drop-in `tests/ci_shard.d/116-edit-text-release.txt`
  (never `ci_shard.txt`).

## 2. Evidence (head of this branch; numbers, not adjectives)

**As nobody, from a bare unzip of the rebuilt `tekton-plugin.zip`** (scratch dir,
`env -i PATH=/usr/bin:/bin HOME=<dir> python3` = 3.11.15, no repo on the path,
no olefile installed — the bootstrap's vendored fallback), the issue's run line
`python3 skills/tekton-native/scripts/_bootstrap.py run rvt_edit_text.py
assets/genesis/<base> --old "GEN Floor Plan" --new "OUR Floor Plan" --utf16 -o
out/edited_<y>.rvt`:

| input | head | `run rvt_validate.py out` (bare) | `detect_release` in → out |
|---|---|---|---|
| `G_ABPD.rvt` (2026) | exit 0, `record id=49552 class=0x486 … stamp 0x553085a4 -> 0x58f085c0`, CRC 0 / ECC 0 / walker 0, stamps 3102/3102, `SELF-CHECK PASS`; **out sha256 `e2698150…fcc87408` == main's output byte for byte** | `VALID (no errors); warnings=1 info=2` (the known DataStorage ES-blob decode warning the base itself carries) | 2026 → 2026 |
| `G_ABPD_2025.rvt` | exit 0, `record id=49552 class=0x46a psize=240 @seg+1268881: stamp 0x0408851e -> 0x07c8853a`, CRC 0 / ECC 0 / walker 0, stamps **3316/3316**, `SELF-CHECK PASS` | `VALID (no errors); warnings=0 info=2`; elements_decoded 3316, decode_failures 0, refs_checked 50759 (judged under 2025) | 2025 → 2025 |
| `G_ABPD_2024.rvt` | exit 0, `record id=49552 class=0x451 psize=240 @seg+1182780: stamp 0x22918574 -> 0x26518590`, CRC 0 / ECC 0 / walker 0, stamps **3278/3278**, `SELF-CHECK PASS` | `VALID (no errors); warnings=0 info=2` | 2024 → 2024 |

The same three runs from the repo (`.venv/bin/python tools/rvt_edit_text.py …`)
give identical reports; `tools/rvt_validate.py --json`: `counts.error == 0` on
all three. One 2025 edit end to end: 0.29 s wall.

**Gates:** `tests/test_edit_text_release.py` 4 passed (0.95 s);
neighbours `tests/test_plugin_sync.py tests/test_bootstrap.py
tests/test_coldstart.py tests/test_edit_own_release.py` 39 passed;
`tools/sync_plugin.py` → synced 1 file, deny-audit clean, validation passed, zip
rebuilt (5252 KB), identity scan 0 mismatches; `--check` clean ("plugin in sync
with source"); `plugin/scripts/validate_plugin.py` PASS (25 assertions);
`tools/dev/check_portable_paths.py` ok (2932 → 2936 tracked paths); whole merged
shard (`shard_list.py --print`, 84 files): see BRANCH STATE.

Not claimed: viewer certification of a 2025/2024 *text-edit* output (validator
green is necessary, not sufficient — rule 4). The 2026 recipe is the certified
one (V18/V19); a `needs-viewer` probe for the 2025/2024 edit lane is already
filed as #117 (Refs #70) and covers this script's output equally — no new issue.

## 3. Findings / follow-ups (filed as issues, not done here)

* **`rvt_selfcheck.py` — the SKILL's *mandatory* pre-delivery gate — has the
  same release-blind shape** (`from rvt.partitions import StreamWalker`, bare
  walk, no context) and is the last hand-authored tekton-native one-off besides
  the read-only `rvt_inspect.py`. Reproduced from the bare unzip: `run
  rvt_selfcheck.py assets/genesis/G_ABPD_2025.rvt` → `ValueError: unexpected
  Partitions header: v=9 cls=0x391`, exit 1; **and the same on this branch's
  valid 2025 edit output** — i.e. after this fix a stranger's 2025 text edit
  succeeds and the very next documented step crashes. Out of this issue's
  territory (different script; the brief fences the territory to
  `rvt_edit_text.py`), so filed task-shaped as **#518** (`Refs #116`, `P2 ready
  area:plugin good-first-pick`) with the repro, the DONE, and a pointer to §1's
  move recipe. Until it lands, `run rvt_validate.py out.rvt` (release-aware
  since #50, 0 errors above) is the working gate on 2025/2024 outputs, and
  `rvt_edit_text.py`'s own read-back already performs the same four checks
  under the right release before printing `SELF-CHECK PASS`.

## SUITE RESULT

Per `docs/inbox/SUITE-COORDINATION.md`: no full-suite run. Stream-local +
neighbours + the whole merged CI shard only. Expected full-suite delta: +6
tests; no native-release behaviour change (2026 output byte-identical to
`main`'s).

## BRANCH STATE

* Branch `cam/116-edit-text-own-release` from `origin/main` @ cd2d5a2; one PR,
  `Closes #116`.
* Files: `tools/rvt_edit_text.py` (new source), `tools/sync_plugin.py` (+1
  mapping), `plugin/skills/tekton-native/scripts/rvt_edit_text.py` (regenerated
  mirror), `tests/test_edit_text_release.py` (new),
  `tests/ci_shard.d/116-edit-text-release.txt` (new), this record.
* Gates: listed in §2; merged shard result pasted in the PR body.
* Staged vs shipped: nothing staged for the viewer (see "Not claimed"); the
  plugin zip is a regenerated, git-ignored artifact.

---

# eng #518 (2026-08-10) — `rvt_selfcheck.py`, the SKILL's mandatory pre-delivery gate, judges a file under its own release; moved to `tools/` with a generated mirror (issue #518, Refs #116 / #70)

Stream: `selfcheck-own-release` (eng #518, engineer session started by the
tech-lead session). Written under this stream's own header; nothing above is
edited. Territory: `tools/rvt_selfcheck.py` (new — the source the plugin
script now mirrors), `tools/sync_plugin.py` (one mapping line beside #521's),
the regenerated `plugin/skills/tekton-native/scripts/rvt_selfcheck.py`
(byte-identical mirror), `tests/test_selfcheck_release.py` +
`tests/ci_shard.d/518-selfcheck-release.txt`, this section. No hot file
touched: `plugin/skills/tekton-native/SKILL.md` unchanged (the documented
invocation `_bootstrap.py run rvt_selfcheck.py out/edited.rvt --json
out/selfcheck.json`, exit `0 PASS · 1 FAIL · 2 not a .rvt`, is the same);
`release_ctx.py`, `src/rvt/versions/**`, `rvt_edit_text.py`, `rvt_edit.py`,
`rvt_job.py` untouched. (The issue body suggested a separate record file
`docs/inbox/selfcheck-own-release.md`; the tech-lead brief asked for this
section instead — same finding lineage as §3 above, one file to read.)

## 518.0 The defect, reproduced (fresh cloud clone, `origin/main` @ d75302b — #521 merged — before any change)

Bare unzip of the rebuilt `tekton-plugin.zip`, as nobody (`env -i
PATH=/usr/bin:/bin HOME=<scratch> python3` = 3.11.15, no repo on the path, no
olefile installed), the SKILL's own line `python3
skills/tekton-native/scripts/_bootstrap.py run rvt_selfcheck.py
assets/genesis/<base> --json out/x.json`:

| input | `main` |
|---|---|
| `G_ABPD.rvt` (2026) | exit 0, `22 verified, 0 CRC` · `5 full pages, 0 mismatches` · `14 blocks in 1 partition stream(s), 0 errors` · `3102/3102 stamps` · `VERDICT : PASS` |
| `G_ABPD_2025.rvt` | exit 1, traceback: `rvt_selfcheck.py:97 check_walker → partitions.py:338 __init__ → partitions.py:243 parse_stream_header … ValueError: unexpected Partitions header: v=9 cls=0x391` |
| `G_ABPD_2024.rvt` | exit 1, same traceback, `cls=0x37b` |
| #521's valid 2025 edit output (`run rvt_edit_text.py <2025 base> --old "GEN Floor Plan" --new "OUR Floor Plan" --utf16 -o out.rvt` → `SELF-CHECK PASS`) then `run rvt_selfcheck.py out.rvt` | exit 1, same traceback (`cls=0x391`) — the documented, mandatory next step after a successful edit crashes |
| a **2026** copy with the first 16 bytes of `Partitions/21` zeroed | exit 1, **traceback** too (`unexpected Partitions header: v=0 cls=0x0`) — the damage-judging tool never reached a verdict on a damaged partition header under any release |

## 518.1 What was built

* **Moved under `tools/` + sync**, exactly §1's recipe: `tools/rvt_selfcheck.py`
  is the source; `tools/sync_plugin.py` gains one
  `(_tool("rvt_selfcheck.py"), "skills/tekton-native/scripts/rvt_selfcheck.py")`
  line directly under #521's; the plugin copy is a generated byte-identical
  mirror (`cmp` clean; `--check` owns drift from now on — a hand edit to the
  mirror reads as `PLUGIN DRIFT: 1 file(s) differ from source:
  skills/tekton-native/scripts/rvt_selfcheck.py`, exit 1 — verified). The import
  header changed the same way (`_bootstrap.ensure_rvt()` preamble → the
  `HERE/../src` + `HERE/../lib/src` header), with the same honestly-stated
  consequence: a *direct* `python skills/tekton-native/scripts/rvt_selfcheck.py`
  from a bare unzip no longer self-bootstraps, like its siblings `rvt_edit.py`
  / `rvt_edit_text.py`; the SKILL documents `_bootstrap.py run` for all three
  and the script's usage text now says so.
* **The fix** (the #70 / #116 pattern): `main()` proves the path exists and
  opens as a CFB container (exit 2 otherwise, unchanged), then — unless the
  file is natively framed, see the latency bullet below — enters
  `rvt.frontdoor.release_ctx.enter_host_release(stack, path)` **once** around
  the four checks. A native (2026) file enters no context (stdout, stderr and
  `--json` byte-identical to `main`'s — diffed, see 518.2); a 2025/2024 file is walked
  with its release's `CONTAINER_CLASS` / block / trailer tags because
  `StreamWalker` is referenced through the module (`P.StreamWalker`) and reads
  `rvt.partitions` at call time; an uncertified/undetectable release prints
  the refusal sentence as a `warning:` on stderr and is checked release-blind
  (the walker then fails honestly). `check_gzip` / `check_ecc` are
  release-agnostic and untouched.
* **A damaged file reaches a verdict, never a traceback** (the DONE's "a
  corrupt file still exits 1"): the walker and stamp checks come from **one**
  walk per partition (`check_partitions(doc, stamps=…)` → `(walker report,
  stamp report)`: one inflate per stream instead of the old two, one
  partition held at a time as before, block data kept only when stamps are
  wanted — `--skip-stamps` walks with `keep_data=False`); a partition whose
  stream header cannot be parsed is one walker error carrying the exception
  text (`first_errors: ["ValueError: unexpected Partitions header: v=0
  cls=0x0"]`, `blocks: 0`) → `VERDICT : FAIL`, exit 1, and contributes no
  stamps (already counted). This is the altitude the engine's own *judging* callers
  use (`validate.py` `WalkedFile.walker()` caches walker-or-exception the same
  way); `StreamWalker` raising on a bad header stays its contract for the ~30
  authoring callers. And because this is *the* tool whose input is expected to
  be broken, the release entry itself is guarded: `enter_host_release` only
  translates `ReleaseContextError`, so a 2025 file with a mangled
  `Formats/Latest` raises a raw `ParseError` out of `_codec_triple_from_base`
  (reproduced: `ParseError: parse error at 0x603b: class marker != 0 …`); the
  script reports it as the same stderr `warning: no release context for …` and
  the checks still run and FAIL on their own evidence (CRC 1 · ECC 1 · walker
  1). That guard is an interim at the wrong altitude — the helper should keep
  its own "note, never raise" promise for all six callers — so it is filed as
  **#535** (`Refs #518`, `area:frontdoor`) and the comment cites it; #535's
  DONE ends with deleting the guard here. When a file FAILs *and* was judged
  without its release context, the note is echoed as the last `failures`
  entry (`judged without its release context (…)`) and as `release_note` in
  `--json`, so the verdict explains itself; a note alone never fails a file,
  and a healthy pin's report carries neither (2026 output unchanged).
* **The native path pays nothing for release awareness (S-2026-08-09-g) —
  corrected after review, with the honest before/after.** The first head
  (b8bb320) imported `rvt.frontdoor.release_ctx` eagerly and always called
  `enter_host_release`, and this record claimed "0.25 s (main 0.27 s)" from two
  single wall-clock runs. The tech-lead review measured it properly
  (interleaved, bare unzip): 2026 median **137 ms vs main 97 ms, +40 ms /
  +40 %** — reproduced here at 165 vs 120 ms (7+7 interleaved): the eager
  import pulls ~14–22 extra `rvt.*` modules and `enter_host_release` re-opens
  the container for `detect_release` even on a native file. Fixed at the
  script's own altitude: `natively_framed(doc)` asks the engine's own header
  parser (`P.parse_stream_header` on each partition's logical bytes — no
  layout re-implemented) whether every partition already parses with the
  container class bound in `rvt.partitions`; if so nothing is entered and
  nothing more is imported. Only a foreign or damaged file imports
  `release_ctx` (lazily, inside `enter_files_release`) and enters
  `enter_host_release(stack, path)` once, as the DONE names. Alternatives
  measured and not taken: gating on `V.detect_release(doc)` (reuses the open
  container but imports `rvt.stream_encoders` → schema/writer/roundtrip, 2026
  median 145 ms, still +25); `global_framing.enter_own_release` (parses
  `Formats/Latest` on every release, ~+70 ms on 2026); always-on
  `enter_host_release` (+40). Result, interleaved 7+7 from a fresh bare unzip
  of the rebuilt zip, `env -i` system python 3.11.15, medians: **2026 main
  108 ms → head 103 ms** (runs: main 106 108 110 109 102 114 105 · head 97 100
  104 109 102 109 103 — parity; the single shared inflate gives the few ms
  back); 2025 head 245 ms, 2024 head 242 ms (5 runs each; main: traceback).
  So: native users pay 0; a 2025/2024 delivery pays ~140 ms once for the
  context that makes the gate work at all.
* **Test** `tests/test_selfcheck_release.py` (9 tests, 1.3 s, fresh-clone safe
  — pins via `conftest.CERTIFIED_YEARS` / `pinned_base`, tools via
  `conftest.load_tool`; `conftest.py` only *read*, it is another engineer's
  territory this wave): PASS with all four counts zero and `records_checked ==
  stamps_valid > 0` on 2024, 2025, then 2026 in one process (foreign first; an
  autouse fixture snapshots the `rvt.partitions` framing table +
  `active_release()` around every test, so a leaked 2025 context would break
  the following native check); the SKILL's documented flow — #116's
  `rvt_edit_text` level rename on the 2025 and 2024 pins, then selfcheck on the
  output → PASS, `detect_release(out) == year`; a copy with the partition
  header zeroed → exit 1, `verdict FAIL`, `walker_errors 1` naming the
  `ValueError`, `ecc_mismatches 1`, no `Traceback` on either stream; a 2025
  copy with 64 bytes of `Formats/Latest` zeroed → the stderr warning, exit 1,
  `FAIL` (CRC 1, walker 1), `release_note` in `--json` and the note as the
  last `failures` line — the #535 guard is exercised, not speculative;
  `--skip-stamps` on a pin → PASS with the exact skipped-stamps dict; a
  CFB-header-zeroed copy and a missing path → exit 2 with the one-line error.
  Damaged copies are built in-test from the tracked pin (`read_entries` →
  replace one stream's raw bytes → `write_cfb`), nothing checked in. In the
  shard via the drop-in `tests/ci_shard.d/518-selfcheck-release.txt`.

## 518.2 Evidence (head of this branch; as nobody from a bare unzip of the rebuilt zip unless stated)

| input | head (`run rvt_selfcheck.py … --json`) | wall (bare unzip, `env -i` python3 3.11.15) |
|---|---|---|
| `G_ABPD.rvt` (2026) | exit 0 · 22/0 CRC · 5 pages/0 · 14 blocks, 0 errors · 3102/3102 · `PASS` — **stdout `diff` vs main: identical; `--json` `cmp` vs main: identical; stderr empty** | median **103 ms** (main **108 ms**), 7+7 interleaved |
| `G_ABPD_2025.rvt` | exit 0 · 23 members/0 CRC · 6 pages/0 mismatches · 15 blocks in 1 stream, 0 errors · **3316/3316** stamps · `PASS` (no stderr) | median 245 ms (5 runs) |
| `G_ABPD_2024.rvt` | exit 0 · 23/0 · 6/0 · 15 blocks, 0 errors · **3278/3278** · `PASS` | median 242 ms (5 runs) |
| #521 flow, 2025: `run rvt_edit_text.py assets/genesis/G_ABPD_2025.rvt --old "GEN Floor Plan" --new "OUR Floor Plan" --utf16 -o out/edited_2025.rvt` (`SELF-CHECK PASS`, 3316/3316) → `run rvt_selfcheck.py out/edited_2025.rvt` | exit 0 · 23/0 · 6/0 · 15/0 · 3316/3316 · `PASS` | — |
| same flow, 2024 | exit 0 · 3278/3278 · `PASS` | — |
| 2025 copy, first 16 bytes of `Partitions/20` zeroed | exit 1 · `2 page ECC : 6 full pages checked, 1 mismatches` · `3 block walker : 0 blocks in 1 partition stream(s), 1 errors` · `4 record stamps: 0/0` · `VERDICT : FAIL -> ECC page mismatches: 1 (Autodesk reader will reject); partition walker errors: 1`; json `first_errors: ["ValueError: unexpected Partitions header: v=0 cls=0x0"]`; **no traceback** | — |
| 2026 copy, same damage | exit 1 · same FAIL verdict (main: traceback) | — |
| 2025 copy, one byte flipped at raw offset 30000 of `Partitions/20` | exit 1 · `1 CRC failures` · `1 mismatches` · walker 0 · 3316/3316 · `FAIL -> gzip CRC failures: 1; ECC page mismatches: 1` | — |
| 2025 copy, 64 bytes of `Formats/Latest` zeroed at raw offset 2000 | exit 1 · stderr `warning: no release context for …: ParseError: parse error at 0x603b: class marker != 0 (0x403c) …` · `1 CRC failures` · `1 mismatches` · walker `0 blocks …, 1 errors` · `VERDICT : FAIL -> …; judged without its release context (no release context for …)`; json `release_note` set; **no traceback** (without the #535 guard: traceback) | — |
| `head -c 65536` truncations of the 2025 / 2026 pins | exit 1 · `FAIL -> partition walker errors: 1` (2025 adds `; judged without its release context (… schema sha256 e3b0c442… but the Revit 2025 pin is c964f9aa… …)`); no traceback | — |
| 2025 copy, CFB header sector zeroed | exit 2 · `ERROR: cannot open as an .rvt container: not an OLE2 structured storage file` | — |

The same runs from the repo (`.venv/bin/python tools/rvt_selfcheck.py …`)
print identical reports.

**Gates:** `tests/test_selfcheck_release.py` 9 passed (1.31 s); neighbours
`tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_coldstart.py
tests/test_edit_text_release.py tests/test_edit_own_release.py` 43 passed
(13.6 s); `tools/sync_plugin.py` → synced 1 file, deny-audit clean, validation
passed, zip rebuilt (5252 KB), identity scan 0 mismatches; `--check` clean;
`plugin/scripts/validate_plugin.py` PASS (25 assertions);
`tools/dev/check_portable_paths.py` ok (2941 tracked paths); whole merged shard
(`shard_list.py --print`, 87 files): see BRANCH STATE / PR body. `/verify`
(the `tools/rvt_*` + plugin rows): the bases/damaged runs above, plus
`head -c 65536` truncations of the 2025 and 2026 pins → exit 1, `VERDICT :
FAIL -> partition walker errors: 1`, no traceback (2025 also prints the honest
`warning: no release context … schema sha256 e3b0c442… but the Revit 2025 pin
is c964f9aa…`); a text file named `.rvt` → exit 2; `tools/surface_bench.py
--zip tekton-plugin.zip`: `go-author-prompt` READY/PASS 2.2 s (cowork, bare
python) · 2.5 s (codeexec) · 1.9 s (local), `go-author-6panels` PASS 3.8 / 4.8
/ 3.9 s, `go-edit` PASS 0.57 / 0.96 / 0.55 s (structural PASS, validation 0
errors), `validate` PASS; the one non-PASS is the pre-existing numpy-less
`author-ifc` (#127), untouched here.

Not claimed: nothing about viewer acceptance changes — selfcheck is the
necessary-not-sufficient hygiene gate (rule 4); #117 (the `needs-viewer` probe
for the 2025/2024 edit lane) is unaffected and still the certification path.

## 518.3 Findings / follow-ups

* **`rvt_inspect.py` is now the last hand-authored one-off in
  `plugin/skills/tekton-native/scripts/` (besides the `_bootstrap.py` shim) and
  has the same shape** (`from rvt.partitions import StreamWalker`,
  `ensure_rvt()` preamble, bare walk at line ~81, no context). Reproduced from
  the bare unzip: its default listing + schema summary is release-agnostic and
  exits 0 on the 2025 base, but the SKILL-documented `run rvt_inspect.py
  assets/genesis/G_ABPD_2025.rvt --records 5` → `ValueError: unexpected
  Partitions header: v=9 cls=0x391`, exit 1. Out of this issue's territory
  (different script), so searched (no existing issue) and filed task-shaped as
  **#533** (`Refs #518`, `P2 ready area:plugin good-first-pick`) with the
  repro, the DONE, and a pointer to §1's move recipe. It is read-only, so
  nothing a user delivers depends on it meanwhile.

## 518 BRANCH STATE

* Branch `cam/518-selfcheck-own-release` from `origin/main` @ d75302b; one PR,
  `Closes #518`.
* Files: `tools/rvt_selfcheck.py` (new source), `tools/sync_plugin.py` (+1
  mapping), `plugin/skills/tekton-native/scripts/rvt_selfcheck.py` (regenerated
  mirror), `tests/test_selfcheck_release.py` (new),
  `tests/ci_shard.d/518-selfcheck-release.txt` (new), this section.
* Gates: listed in 518.2; whole merged shard (87 files, `RVT_SKIP_LARGE=1 … -p
  no:cacheprovider $(shard_list.py --print)`): **1805 passed, 133 skipped, 3
  xfailed** in 6 min 12 s on the final head (1804/133/3 on the pre-/simplify
  head; +1 = the damaged-schema test).
* Follow-ups filed: #533 (`rvt_inspect.py --records` under the file's own
  release + move to `tools/`), #535 (`enter_host_release` note-not-raise for a
  damaged schema stream + swap restore before `yield`; then drop the guard here).
* Staged vs shipped: nothing staged for the viewer; the plugin zip is a
  regenerated, git-ignored artifact.

---

# eng #535 (2026-08-10) — `release_ctx.enter_host_release` keeps its "note, never raise" promise for a file it cannot even probe; `host_release_context` raises ONE typed `UnreadableHost`; the swap set is restored when setup fails before `yield`; `rvt_selfcheck.py` drops its interim guard (issue #535, Refs #518 / #116 / #70)

Stream: `release-ctx-refusal` (eng #535, engineer session started by the
tech-lead session). Written under this stream's own header; nothing above is
edited (eng #533 appends its own section to this same file in parallel — if
both PRs are open at once the tech lead settles the record conflict; note for
#533: `tools/rvt_inspect.py` can now call `enter_host_release` bare, no local
`except` needed, and gets a note for a text file / truncation / damaged schema).
Territory: `src/rvt/frontdoor/release_ctx.py` (the exception type, the two
guarded probes, the restore registration — the swap tables themselves are
untouched), `tools/rvt_selfcheck.py` (the 5-line guard removal the issue's
DONE ends with), `tools/rvt_edit_text.py` (its own downstream guard: container
open + walker construction → one line, exit 2), their regenerated mirrors,
`tests/test_release_ctx_refusal.py` + `tests/ci_shard.d/535-release-ctx-refusal.txt`,
this section. `src/rvt/frontdoor/__init__.py` needed **no** change (checked
first, see 535.1). No hot file touched; `versions/**`, `base.py`,
`tools/frontdoor.py`, `tools/rvt_edit.py`, `tools/rvt_inspect.py`,
`manifest.py` untouched. (The issue body suggested a separate record
`docs/inbox/release-ctx-damaged-host.md`; the tech-lead brief asked for this
section — same lineage as 518.3, one file to read.)

## 535.0 The defect, reproduced (fresh cloud clone, `origin/main` @ 074af6b, before any change)

Bad inputs built in the scratchpad from the tracked pins (nothing checked in):
`text.rvt` (8 lines of ASCII), `trunc4k_<y>.rvt` (first 4096 bytes of each
pin), `trunc64k_<y>.rvt`, `bfi_{gone,zero,short}_<y>.rvt` (`BasicFileInfo`
dropped / zeroed / cut to 10 bytes via `read_entries` → `write_cfb`),
`schema_dmg_<y>.rvt` (64 bytes of `Formats/Latest` zeroed at raw offset 2000 —
#518's repro), `schema_gone_<y>.rvt` (`Formats/Latest` dropped), y ∈
{2026, 2025, 2024}.

`enter_host_release(stack, p)` on `main` (documented contract: *None or the
refusal sentence*):

| input | `main` | why |
|---|---|---|
| `text.rvt` | **raises** `NotOleFileError: not an OLE2 structured storage file` | `_classify_release → V.detect_release → open_rvt` |
| `trunc4k_*` | **raises** `OleFileError: incomplete OLE sector` | same |
| `schema_dmg_2025/2024` | **raises** `ParseError: parse error at 0x603b: class marker != 0 (0x403c) @0x603b: 14 1e …` | `_codec_triple_from_base → global_framing.schema_of` |
| `schema_gone_2025/2024` | **raises** `OSError: file not found` | same (`doc.concat("Formats/Latest")`) |
| `schema_dmg_2026`, `schema_gone_2026` | `None` (native: the schema is never parsed to enter) | — |
| `bfi_{gone,zero,short}_*` | `None`, context entered on 2025/2024 (`detect_release` falls back to the schema signature; the context reads identity strings from OUR bundled base, not the host) | readable — control |
| `trunc64k_2025/2024` | note: `base … carries schema sha256 e3b0c442… but the Revit 2025 pin is c964f9aa…` (`schema_of` yields an *empty* schema; the pin check refuses) | readable-but-unpinned — control |
| the three pins | `None`; 2025/2024 → `active_release() == year` | control |

The three surfaces on `main`, same inputs (`--edit "set level 1351691 elevation to -9 ft"` / `--old "GEN B1 - Basement" --new "OUR B1 - Basement" --utf16`):

| input | front door `author --rvt … --edit … --json` | `rvt_edit_text.py` | `rvt_selfcheck.py` |
|---|---|---|---|
| `text.rvt`, `trunc4k_2025` | rc 2, one stderr line `REFUSED (input release): … not an OLE2 compound file …` (#176's precheck, before the release entry — not this issue's, unchanged) | **rc 1, 45/52-line traceback** (`NotOleFileError` / `OleFileError` out of `enter_host_release`) | rc 2 `ERROR: cannot open as an .rvt container: …` (opens first) |
| `schema_dmg_2025` / `_2024` | **rc 1, stdout empty, 56-line traceback ending `rvt.schema.ParseError: parse error at 0x603b …`** (out of `_route_rvt → enter_host_release`; `tools/frontdoor.py`'s last-resort `traceback.print_exc()`); bare-unzip `go author --rvt … --edit … --json`: rc 1, `"result": null`, same 56 lines on stderr | **rc 1, 53-line traceback** (`ParseError`) | rc 1 `VERDICT : FAIL` + `warning: no release context for …: ParseError: …` — only because of #518's local `except Exception` guard |
| `schema_gone_2025` | rc 2 `REFUSED (input release): … (no Formats/Latest class schema stream)` (#176) | **rc 1, 50-line traceback** (`OSError: file not found`) | rc 1 FAIL + warning (guard) |
| `trunc64k_2025` | rc 3, one JSON, `errors[0] = cannot open/plan …: RuntimeError: Partitions/20: walker errors [...] (no release context for …)` | note printed, then **17-line traceback** `ValueError: unexpected Partitions header: v=9 cls=0x391` out of the bare `P.StreamWalker(...)` | rc 1 FAIL + warning |

And the secondary hardening, made observable: with `RC._by_name` patched to
raise `ReleaseContextError` on its 3rd call (two `rvt.mutate` class ids already
swapped), `with host_release_context(G_ABPD_2025)` on `main` raises — and
leaves `MU.CLASS_ELEMENT_HEADER == 1479` (the 2025 id) behind in the process
(native: 1509); the `rvt.partitions` framing table is restored only because
`V.reading`'s own `with` unwinds. Head: everything restored (535.2).

## 535.1 What was built

* **`release_ctx.UnreadableHost(ReleaseContextError)`** — `.path`, `.why` (one
  sentence, no newline), the layer's own error as `__cause__`, `str(e) ==
  f"{path}: {why}"`; exported in `__all__`, documented in the module docstring.
  A *subclass* of `ReleaseContextError` on purpose: every existing `except
  ReleaseContextError` (`enter_host_release` itself, `build.py:417`, the famload
  tests) keeps working and now also covers the unreadable cases; `with
  host_release_context(p)` callers that want to tell "unreadable" from
  "uncertified" catch the subclass. (The brief floated `FrontDoorError` /
  `ValueError` as bases: `FrontDoorError` lives in `rvt.frontdoor.__init__`,
  which imports this module lazily — a base there is an import cycle and the
  wrong direction; and a `ValueError` base would be caught by unrelated `except
  ValueError` arms such as `rvt_edit._run_json`'s "unknown id" line. One parent,
  the one every caller already names.)
* **Two guarded probes, nothing else moved:** `_detect_release(path)` (used by
  `_classify_release` and by the exported `needs_release_context`, so the module
  has no unguarded probe left — /simplify altitude finding) wraps
  `V.detect_release(path)` → `UnreadableHost(path, "not a Revit container tekton
  can open (<Exc>: <msg>)")`; `_codec_triple_from_base` wraps
  `global_framing.schema_of(base_path)` → `UnreadableHost(path, "its
  Formats/Latest class schema cannot be read (<Exc>: <msg>)")`. Both `except
  Exception … from e` with a `noqa: BLE001` and the reason inline — the same
  altitude and breadth as the instrument ladder the issue points at
  (`global_framing.enter_own_release`, `except Exception` → sentence): the probe
  reads an untrusted file, *any* failure to read it is the finding, and the
  detail survives on `__cause__`. The schema-pin mismatch stays a plain
  `ReleaseContextError` (readable, unpinned — not unreadable). A native host
  returns from `_classify_release` before the schema probe exactly as before, so
  the 2026 path parses nothing new and imports nothing new (535.2).
* **`enter_host_release`** gains one `except UnreadableHost as e: return f"no
  release context for {host_path}: {e.why}"` arm above the existing one, so the
  note carries the path once, then the why (the generic arm would print the path
  twice, as the pin-mismatch note already does today — left alone).
* **Restore-before-yield (`_release_context`)**, minimal-diff: the `with
  V.reading(…) as ords, GF.bound(…)` line also opens a `contextlib.ExitStack()
  as undo`; the three shared dicts (`GSK._SCHEMA_CACHE`, `port._STATE`,
  `SA._SCHEMA_STATE`) are snapshotted right there as one `[(live, dict(live)), …]`
  list and one `@undo.callback _restore()`
  (the old `finally:` body, verbatim in effect: `_ACTIVE` cleared, `saved`
  replayed LIFO, the three dicts reset) is registered **before the first
  `swap()`**; the trailing `try: yield / finally:` collapses to `yield info`. The
  ~170-line swap body keeps its indentation and content byte-for-byte (the three
  `prev_* = dict(…)` lines moved up, nothing else) — the diff is the point of
  review, so it was kept small. Unwind order is unchanged (undo is innermost:
  swaps first, then `GF.bound`, then `V.reading`, as the old `finally` inside
  the `with` did).
* **`tools/rvt_selfcheck.py`**: the interim `try/except Exception` around
  `enter_host_release` (cited to this issue) is deleted; `enter_files_release`
  is `natively_framed(doc)` → lazy import → `return enter_host_release(stack,
  path)`. The 2026 path still never imports `release_ctx` (#518's latency fix
  intact — module set identical, 535.2). The warning text for a damaged schema
  changes from the guard's `…: ParseError: parse error …` to the helper's `…:
  its Formats/Latest class schema cannot be read (ParseError: parse error …)`;
  #518's `test_damaged_schema_stream_still_reaches_a_verdict` asserts the prefix
  and stays green unmodified (its docstring still says "the helper raises past
  its own refusal" — another stream's file, left for them; harmless).
* **`tools/rvt_edit_text.py`** had no local catch around `enter_host_release`
  (the brief assumed one); what it lacked was its *own downstream guard*, which
  the issue's DONE names ("warning + exit ≠ 0 at the walker, no traceback"). It
  now has `rvt_selfcheck.main`'s exact order and words: `main()` opens the
  container first (`ERROR: cannot open as an .rvt container: <e>`, exit 2 — a
  text file / truncation is ONE line and never reaches the release entry; the
  first head guarded inside `_edit` and printed warning + error, /simplify moved
  it up), then enters the release, then hands `doc` to `_edit(a, doc, …)`, which
  constructs the walker under a `try` (`ERROR: input partition does not walk
  cleanly: <Exc>: <msg>`, exit 2 — the same words its existing `w.errors`
  refusal prints; a damaged-schema host *does* open, so this is legitimately
  `_edit`'s). `with open_rvt(a.path) as doc:` became `with doc:` so the 70-line
  body is not re-indented. Happy path untouched (2025/2024 edit: `SELF-CHECK
  PASS`, repo and bare unzip).
* **`src/rvt/frontdoor/__init__.py`: no change needed** — checked first as the
  brief asked. `_route_rvt` already treats the return of `enter_host_release` as
  a note, then enters `global_framing.enter_own_release` (never raises) and
  `_route_rvt_inner`, whose `Document.from_file` arm turns the open failure into
  `errors[0] = "cannot open/plan <path>: <Exc>: <msg> (<note>)"` → manifest
  `FAILED …` → `EX_INCOMPLETE` (3) with the one JSON on stdout. The only reason
  it tracebacked was the raise *inside* `enter_host_release`.
* **Test** `tests/test_release_ctx_refusal.py` (12 tests, 0.6 s, fresh-clone
  safe — pins via `conftest.CERTIFIED_YEARS` / `pinned_base`, tools via
  `conftest.load_tool`, `conftest.py` only read): the four unreadable hosts
  (text, 4 KB truncation, damaged schema, dropped schema — built in a module
  fixture from the tracked pins) → `host_release_context` raises `UnreadableHost`
  that **is** a `ReleaseContextError`, `.path == p`, one-line `.why` naming the
  cause class, `type(__cause__).__name__` == the expected layer error;
  `enter_host_release` → a `str` starting `no release context for <p>: ` with the
  path exactly once, `active_release()` None throughout; controls: the three pins
  enter/skip exactly as before, and a `BasicFileInfo`-less copy of each pin is
  still readable (2025/2024 enter with `info["base"]` = our bundle, not the
  host); the setup-failure restore (`_by_name` raising on call 3 →
  `ReleaseContextError`, every snapshotted constant back, and the next
  `host_release_context` on the same pin enters fine); the callers: front door
  API `FD.author(rvt=schema_dmg, …)` → `ok False`, `status FAILED…`, `errors[0]`
  = `cannot open/plan … no release context for … Formats/Latest …`, manifest on
  disk; CLI `main([... --json])` → rc `EX_INCOMPLETE == 3`, **stderr `""`**,
  stdout parses as ONE JSON; `rvt_edit_text` on the damaged schema → rc 2,
  exactly two stderr lines (warning + `ERROR: input partition does not walk
  cleanly: ValueError: …`), nothing written, and on text / truncation → rc 2,
  exactly ONE line `ERROR: cannot open as an .rvt container: …`; `rvt_selfcheck` on the
  damaged schema with the guard gone → rc 1, `FAIL`, `release_note`, walker 1, no
  traceback. An autouse fixture snapshots the framing table, `active_release()`,
  three `rvt.mutate` ids, two `genesis.skeleton` builders + cache keys, and
  `standalone`'s `bundled_base_path` / template / `_SCHEMA_STATE` around every
  test. In the shard via the drop-in.

## 535.2 Evidence (head of this branch)

**The three surfaces, after** (same inputs as 535.0; repo `.venv` and — where
marked † — the bare unzip of the rebuilt zip as nobody, `env -i PATH=/usr/bin:/bin
python3` 3.11.15):

| input | front door `--json` | `rvt_edit_text.py` | `rvt_selfcheck.py` |
|---|---|---|---|
| `schema_dmg_2025` | **rc 3, 32-line JSON on stdout, stderr 0 lines**; `status: FAILED (edit did not complete: rc None)`; `errors[0]: cannot open/plan …/schema_dmg_2025.rvt: ParseError: parse error at 0x603b: … (no release context for …: its Formats/Latest class schema cannot be read (ParseError: …))`. † `go author --rvt … --edit … --json`: rc 3, `go.ready true`, `result.status FAILED (…)`, stderr **0** lines (main: rc 1, `result null`, 56 lines) | rc 2, stderr exactly: `warning: no release context for …: its Formats/Latest class schema cannot be read (ParseError: parse error at 0x603b: class marker != 0 (0x403c) …)` / `ERROR: input partition does not walk cleanly: ValueError: unexpected Partitions header: v=9 cls=0x391`; † identical | rc 1, `VERDICT : FAIL -> gzip CRC failures: 1; ECC page mismatches: 1 …; partition walker errors: 1; judged without its release context (…)`, one stderr warning (the helper's sentence); † identical |
| `schema_dmg_2024` | rc 3, one JSON, stderr empty (`ParseError … 0x5c1b`) | — | — |
| `schema_gone_2025` | rc 2 (#176 precheck, unchanged) | rc 2: warning `… cannot be read (OSError: file not found)` + `ERROR: input partition does not walk cleanly: ValueError: …` | rc 1 FAIL + warning |
| `text.rvt` / `trunc4k_2025` | rc 2 (#176 precheck, unchanged) | rc 2, ONE line: `ERROR: cannot open as an .rvt container: not an OLE2 structured storage file` (resp. `incomplete OLE sector`) — opened before the release entry, like selfcheck; † identical | rc 2, one line (unchanged) |
| `trunc64k_2025` | rc 3, one JSON (unchanged) | rc 2: pin-mismatch warning + `ERROR: input partition does not walk cleanly: ValueError: …` (main: traceback) | rc 1 FAIL (unchanged) |
| `bfi_gone_2025` | rc 3 `edit not understood` for the placeholder sentence / proceeds for a real one (unchanged — readable) | — | — |

**Identity — readable files behave byte-identically:**

* `frontdoor.py author --rvt <pin> --edit "set level 1351691 elevation to -9 ft" --out … --json`, head vs `main`, all rc 0 `PROOF-ONLY, NOT-DELIVERABLE (hard gates PASSED)`; md5 of the edited `.rvt`: 2026 `ad47a1681c0bbbbef4285b4300aea689` = `ad47a168…a689`; 2025 `bb8299902f1b37fd815986a10c28782b` = same; 2024 `4a70a9460330c26e3d68ef441989de19` = same. **×3 identical.**
* `-X importtime`, module sets sorted and diffed head vs `main`: `tools/rvt_selfcheck.py G_ABPD.rvt` (2026, native) **114 = 114 modules, diff empty** (`release_ctx` still not among them); `enter_host_release(stack, G_ABPD.rvt)` in a bare interpreter **157 = 157, diff empty**. The 2026 path gained no import and no probe (the schema guard sits after the native early-return; the detect guard wraps the same call).
* † `run rvt_selfcheck.py assets/genesis/G_ABPD.rvt` → `VERDICT : PASS`, rc 0, no warning; † `run rvt_edit_text.py assets/genesis/G_ABPD_2025.rvt --old "GEN B1 - Basement" --new "OUR B1 - Basement" --utf16 -o out/e25.rvt` → `SELF-CHECK PASS`.
* Restore hardening: the 535.0 `_by_name`-fails-on-call-3 experiment on head → raises `ReleaseContextError`, every snapshotted constant equal to before (`main`: `MU.CLASS_ELEMENT_HEADER` 1509 → 1479 leaked), and an immediate re-entry on the 2025 pin yields `release 2025` and restores again.

**Gates:** `tests/test_release_ctx_refusal.py` **12 passed** (0.6 s);
neighbours `test_selfcheck_release test_edit_text_release test_edit_own_release
test_famload_2025 test_input_release` + mine: **72 passed** (14 s);
`tools/sync_plugin.py` → synced 3 files (the `release_ctx` lib mirror + the two
tekton-native script mirrors), deny-audit clean, validation passed, zip rebuilt
(5273 KB), identity scan 0 mismatches; `--check` clean;
`plugin/scripts/validate_plugin.py` PASS (25 assertions);
`tools/dev/check_portable_paths.py` ok (2955 paths); whole merged shard: see
BRANCH STATE / PR body.

## 535.3 Findings / follow-ups (searched first, then filed task-shaped)

* **#559** (`Refs #535`, `P2 ready area:frontdoor good-first-pick`): the edit
  route's status for a job that never ran is `FAILED (edit did not complete: rc
  None)`; the reason rides only in `errors[0]`. The create routes already say
  `FAILED (<errors[0][:160]>)`. `manifest.py` is outside this territory; the
  brief's "status sentence names the reason" is met by `errors[0]` today and by
  `status` once #559 lands (its DONE lets this stream's front-door test tighten
  to `"Formats/Latest" in doc["status"]`).
* **#560** (`Refs #535`, `P2 ready area:plugin good-first-pick`): `tools/rvt_edit.py
  <file> info` *without* `--json` still tracebacks on every note case (its
  `_run` calls `Document.from_file` bare; `_run_json` — what the skill uses —
  answers cleanly). Pre-existing for the pin-mismatch / uncertified cases; #535
  merely adds the unreadable ones to the same clean-on-`--json`, raw-on-plain
  split. `rvt_edit.py` is outside this territory.
* **#561** (`Refs #12 / #498`, `P1 ready area:famgen`): running every `release_ctx`-touching
  test file surfaced `tests/test_famdoc_scan_fp.py::test_provenance_scan_on_the_real_bundled_universe_is_clean`
  **red on `origin/main` @ 074af6b with nothing changed** (6 donor-universe id hits, ids
  18403/18404/18453, not classified as window false positives). Pre-existing, outside the
  shard (so no gate shows it), inside the #498 fence — filed, not touched; the other 165
  tests of those 12 files pass on head.
* /simplify (4 angles) applied: the shared `_detect_release` probe (closes the
  `needs_release_context` hole), the snapshot list in `_restore`, the trimmed
  module docstring, `rvt_edit_text` open-before-enter (one line for a non-container),
  test const hoist + single-open damaged-copy builder. Skipped with reason: hoisting
  `_rewrite_stream` / the no-leak fixture into `tests/conftest.py` (that file is
  another engineer's territory this wave, PR #558 — worth a follow-up sweep once it
  lands: four test files now carry a copy); collapsing `enter_host_release`'s two
  `except` arms into one `getattr(e, "why", e)` (explicit arms preferred; `.why` is
  the documented field the brief asked for); dropping the 8-line selfcheck assertion
  that overlaps #518's test (kept: it pins the helper's new wording, and #518's
  docstring still describes the old guard).
* Not filed (note for eng #533, above): `rvt_inspect.py` needs no local guard.
* Not filed: `tests/test_selfcheck_release.py`'s docstring line "(#535: the
  helper raises past its own refusal)" is now history; whoever next touches that
  file can reword it.

## 535 BRANCH STATE

* Branch `cam/535-release-ctx-refusal` from `origin/main` @ 074af6b; one PR,
  `Closes #535`.
* Files: `src/rvt/frontdoor/release_ctx.py`, `tools/rvt_selfcheck.py`,
  `tools/rvt_edit_text.py`, regenerated mirrors
  `plugin/lib/src/rvt/frontdoor/release_ctx.py` +
  `plugin/skills/tekton-native/scripts/{rvt_selfcheck,rvt_edit_text}.py`,
  `tests/test_release_ctx_refusal.py` (new),
  `tests/ci_shard.d/535-release-ctx-refusal.txt` (new), this section.
* Gates: listed in 535.2; whole merged shard (`RVT_SKIP_LARGE=1 … -p
  no:cacheprovider $(shard_list.py --print)`): **1863 passed, 134 skipped, 3
  xfailed** (92 files, 6 min 52 s) on the pre-/simplify head over 074af6b; **1900
  passed, 134 skipped, 3 xfailed** (93 files, 6 min 34 s, pytest rc 0) on the final
  head 7292372 rebased over 152d009 (+37 = #556's `test_ifc_assembly` drop-in that
  landed on main meanwhile; this record-only commit on top changes no test input).
* Follow-ups filed: #559, #560, #561 (pre-existing red famgen test on main, fenced).
* Staged vs shipped: nothing staged for the viewer (no output byte changes —
  md5 ×3 identical); the plugin zip is a regenerated, git-ignored artifact.

---

# eng #533 (2026-08-10) — `rvt_inspect.py --records` walks a project under its own release; the last hand-authored tekton-native one-off moves to `tools/` with a generated mirror (issue #533, Refs #518 / #116 / #70)

Stream: `inspect-own-release` (eng #533, engineer session started by the
tech-lead session). Written under this stream's own header; nothing above is
edited (eng #535's section landed on `main` via #563 while this branch was
open; the rebase kept both, theirs first). Territory: `tools/rvt_inspect.py`
(new — the source the plugin script now mirrors), `tools/sync_plugin.py` (one
mapping line directly under #538's), the regenerated
`plugin/skills/tekton-native/scripts/rvt_inspect.py` (byte-identical mirror),
`tests/test_inspect_release.py` + `tests/ci_shard.d/533-inspect-release.txt`,
this section. No hot file touched: `plugin/skills/tekton-native/SKILL.md`
unchanged (the documented invocation `_bootstrap.py run rvt_inspect.py
model.rvt [--classes Wall] [--records 20] [--dump-schema out/schema.json]` is
the same); `release_ctx.py`, `global_framing.py`, `src/rvt/versions/**`,
`rvt_selfcheck.py`, `rvt_edit_text.py` untouched. After this,
`plugin/skills/tekton-native/scripts/` holds only the `_bootstrap.py` shim +
`requirements.txt` + ten `cmp`-clean mirrors of `tools/*.py` (checked file by
file) — the issue's third DONE bullet.

## 533.0 The defect, reproduced (fresh cloud clone, `origin/main` @ 6250424 — #538 and #563 merged — before any change)

Bare unzip of the `tekton-plugin.zip` built at `main`, as nobody (`env -i
PATH=/usr/bin:/bin HOME=<scratch> python3` = 3.11.15, no repo on the path,
`import olefile` → `ModuleNotFoundError`, i.e. the bootstrap's vendored
fallback), the SKILL's own line `python3
skills/tekton-native/scripts/_bootstrap.py run rvt_inspect.py
assets/genesis/<base> --records 20 --classes Wall`:

| input | `main` |
|---|---|
| `G_ABPD.rvt` (2026) | exit 0 · stream listing · `schema (Formats/Latest, 496,597 bytes inflated): 4690 classes …` · `classes matching 'Wall': 128` · 20 decoded records · `decode summary: {'clean': 20}` |
| `G_ABPD_2025.rvt` | listing + schema + classes print, then exit 1, traceback: `rvt_inspect.py:81 main → partitions.py:338 __init__ → partitions.py:243 parse_stream_header … ValueError: unexpected Partitions header: v=9 cls=0x391` |
| `G_ABPD_2024.rvt` | exit 1, same traceback, `cls=0x37b` |
| any pin, first 16 bytes of `Partitions/<N>` zeroed, `--records 5` | exit 1, traceback (`unexpected Partitions header: v=0 cls=0x0`) under every release |
| `head -c 65536` of a pin (still opens as CFB), no flags at all | exit 1, traceback: `schema.py stats … ValueError: max() arg is an empty sequence` (the truncated `Formats/Latest` inflates to 0 bytes → a 0-class schema) |
| a text file named `.rvt` / a CFB-header-zeroed copy | exit 1, traceback out of `open_rvt` (`olefile.olefile.NotOleFileError: not an OLE2 structured storage file`) |

Without `--records` the 2025/2024 pins already exit 0 on `main` (listing +
schema summary + `--classes` + `--dump-schema` are release-agnostic) — the
walk is the only release-blind step, exactly as #518.3 found.

## 533.1 What was built

* **Moved under `tools/` + sync**, §1's recipe a third time:
  `tools/rvt_inspect.py` is the source; `tools/sync_plugin.py` gains one
  `(_tool("rvt_inspect.py"), "skills/tekton-native/scripts/rvt_inspect.py")`
  line directly under #538's; the plugin copy is a generated byte-identical
  mirror (`cmp` clean; a hand edit to the mirror reads as `PLUGIN DRIFT: 1
  file(s) differ from source: skills/tekton-native/scripts/rvt_inspect.py` —
  verified, then restored). The import header changed the same way
  (`_bootstrap.ensure_rvt()` preamble → the `HERE/../src` + `HERE/../lib/src`
  header), with the same honestly-stated consequence as its three siblings: a
  *direct* `python skills/tekton-native/scripts/rvt_inspect.py` from a bare
  unzip no longer self-bootstraps; the SKILL documents `_bootstrap.py run` and
  the script's usage text now says so.
* **The fix — same shape as #538, but the read-side entry, stated plainly
  because the issue text names the other one.** `natively_framed(doc)` asks the
  engine's own `P.parse_stream_header` whether every `Partitions/<N>` header
  already parses with the container class bound in `rvt.partitions`; if so
  nothing is entered and nothing more is imported. Otherwise
  `enter_files_release` lazily imports and enters, **once**, on an `ExitStack`
  scoped to the walk, **`rvt.global_framing.enter_own_release(stack, path)`** —
  the *instrument* ladder ("the lenient ladder every instrument uses": own
  schema by name → the pinned table of the release `BasicFileInfo` declares →
  the built-in constants; returns a note, never raises, by contract) that
  `tools/rvt_analyze.py`, `tools/seed_audit.py`, `provenance`, `census`,
  `inventory`, `render/inspect` already enter — **not**
  `release_ctx.enter_host_release`, which the issue's DONE and the brief name
  by analogy with the two *edit* CLIs and the pre-delivery gate. Why the
  deviation is the right call for this tool, with numbers: (1) it is the
  existing helper for exactly this job (a read-only instrument on "ANY .rvt"),
  so nothing is re-invented and the `/simplify` reuse pass pointed straight at
  it; (2) it is **half the weight on the foreign path** — measured in-process on
  the 2025 pin after the schema parse both need: `enter_host_release` 81–104 ms
  and 3 → 41 `rvt.*` modules (it builds an `ObjectEncoder`, imports
  `encode`/`mutate`/`regadd`/`famgen.factory`/`genesis.skeleton`/`standalone`,
  re-opens the file for `detect_release`, sha256s the bundled base) vs
  `enter_own_release` **44–46 ms and 3 → 18 modules**; end to end from the
  bare unzip `--records 20` on the 2025 pin: **187 ms vs 229 ms** with the host
  entry (first head of this branch); (3) it walks every release the engine can
  *read* — including the ≤ 2023 32-bit-id era through `records32.reading32`,
  which is why `iter_records` is now referenced through the module
  (`O.iter_records`, the name that layer rebinds; `records32.py:51` warns of
  exactly the by-name import the old script had) — instead of refusing
  anything we cannot *author* into; a host context's port layer, encoder and
  famgen token swaps buy a reader nothing. What the DONE actually asks for is
  intact: one entry, once, lazily; the walker through the module
  (`P.StreamWalker`) so it reads the swapped container class / block / trailer
  tags at call time; a note echoed as `warning: <note>` on stderr when the
  ladder had to fall back a rung, and the walk proceeds and reports what it
  finds. If the reviewer wants literal parity with #538 instead, it is a
  two-line swap back (import + call) and +45 ms per foreign run; I recommend
  not. **The context is entered only on the `--records` path** — the one step
  that walks partitions — so the default summary, `--classes` and
  `--dump-schema` stay context-free on *every* release, and the `ExitStack`
  lives exactly as long as the walk. `ObjectDecoder(sch)` was already bound to
  the file's own `Formats/Latest`, so records decode against the file's own
  schema with no further change (300/300 clean on both foreign pins).
* **A damaged file is a reported finding, never a traceback** — at the three
  places this read-only tool can meet damage: (1) the path does not exist /
  does not open as a CFB container → one `ERROR:` line on stderr, **exit 2**
  (the `rvt_selfcheck.py` convention); (2) `Formats/Latest` does not parse, or
  inflates to a class map with no classes (truncation — `rvt.schema.parse(b"")`
  returns a 0-class `Schema` rather than raising, so the tool checks
  `sch.classes` itself; see 533.3 / #569) → the stream listing still prints, then
  `schema (Formats/Latest): unreadable (<Type>: <msg>) -- nothing below the
  stream listing can be reported`, **exit 1** (the class map is this tool's
  subject; `--classes` / `--records` / `--dump-schema` all need it); (3) a
  partition whose stream header parses under no release → its own line
  `  Partitions/<N>: cannot be walked (ValueError: unexpected Partitions header:
  v=0 cls=0x0)`, the walk continues with the next partition, and the summary
  counts it (`decode summary: {'unwalkable': 1}`) → **exit 1**. Catching at the
  judging caller is the altitude `validate.WalkedFile.walker()` and
  `rvt_selfcheck.check_partitions` use; `StreamWalker` raising stays its
  contract for the authoring callers. Exit codes are now documented in the
  usage text: `0` inspected · `1` something asked for could not be read · `2`
  not a container. A healthy file's output is byte-identical to `main`'s (next
  bullet), so nothing a current caller parses moved. Because the schema is
  parsed (and an unreadable one reported) *before* the `--records` branch, and
  a non-container never gets past `open_rvt`, the release entry is only ever
  reached for a file whose container and schema stream already read — no local
  broad `except` was ever needed around it, #563 or not.
* **The native path pays nothing (S-2026-08-09-g), measured, not asserted:**
  fresh bare unzips of the `main` zip and the head zip side by side, `env -i`
  system python 3.11.15, 7+7 **interleaved**, `run rvt_inspect.py
  assets/genesis/G_ABPD.rvt --records 20`: medians **main 138 ms · head 136
  ms** (sorted runs: main 135 135 136 138 138 139 141 · head 133 133 134 136
  138 139 181); default mode (no flags): **main 126 ms · head 124 ms** (main
  123 123 124 126 126 133 135 · head 123 123 124 124 126 128 133) — parity.
  `PYTHONPROFILEIMPORTTIME=1` on the head, native pin, `--records 5`: **0**
  `rvt.frontdoor.*` modules, **9** `rvt.*` modules in all (what the old script
  imported); the 2025 pin, same flags: still **0** `rvt.frontdoor.*`, 22
  `rvt.*` (the ladder: `global_framing`, `versions.records32`, `adocument`, the
  three token holders). Foreign `--records 20` wall from the bare unzip: 2025
  **187 ms**, 2024 **186 ms** (medians of 5: 184 187 187 189 205 · 184 184 186
  188 213; main: traceback).
* **Test** `tests/test_inspect_release.py` (13 tests, 1.5 s, fresh-clone safe —
  pins via `conftest.CERTIFIED_YEARS` / `pinned_base`, tools via
  `conftest.load_tool`; `conftest.py` only *read*, it is eng #542's this
  wave): `--records 20 --classes Wall` → exit 0, schema line, `decode summary:
  {'clean': 20}` as the last line, `classes matching 'Wall':` with `ArcWall`
  listed, empty stderr — on 2024, 2025, then 2026 in one process (foreign
  first; an autouse fixture snapshots the `rvt.partitions` framing table,
  `release_ctx.active_release()`, **and what the instrument ladder rebinds on
  top** — `objects.iter_records`, `adocument._DECODER`,
  `famdoc_adoc.FAMILY_END_RECORD` — around every test, so a leaked 2025
  context of either kind would break the following native walk); the default
  summary + `--dump-schema` on every pin (file written, non-empty); **the
  native pin with `global_framing.enter_own_release` monkeypatched to raise**
  still decodes 20 clean records (the ladder is provably never reached for a
  native file); the SKILL's documented flow — #116's `rvt_edit_text` level
  rename on the 2025 and 2024 pins (`SELF-CHECK PASS`, `detect_release(out)
  == year`), then `--records 20` on the output → clean; a copy with the
  partition header zeroed → exit 1, the `cannot be walked (ValueError:
  unexpected Partitions header` line, `{'unwalkable': 1}` last, schema summary
  still present, and the same copy *without* `--records` → exit 0, empty
  stderr (never touches the partition); a copy with 64 bytes of
  `Formats/Latest` zeroed → exit 1, `schema (Formats/Latest): unreadable
  (ParseError: …`, no `decoding first`; a 64 KiB truncation → exit 1,
  unreadable; a CFB-header-zeroed copy → exit 2, `cannot open as an .rvt
  container`, empty stdout; a missing path → exit 2, `no such file`;
  `Traceback` asserted absent on both streams in every run. Damaged copies are
  built in-test from the tracked pin (`read_entries` → replace one stream's raw
  bytes → `write_cfb`), nothing checked in. In the shard via the drop-in
  `tests/ci_shard.d/533-inspect-release.txt`.

## 533.2 Evidence (final head; as nobody from a bare unzip of the rebuilt zip unless stated)

| input (`run rvt_inspect.py …`) | head | wall |
|---|---|---|
| `G_ABPD.rvt` (2026): `--records 20 --classes Wall`; no flags; `--dump-schema x.json` | exit 0 — **stdout+stderr `diff` vs the `main` unzip: identical for all three invocations** | `--records 20` median **136 ms** (main **138**); default **124 ms** (main **126**); 7+7 interleaved |
| `G_ABPD_2025.rvt`, `--records 20 --classes Wall` | exit 0 · `classes matching 'Wall': …` · 20 records (`KeynoteTable`, `AutoJoinTracker`, `AssemblyCodeTable psize=125228`, `CategoryElem`/`GStyleElem` …) · `decode summary: {'clean': 20}` · **stderr empty (0 bytes)**; `--records 300` → `{'clean': 300}` | median **187 ms** (5) |
| `G_ABPD_2024.rvt`, same | exit 0 · 20 clean (`… FabricationServiceSettings …`) · `{'clean': 20}`; `--records 300` → `{'clean': 300}` | median **186 ms** (5) |
| #521 flow, 2025/2024: `rvt_edit_text` level rename (`SELF-CHECK PASS`) → `--records 20` on the output | exit 0 · `{'clean': 20}` (in-test, both years) | — |
| 2025 copy, first 16 bytes of `Partitions/20` zeroed, `--records 5` | exit 1 · listing + schema summary print · `  Partitions/20: cannot be walked (ValueError: unexpected Partitions header: v=0 cls=0x0)` · `decode summary: {'unwalkable': 1}` · **no traceback**, stderr empty (its schema still resolves the release, so no note) | — |
| 2026 copy, same damage | exit 1 · same report (main: traceback) | — |
| 2025 / 2026 copies, 64 bytes of `Formats/Latest` zeroed at raw offset 2000, `--records 5` | exit 1 · listing, then `schema (Formats/Latest): unreadable (ParseError: parse error at 0x603b: class marker != 0 (0x403c) …)` (2026: `… at 0x649b (0xc2b7) …`); no traceback | — |
| `head -c 65536` of the 2025 / 2026 pins; `size − 400000` / `− 900000` tail cuts of the 2025 pin | exit 1 · `schema (Formats/Latest): unreadable (ValueError: no classes in 0 inflated bytes) -- nothing below the stream listing can be reported`; the `size − 100000` cut: `unreadable (ParseError: … bad class name len=42 …)`; no traceback (main: `max() arg is an empty sequence` traceback) | — |
| 2025 copy, 256 bytes zeroed at raw offsets 2000 / 30000 / 120000 of `Partitions/20`, `--records 5` | exit 0 · 5 clean records (the walker tolerates it; *judging* damage is `rvt_selfcheck.py`'s job, which FAILs these on CRC/ECC) | — |
| CFB header sector zeroed; a text file named `.rvt` | exit 2 · `ERROR: cannot open as an .rvt container: not an OLE2 structured storage file`, stdout empty (main: `NotOleFileError` traceback) | — |
| missing path | exit 2 · `ERROR: no such file: …` | — |

The same runs from the repo (`.venv/bin/python tools/rvt_inspect.py …`) print
identical reports.

**Gates:** `tests/test_inspect_release.py` 13 passed (1.54 s); neighbours
`tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_selfcheck_release.py
tests/test_edit_text_release.py tests/test_edit_own_release.py` + mine: 54
passed (10.5 s); `tools/sync_plugin.py` → synced 1 file, deny-audit clean,
validation passed, zip rebuilt (5290 KB), identity scan 0 mismatches; `--check`
clean; `plugin/scripts/validate_plugin.py` PASS (25 assertions);
`tools/dev/check_portable_paths.py` ok; whole merged shard (`shard_list.py
--print`, 94 files, `RVT_SKIP_LARGE=1 … -p no:cacheprovider`): **1910 passed,
134 skipped, 3 xfailed** in 6 min 47 s on the first head (host entry, base
7d04c82); **1922 passed, 134 skipped, 3 xfailed** in 6 min 45 s on the final
head (own-release entry, rebased on 6250424, 95 files — #563's suite joined).
`/verify` (the `tools/rvt_*` + plugin rows): the repo-side runs of the three
pins (`{'clean': 20}`, exit 0, stderr 0 bytes; schema lines 4690 / 4600 / 4492
classes), `head -c 65536` truncation → exit 1 unreadable, a text `.rvt` → exit
2, `--dump-schema` on the 2024 pin → a 4492-entry JSON list; and
`tools/surface_bench.py --zip tekton-plugin.zip`: cowork / codeexec / local all
`preflight` · `author-prompt` · `go-author-prompt` (1.9 / 2.7 / 1.9 s) ·
`go-author-6panels` (3.9 / 4.7 / 4.3 s) · `edit-roundtrip` · `go-edit` ·
`validate` PASS; the one non-PASS is the pre-existing numpy-less `author-ifc`
BLOCKED on the two bare surfaces (#553's honest classification), untouched here.

Not claimed: nothing about viewer acceptance — this tool only reads. Not
claimed either: a ≤ 2023 project actually decoding through this path — no 2023
pin is tracked, so the `records32` rung is wired (module-referenced
`iter_records`) but unexercised here; #17's compose would give it a pin.

## 533.3 Findings / follow-ups

* **`natively_framed` now exists twice, word for word** (`tools/rvt_selfcheck.py`,
  `tools/rvt_inspect.py`; `enter_files_release` differs only in which entry it
  imports) — deliberately copied, not imported: importing the sibling tool
  would drag `rvt.ecc` (~6 ms of import) onto inspect's native path for
  nothing, and `release_ctx.py` / `rvt_selfcheck.py` are other engineers'
  territory this wave (#554 / #563). The right home is one engine-side
  predicate answering "already native?" from the *open* container's partition
  headers (no `detect_release` re-open, nothing heavy imported), shared by both
  entries and all three tools; then the copies go. Searched (`natively_framed`,
  `enter_files_release`, "native fast path"): no existing issue (#252 is the
  reader-object cousin, not this); filed task-shaped as **#567** (`Refs #533
  #518 #116`, `P2 ready area:frontdoor`).
* **`rvt.schema.parse` returns a 0-class `Schema` for empty / truncated input
  although its docstring says it raises `ParseError`**, and `Schema.stats()`
  then dies on `max()` of an empty sequence — the `main` traceback in 533.0.
  The tool's `if not sch.classes: raise ValueError(...)` inside its own guard
  is the correct shim at this altitude (a 0-class schema would poison
  `by_name['ADocument']`, `ObjectDecoder` and the sha-keyed memo anyway), but
  the law belongs in `_materialise` (`src/rvt/schema.py`, engine territory, not
  this issue's): `parse` should raise `ParseError("no class records in N
  bytes")` and honour its docstring for every caller. Searched ("schema parse
  empty", "zero classes", "stats max empty"): none; filed task-shaped as a
  **#569** (`Refs #533`, `P2 ready area:engine good-first-pick`); its DONE
  ends with deleting the two-line shim here.

## 533 BRANCH STATE

* Branch `cam/533-inspect-own-release` from `origin/main` @ 7d04c82, rebased
  onto 6250424 (#563) with the record conflict resolved (both sections kept,
  #535's first); one PR, `Closes #533`.
* Files: `tools/rvt_inspect.py` (new source), `tools/sync_plugin.py` (+1
  mapping), `plugin/skills/tekton-native/scripts/rvt_inspect.py` (regenerated
  mirror), `tests/test_inspect_release.py` (new),
  `tests/ci_shard.d/533-inspect-release.txt` (new), this section.
* Gates: listed in 533.2 (final head: merged shard 1922 passed / 134 skipped /
  3 xfailed; stream-local 13 passed; sync `--check` clean; validate_plugin
  PASS; portable paths ok).
* Follow-ups filed: #567 (engine-side "already native?" predicate; both tools
  drop their copies), #569 (`schema.parse` raises for empty input; then the
  two-line shim here goes).
* Staged vs shipped: nothing staged for the viewer; the plugin zip is a
  regenerated, git-ignored artifact.

---

# eng #567 (2026-08-10) — one engine-side "already natively framed?" predicate + "enter the file's own release lazily, once, note-never-raise" helper (`rvt.native_framing`); `rvt_selfcheck.py` and `rvt_inspect.py` drop their word-for-word copies (issue #567, Refs #533 / #518 / #116)

Stream: `natively-framed` (eng #567, engineer session started by the
tech-lead session). Written under this stream's own header; nothing above is
edited (eng #560 appends its own section to this same file this wave — if
both PRs are open at once the tech lead settles the record conflict; neither
touches the other's lines). Territory: `src/rvt/native_framing.py` (new, 70
lines incl. docstring) + its generated mirror `plugin/lib/src/rvt/native_framing.py`,
`tools/rvt_selfcheck.py` + `tools/rvt_inspect.py` (the two private copies →
one import + one call each; nothing else) + their regenerated tekton-native
mirrors, `tests/test_natively_framed.py` + `tests/ci_shard.d/567-natively-framed.txt`,
this section. **Untouched:** `src/rvt/global_framing.py` (see 567.1 for why the
helper did *not* land there although the brief named it first),
`release_ctx.py`, `src/rvt/versions/**`, `tools/rvt_edit.py` (eng #560),
`tools/rvt_edit_text.py` (checked: it imports `enter_host_release` eagerly at
module level and calls it unconditionally — it never carried the
`natively_framed` copy, so it is out of scope as the brief said), both
neighbour test files (green **unchanged** — not even the one assertion the
issue body offered to adapt), every hot file.

## 567.0 The starting point (fresh cloud clone, `origin/main` @ 4bd7ecf — #572 merged — before any change)

`tools/rvt_selfcheck.py:130-151` and `tools/rvt_inspect.py:56-76` each define
`natively_framed(doc)` (11 identical lines: `P.parse_stream_header(doc.logical(name))`
over `doc.partition_streams()`, any exception → False) and
`enter_files_release(stack, doc, path)` (native → None; else a lazy import +
one call), differing only in **which ladder** the second one imports:
selfcheck enters the authoring context `release_ctx.enter_host_release`
(#518/#538), inspect the read-side instrument ladder
`global_framing.enter_own_release` (#533's scope note on the issue). That
difference is load-bearing, not cosmetic — on a 2025 copy with 64 bytes of
`Formats/Latest` zeroed the host context cannot be built and enters *nothing*
(selfcheck's `warning: no release context for …`, walker 1 error, FAIL —
`test_damaged_schema_stream_still_reaches_a_verdict` asserts that sentence),
while the instrument ladder falls to the pinned table of the release
`BasicFileInfo` declares and the partitions then *walk*. So "make selfcheck use
the ladder inspect uses" would change a verdict; the shared helper has to
offer both.

Baseline captured before the change (kept for the diffs in 567.2): both tools,
repo CLI **and** as nobody from a bare unzip of the `main` zip (`env -i
PATH=/usr/bin:/bin HOME=<scratch> python3` 3.11, `_bootstrap.py run …`), on the
three pins + a header-zeroed and a schema-zeroed copy of each (built with
`read_entries` → replace one stream → `write_cfb`, nothing checked in): 45
runs, stdout / stderr / exit code / `--json` saved; `-X importtime` module sets
for both tools on the 2026 and 2025 pins, repo and bare.

## 567.1 What was built

* **`src/rvt/native_framing.py`** — two functions and one import
  (`from . import partitions as P`):
  `natively_framed(doc) -> bool` (the 11 lines, verbatim semantics: every
  `Partitions/<N>` header of the *open* document parses with the container
  class bound in `rvt.partitions` *right now* → True; a foreign release or a
  damaged header → False; never raises; no `detect_release` re-open, no schema
  parse) and `enter_files_release(stack, doc, path, *, host=False) -> str | None`
  (native → None, nothing entered, nothing imported; else lazily import and
  enter **once** on the caller's `ExitStack` either
  `global_framing.enter_own_release` — the default, the instrument ladder — or,
  with `host=True`, `release_ctx.enter_host_release`; returns that ladder's own
  note, never raises). `enter_own_release` / `enter_host_release` themselves are
  untouched, so every sentence either tool prints is the one it printed before.
* **Why its own module and not `global_framing.py`** (the brief's first
  choice, with exactly this fallback pre-authorised): the predicate has to be
  imported on the *native* path to be asked, so wherever it lives is one module
  more than `main` imports — "identical module set" is unreachable by
  construction for any shared engine home the tools did not already import
  (`rvt`, `container`, `partitions`, `objects`, `schema`, `ecc` — none in
  territory). The choice is therefore *which* one module. `global_framing`
  would cost about the same today (its top level is stdlib-only, 0.19 ms), but
  (a) the brief's rule was "`rvt.global_framing` may only be imported when the
  predicate says foreign", and (b) the `host=True` branch would make the
  read-side leaf lazily import `frontdoor.release_ctx` — the module whose
  docstring says it *composes* `global_framing`, i.e. an upward import. A
  30-line dispatcher that sits above both ladders and imports only
  `rvt.partitions` (which anyone about to walk a partition already holds) has
  neither problem: measured **0.14–0.16 ms** to import (`-X importtime`, repo
  and bare), pulls 0 new stdlib modules, 0 `rvt.frontdoor.*`, and no
  `rvt.global_framing` on the native path.
* **The tools**: `rvt_selfcheck.py` loses lines 130-151 and gains
  `from rvt.native_framing import enter_files_release` + `enter_files_release(stack,
  doc, a.path, host=True)`; `rvt_inspect.py` loses lines 56-76 and gains the
  same import + `enter_files_release(stack, doc, a.path)`. Nothing else moved
  (both still `import contextlib` for their `ExitStack`; both still reference
  `P.StreamWalker` through the module). Mirrors regenerated by
  `tools/sync_plugin.py` (3 files synced: the two scripts + the new
  `plugin/lib/src/rvt/native_framing.py`).
* **Test** `tests/test_natively_framed.py` (16 tests after `/simplify`, 0.7 s,
  fresh-clone safe — pins via `conftest.CERTIFIED_YEARS` / `pinned_base`;
  damaged copies built in-test; an autouse fixture snapshots the framing table +
  `active_release()` around every test): predicate True on the 2026 pin, False
  and no raise on a header-zeroed copy of **each** pin, False on each foreign
  pin bare and True again *inside* the file's own release (it reads
  `rvt.partitions` at call time); helper on the
  native pin with **both** ladders monkeypatched to raise → None for `host` in
  {False, True} (provably never reached); on each foreign pin the default enters
  the read-side ladder once (`CONTAINER_CLASS` == that year's, `active_release()`
  still None, a `StreamWalker` constructs, restored when the stack closes) and
  `host=True` enters the authoring context once (`active_release() == year`,
  restored); the schema-zeroed 2025 copy → `host=True` returns `no release
  context for <path>: …` with nothing entered, default returns `own schema
  unreadable (…); checked against the pinned Revit 2025 framing table (the
  release BasicFileInfo declares)` and the header then parses — the two
  sentences the two tools print; a subprocess `import rvt.native_framing` law
  (no `rvt.frontdoor` / `global_framing` / `versions` / `schema` / `famgen` /
  `genesis` in `sys.modules`); and the **AST law** on both tools: no
  `FunctionDef` named `natively_framed` / `enter_files_release`, no call of
  `.parse_stream_header`, and the `rvt.native_framing` import present (what
  ships is the byte-identical mirror `tests/test_plugin_sync.py` + `--check`
  already own, so the law covers the plugin too). In the shard via
  `tests/ci_shard.d/567-natively-framed.txt`.
* **`/simplify`** (four review angles on the diff): applied — module docstring
  cut from 35 to 17 lines (issue archaeology lives here, not there) and the two
  function docstrings to what a caller needs; `typing.Any` dropped (`doc`
  untyped, as the tools had it); two redundant test rows removed (a foreign-pin
  `False` row subsumed by the inside-its-own-release row; a mirror byte-compare
  duplicating `test_plugin_sync.py`), `FOREIGN_FIRST` → `CERTIFIED_YEARS` where
  order is irrelevant, a one-caller helper inlined, the two tools' import lines
  made identical. Skipped, with reasons: (1) "`host: bool` → accept the
  enter-callable" — the callable would have to be imported by the *tool*, either
  eagerly (release_ctx back on selfcheck's native path, the +40 ms of 518.1) or
  inside a private lazy wrapper (the very copy this issue deletes); the shared
  lazy import *is* the value, so the switch stays, named after the engine's own
  `enter_host_release` / `host_release_context`; (2) hoisting `_rewrite_stream`
  / `_native_constants` / `_no_leak` into `tests/conftest.py` — right, but
  `conftest.py` is outside this territory and the hoist across all six
  `test_*_release.py` files is already filed as **#579**; this file's copies go
  with it. Efficiency angle: nothing — `doc.logical()` is memoised
  (`container.py` `_logical_cache`) and the walk that follows reuses the bytes,
  exactly as before.

## 567.2 Evidence (head of this branch vs the `main` baseline of 567.0; bare unzip of the rebuilt zip, as nobody, unless stated)

| what | result |
|---|---|
| stdout, stderr, exit code and `--json` of `run rvt_selfcheck.py <f> --json …`, `run rvt_inspect.py <f> --records 20 --classes Wall` (bare) and the same two + `rvt_inspect.py <f>` (repo CLI), for `<f>` in {`G_ABPD.rvt`, `G_ABPD_2025.rvt`, `G_ABPD_2024.rvt`, header-zeroed copy of each, `Formats/Latest`-zeroed copy of each} = 45 runs, 153 captured out/err/rc/json files, captured twice (first head and the post-`/simplify` head, each from a fresh unzip of its own rebuilt zip) | **`diff` vs `main`: 0 of 153 files differ**, both times (the only normalisation: the scratch dir name inside the `json :` line). Exit codes identical run for run: pins 0/0/0; header-zeroed 1 (selfcheck, inspect `--records`) / 0 (inspect default); schema-zeroed 1 everywhere. |
| `-X importtime` module set, **2026 pin** (native path), repo CLI: selfcheck / inspect `--records 5` | main 113 / 114 modules (`rvt.*`: 6 / 5, `rvt.frontdoor.*`: 0 / 0) → head 114 / 115 (`rvt.*`: 7 / 6, frontdoor **0 / 0**); `comm`: only-in-head = **`rvt.native_framing`**, only-in-main = ∅ — for both tools |
| same, bare unzip (`PYTHONPROFILEIMPORTTIME=1 … _bootstrap.py run …`) | main 129 / 130 (`rvt.*` 10 / 9) → head 130 / 131 (`rvt.*` 11 / 10), frontdoor 0 → 0; delta = `rvt.native_framing` only; its own import line: 157 µs |
| same, **2025 pin** (foreign path), repo and bare, both tools | delta = `rvt.native_framing` only (selfcheck keeps its 5 `rvt.frontdoor.*` via the host context; inspect keeps 0); no `rvt.global_framing` on selfcheck's path, no `release_ctx` on inspect's — each ladder is imported only by the tool that enters it |
| wall, interleaved **7+7**, two fresh bare unzips (main zip / head zip) side by side, `env -i` python3, medians (sorted runs) | `selfcheck G_ABPD.rvt`: main **118** · head **114** ms (109 115 116 118 118 119 121 · 112 113 114 114 116 116 121); `inspect … --records 20`: **174 · 172** (171 173 174 174 175 175 212 · 165 168 170 172 181 192 194); `inspect` default: **167 · 163**; `selfcheck G_ABPD_2025.rvt`: **281 · 283**; `inspect 2025 --records 20`: **228 · 232**; `selfcheck` header-zeroed 2026 copy: **156 · 153** — parity everywhere, both directions inside the run-to-run spread |

**Gates:** `tests/test_natively_framed.py` 20 passed (0.84 s) on the first
head, **16 passed** after `/simplify`; `tests/test_selfcheck_release.py` +
`tests/test_inspect_release.py` + `tests/test_edit_text_release.py` **26 passed,
unchanged files** (3.1 s); with `tests/test_plugin_sync.py` + mine: 51 passed
(4.3 s); `tools/sync_plugin.py` → synced 3 files (then 2 after `/simplify`),
deny-audit clean, validation passed, zip rebuilt (5292 KB), identity scan 0
mismatches; `--check` clean; `plugin/scripts/validate_plugin.py` PASS (25
assertions); `tools/dev/check_portable_paths.py` ok (2970 tracked paths); whole
merged shard (`shard_list.py --print`, **97 files**, `RVT_SKIP_LARGE=1 … -p
no:cacheprovider`): **1952 passed, 134 skipped, 3 xfailed** in 8 min 10 s on
the first head (3ae176d; the `/simplify` commit on top only trims docstrings
and removes test rows — its stream-local + neighbour files re-run green, 51
passed, and the 153-file output diff was re-taken: 0 differ). `/verify` (the
`tools/rvt_*` + plugin rows, from the bare unzip as nobody): the 45 runs above;
`head -c 65536` truncations of the 2025 / 2026 pins → selfcheck exit 1 `VERDICT
: FAIL -> partition walker errors: 1` (2025 adds `; judged without its release
context (no release context for …: … schema sha256 e3b0c442… but the Revit 2025
pin is c964f9aa… …)`), inspect `--records 5` exit 1 `schema (Formats/Latest):
unreadable (ValueError: no classes in 0 inflated bytes) …`; a text file named
`.rvt` → exit 2 `ERROR: cannot open as an .rvt container: not an OLE2
structured storage file` from both — and for all six of those, **head output
`cmp` main output: identical, exit codes identical**; `tools/surface_bench.py
--zip tekton-plugin.zip`: cowork / codeexec / local `preflight` PASS ·
`author-prompt` PASS (2.8 / 3.6 / 3.2 s) · `go-author-prompt` PASS (2.2 / 3.2 /
2.4 s) · `go-author-6panels` PASS (4.5 / 6.0 / 4.8 s) · `edit-roundtrip` PASS ·
`go-edit` PASS (0.7 / 1.2 / 0.7 s; structural PASS, validation 0 errors) ·
`validate` PASS; the one non-PASS is the pre-existing numpy-less `author-ifc`
BLOCKED on the two bare surfaces (#553's honest classification, PR #571's
territory), untouched here.

Not claimed: no output byte of either tool changes on any input measured, so
nothing about viewer acceptance or the SKILL text moves; `enter_own_release` /
`enter_host_release` semantics are untouched (their other callers —
`rvt_analyze`, `seed_audit`, `provenance`, the two edit CLIs, `rvt_job` — are
not re-measured because they are not re-wired).

## 567.3 Findings / follow-ups

* **The next read-side CLI gets release awareness in two lines**
  (`from rvt.native_framing import enter_files_release`; `note =
  enter_files_release(stack, doc, path[, host=True])`) — the point of the
  issue. Candidates already named on GitHub: #560's plain `rvt_edit.py info`
  (eng #560, this wave — not touched here; if it wants the fast path it is that
  import) and #335's gate. No new issue needed: both exist.
* **The two *edit* CLIs still pay the eager price on a native file** — measured
  here in-process, three runs, after importing exactly `rvt_edit_text.py`'s own
  header modules: `from rvt.frontdoor.release_ctx import enter_host_release`
  costs **21–24 ms** (+10 `rvt.*` modules) and `enter_host_release(stack,
  <2026 pin>)` another **18–19 ms** (+3 modules; it re-opens the container for
  `detect_release`, then enters nothing) — ≈ 40 ms per native
  `rvt_edit_text.py` run for a `None`, against **0.7 ms** for
  `natively_framed(doc)` on the document the tool has already opened. Same
  shape as 518.1's +40 ms on selfcheck, now fixable in two lines
  (`enter_files_release(stack, doc, path, host=True)` after `open_rvt`, import
  moved off the module header). Not done here: `rvt_edit_text.py` carries no
  private copy so the brief put it out of scope, and `rvt_edit.py` is eng
  #560's this wave. Searched ("enter_host_release native fast path",
  "rvt_edit_text latency native", "detect_release re-open"): nothing open but
  this issue (#252 is the reader-*object* cousin — readers remembering their
  release — not the CLI entry); filed task-shaped as **#575** (`Refs #567 #518
  #116`, `P2 ready area:plugin good-first-pick`) with the measurement above as its evidence and
  "stdout/stderr/output bytes identical to main on the three pins + a damaged
  copy; bare-unzip interleaved medians before/after" as its DONE.

## 567 BRANCH STATE

* Branch `cam/567-natively-framed` from `origin/main` @ 4bd7ecf; one PR
  (#577), `Closes #567`.
* Files: `src/rvt/native_framing.py` (new) + `plugin/lib/src/rvt/native_framing.py`
  (generated mirror), `tools/rvt_selfcheck.py` + `tools/rvt_inspect.py` (copies
  → import + call) + `plugin/skills/tekton-native/scripts/rvt_selfcheck.py` /
  `rvt_inspect.py` (regenerated mirrors), `tests/test_natively_framed.py` (new),
  `tests/ci_shard.d/567-natively-framed.txt` (new), this section. Untouched:
  `global_framing.py`, `release_ctx.py`, `versions/**`, `rvt_edit.py`,
  `rvt_edit_text.py`, both neighbour test files, every hot file.
* Gates: listed in 567.2 (merged shard 1952 passed / 134 skipped / 3 xfailed;
  stream-local 16 passed; neighbours 26 passed unchanged; sync `--check` clean;
  validate_plugin PASS; portable paths ok; output diff vs `main` 0 of 153;
  importtime delta = `rvt.native_framing` only, 0 `rvt.frontdoor.*` on the
  native path; interleaved 7+7 medians at parity).
* Follow-ups filed: #575 (the two edit CLIs' native path through
  `native_framing`, ≈ −40 ms measured). Cited, not filed twice: #579 (test
  scaffolding into `conftest.py`), #560 / #335 (the next callers).
* Staged vs shipped: nothing staged for the viewer (no output byte of either
  tool changes); the plugin zip is a regenerated, git-ignored artifact.

---

# eng #560 (2026-08-10) — `rvt_edit.py` refuses a file it cannot open or plan at ONE open boundary, on both its paths: plain = warning + ONE `[rvt_edit] FAILED (…)` line, `--json` = ONE `{"ok": false, "error": …}` object and an empty stderr; exit 2 = not a container, 1 = cannot plan / impossible edit (issue #560 + its scope-widening comment, Refs #535 / #563 / #70 / #111)

Stream: `rvt-edit-refusal` (eng #560, engineer session started by the
tech-lead session). Written under this stream's own header; nothing above is
edited (eng #567 adds its own header to this file in the same wave — disjoint
lines). Territory: `tools/rvt_edit.py` (the open boundary of `_run` /
`_run_json`: one `_open` helper both call, the except arms, the exit-code
constants, the docstrings that document them; `main()` changed by exactly the
dispatch line — the warning print moved into `_run` so `--json`'s stderr is
empty), its three regenerated byte-identical mirrors
(`plugin/skills/tekton-edit/scripts/rvt_edit.py`,
`plugin/skills/tekton-native/scripts/rvt_edit.py`, `plugin/lib/tools/rvt_edit.py`
— `tools/sync_plugin.py`'s `EDIT_SCRIPTS`, the tekton-native file map and
`LIB_TOOLS_SHIM`), `tests/test_rvt_edit_refusal.py` +
`tests/ci_shard.d/560-rvt-edit-refusal.txt` (new), this section. Not touched:
`release_ctx.py` (settled by #563), `rvt_selfcheck.py` / `rvt_inspect.py` /
`rvt_edit_text.py` / `global_framing.py` (eng #567), `rvt_job.py`, every
`SKILL.md` (tekton-edit's documents `--json`, `ok:false` + ONE `error`, exit 0 =
written and both gates PASS — all still true), every hot file.

## 560.0 The defect, reproduced (fresh cloud clone, `origin/main` @ 4bd7ecf — #563 and #568 merged — before any change)

Bad inputs built in the scratchpad from the tracked pins (nothing checked in):
`text.rvt` (3 lines of ASCII), `trunc4k[_<y>].rvt` (first 4096 bytes of a pin),
`trunc64k[_<y>].rvt` (first 65536 bytes — still opens as CFB), `schema_dmg_<y>.rvt`
(64 bytes of `Formats/Latest` zeroed at raw offset 2000 via `read_entries` →
`write_cfb`, #518's repro), y ∈ {2026 (no suffix), 2025, 2024}.
`rvt_edit.py <bad> info [--json]`, repo `.venv` (3.11) and — same numbers ± the
bootstrap's frames — as **nobody** from a bare unzip of the zip built at `main`
(`env -i PATH=/usr/bin:/bin python3` 3.11.15, `import olefile` →
`ModuleNotFoundError`, i.e. the vendored fallback; `_bootstrap.py run rvt_edit.py …`):

| input | plain, `main` | `--json`, `main` |
|---|---|---|
| `text.rvt` | rc 1 · warning line, then **27-line traceback** (41 via `_bootstrap run`) ending `olefile.olefile.NotOleFileError: not an OLE2 structured storage file` | **identical traceback**, stdout EMPTY (no JSON at all): `NotOleFileError` is an `OSError`, outside `_run_json`'s `(ValueError, LookupError)` arm — the tech lead's widening comment |
| `trunc4k_2025` / `trunc4k` (2026) | rc 1 · warning + **34-line traceback** (48) `OleFileError: incomplete OLE sector` | same traceback, no JSON |
| `trunc64k_2025` | rc 1 · pin-mismatch warning + **20-line traceback** (34) `ValueError: unexpected Partitions header: v=9 cls=0x391` (the issue body's transcript) | clean: rc 1, ONE JSON (`error: "info failed: ValueError: …"`, `release_note`), 1 stderr line (the warning) |
| `trunc64k` (2026, native — **no note**) | rc 1 · **13-line traceback** (27) `RuntimeError: Partitions/21: walker errors ['no trailer for block at 30575 (B=8408)']` | **same traceback, no JSON** — `RuntimeError` is outside the arm too, and there is no note to blame |
| `schema_dmg_2025` / `_2024` | rc 1 · `… its Formats/Latest class schema cannot be read (ParseError …)` warning + **20-line traceback** `ValueError: unexpected Partitions header: v=9 cls=0x391` / `0x37b` | clean: rc 1, ONE JSON + 1 stderr line |
| `no_elemtable` (the 2026 pin re-emitted without `Global/ElemTable`: opens, native, no note) | rc 1 · **22-line traceback** `OSError: file not found` (olefile's `openstream`, *after* the container opened) | **same traceback, no JSON** |
| `G_ABPD.rvt deps --id 999` · `set-level --id 999 …` · `retype --id 1351691 --symbol 5 …` (a readable pin, an impossible edit) | rc 1 · **13/16-line traceback** `rvt.manipulate.ManipulationError: element 999 is not a host-document element …` / `… is a None, not a Level` / `5 is not a FamilySymbol` | clean: rc 1, `error: "<verb> impossible: …"`, stderr empty |
| the skill's real one call, `go edit <bad> info` (tekton-edit `_bootstrap.py`) | — | `text` / `trunc64k`: rc 1, `result: null`, `go.exception: "NotOleFileError: …"` / `"RuntimeError: …"` (go's last-resort `BaseException` net), 1 / 0 stderr lines; `schema_dmg_2025`: rc 1, `result.error: "info failed: ValueError: …"`, 1 stderr line |

So on `main` the plain path tracebacks on **every** unusable input and every
impossible edit (its `_run` calls `Document.from_file` and the verbs bare), and
the `--json` path tracebacks whenever the raise is not a `ValueError` /
`LookupError` / `ManipulationError`: a non-container (`OSError`) and a native
container whose partitions do not walk (`RuntimeError`).

## 560.1 What was built

* **One open boundary, `_open(path)`**, called by both `_run` and `_run_json`
  in place of the bare `Document.from_file(a.file)` (through the module global
  `Document`, so `test_frontdoor_json_strict`'s monkeypatch of `T.Document`
  keeps working — that contract test calls `_run_json` directly with a fake
  `Document` and a path that does not exist, which is also why the boundary
  cannot *start* with a container probe). It is the ONE place a user's file that
  cannot be used becomes ONE `Unopenable(sentence, rc)`: `Document.from_file`
  under one scoped `except Exception`; **only when that open has failed** is the
  container probed the way the three sibling CLIs probe it (`open_rvt(path)`,
  0.2–0.3 ms, failure path only — a readable file pays nothing): the probe fails
  → `"cannot open as an .rvt container: <e>"`, `rc = 2` (`rvt_edit_text` /
  `rvt_selfcheck` / `rvt_inspect`'s exact words and code for a text file / a
  mid-sector cut; `main()` has already refused a missing path via `ap.error`);
  the probe opens → `"cannot open/plan <path>: <Exc>: <msg>"`, `rc = 1` (the front
  door's words: `rvt.frontdoor._route_rvt_inner` guards this same call on this
  same kind of input with exactly this arm). The first head decided 2-vs-1 by
  exception *type* (`except OSError` → 2); the /simplify altitude pass showed that
  is the wrong depth — olefile raises `OSError` well *after* a successful open too
  (`IOError("file not found")` for a container lacking `Global/ElemTable`,
  `incorrect OLE FAT` for a broken chain), so a stream-less container read as
  "not a container, exit 2, release null" while the front door and the siblings
  call the same file opened-but-unplannable. Deciding it *positionally*, as the
  siblings do, fixes that (`no_elemtable` is now a test row: exit 1, `cannot
  open/plan …: OSError: file not found`, `release.input` = its year). **On
  breadth — the one judgment call to review:** the brief says "catching
  `UnreadableHost` / the note and the container-open `OSError`, nothing broader".
  `UnreadableHost` never reaches this tool (by #563's design `enter_host_release`
  turns it into the note before `_run*` is called), "the note is set" is not a
  usable discriminator (the native 64 KiB truncation in 560.0 has **no note** and
  raises `RuntimeError`; a damaged `Global/ElemTable` raises `zlib.error` /
  `struct.error` / `OSError`; a damaged schema on a native host `ParseError`), and
  `OSError` mostly means the wrong thing (above) — an enumerated tuple at this
  call is either incomplete (leaves tracebacks the DONE forbids) or a disguised
  `Exception`. So the guard is **scoped, not typed**: it wraps exactly one call —
  open + walk + parse of the INPUT — and nothing after it; the verb bodies keep
  precisely the arms `_run_json` already had, `_gates` / `_report` /
  `verify_manipulated` stay unguarded, so an engine bug during an edit still
  surfaces as itself. House precedent for a scoped `BLE001` at exactly this kind
  of boundary: `_route_rvt_inner`, `input_release_block`,
  `release_ctx._detect_release`, `rvt_edit_text._edit` (merged today — the shape
  the brief says to copy). A readable file never raises in `from_file`, so
  breadth cannot change any readable file's behaviour (560.2 proves it byte for
  byte); the /simplify altitude agent independently judged the breadth "right
  call, keep it".
* **One classifier for both doors, `_failure(cmd, e) -> (sentence, rc)`** over
  `REFUSED = (Unopenable, M.ManipulationError, ValueError, LookupError)`:
  `Unopenable` → its sentence + its rc; `DependentsError` → its first line (names
  the blocker); `ManipulationError` → `"<verb> impossible: <e>"`; `ValueError` /
  `LookupError` → `"<verb> failed: <Exc>: <e>"` — byte for byte the strings
  `_run_json`'s three arms produced on `main`, now defined once (the first head
  had copied the ladder into `_run` with drift already in it; /simplify folded
  it). `_run_json` has ONE `except REFUSED` arm (+ the `dependents` list when it
  is a `DependentsError`); `_run` has the same ONE arm.
* **`_run(a, note)`** (plain): prints the release warning itself (moved from
  `main()`, same text, same position — first line of stderr), then one `try`
  around open + verb: `info` / `deps` print as before; a write verb runs `_edit`,
  records `written = a.out` the moment `commit_plans` returned, then `_report`;
  `except REFUSED` → ONE line `[rvt_edit] FAILED (<sentence>)`, plus ` -- written:
  <out>` when a file is already on disk (deliverable rule: name it, whatever
  follows), and the classifier's rc. This is the issue's DONE bullet 1 ("`_run`
  catches what `_run_json` catches") with `_run_json`'s own messages. Success
  output is byte-identical (560.2).
* **`_run_json(a, note)`**: `rc = EX_FAIL` up front; ONE `except REFUSED as e:
  res["error"], rc = _failure(a.cmd, e)`; `res["release"] = _release_block(a.file,
  out)` unconditionally, as on `main` — `_release_block` is now *total*
  (`V.detect_release` on the input under a scoped guard → `None`; on `main` it sat
  outside the `try` and was the *next* traceback for a text file), so a
  non-container reports `{"input": null, "output": null, "opens_in": null, "line":
  null}` and every container reports its year whether or not it could be planned
  (the first head inferred `null` from the exit code — a smell /simplify removed);
  `return EX_OK if res["ok"] else rc`. The object keeps every key the success
  shape uses where one applies: `ok`, `command`, `input.path`, `release`,
  `release_note` (iff the entry produced a note), `seconds`, + `error`. **stderr
  is empty for every `--json` outcome**: the note already rides in `release_note`
  (it did on `main` too), so the duplicate `[rvt_edit] warning:` line is no
  longer printed in `--json` mode — the one observable change for a
  *readable-but-noted* input (`trunc64k_2025 --json`: 1 stderr line → 0; its JSON
  is unchanged but for the `error` wording `info failed: ValueError: …` → `cannot
  open/plan <path>: ValueError: …`). No test, tool, skill or reference reads that
  stderr line (grepped: `[rvt_edit]` appears nowhere outside the tool); `go`
  captures stdout only.
* **Exit codes documented** in the module docstring and as `EX_OK, EX_FAIL,
  EX_NOT_RVT = 0, 1, 2`: 0 done (read verb answered / written AND both gates
  PASS); 1 the input opened but could not be planned, the edit is impossible, or
  a gate stopped it (a written file is still named); 2 the input is not a Revit
  container at all (argparse usage errors are 2 as well, as in every sibling).
  `rvt_edit_text` uses 2 for all its refusals; `rvt_inspect` / `rvt_selfcheck`
  use 2 = not a container, 1 = could not read / FAIL — this tool now matches the
  latter pair and the brief ("2 for cannot-open, the tool's existing code for a
  note/FAIL"). `go edit` relays the code as `go.exit_code` unchanged.
* **`main()`**: the dispatch line only — `note = enter_host_release(stack,
  a.file)` (a note, never a raise, #563) then `_run_json(a, note) if want_json
  else _run(a, note)`; the two-line warning print moved into `_run`. Argument
  parsing, the missing-file `ap.error`, the `-o` directory creation and the
  `ExitStack` are untouched.
* **Test** `tests/test_rvt_edit_refusal.py` (11 tests, 2.0 s, fresh-clone safe —
  pins via `conftest.CERTIFIED_YEARS` / `pinned_base`, the tool via
  `conftest.load_tool`; `conftest.py` only read): a module fixture builds `text`,
  `trunc4k`, `trunc64k_<every certified year>`, `no_elemtable` (a pin re-emitted
  without `Global/ElemTable`: opens, enters its release, cannot be planned — the
  row that tells a positional discriminator from a type-based one) and (when a
  foreign pin is certified) `schema_dmg` from the tracked pins, each tagged with
  its expected rc, error prefix, source year and whether the release entry has a
  note for it;
  **plain** `info` on each → that rc, stdout `""`, no `Traceback`, exactly `1 +
  warned` stderr lines, the warning first (`[rvt_edit] warning: no release context
  for <path>: `), the last line `[rvt_edit] FAILED (<prefix>…)`; **`--json`**
  `info` on each → that rc, **stderr `""`**, stdout parses as ONE object, `ok
  false`, `command`, one-line `error` with the prefix, `input.path`, float
  `seconds`, `release_note` present iff warned, `release` == the four-`null` block
  for rc 2 and `input == <year>` / `opens_in` "Revit <year> …" for rc 1; a
  **write verb** (`set-level`) on `text` and `schema_dmg`, both modes → same rc and
  sentence, `output` absent, `written` never claimed, nothing on disk; an
  **impossible edit** in plain mode (`set-level --id 999999999`, `deps --id
  999999999` on a pin) → rc 1, exactly ONE `[rvt_edit] FAILED (<verb> impossible:
  …)` line, nothing written; **controls** on 2024, 2025, then 2026 in one process
  (foreign first; an autouse fixture snapshots the `rvt.partitions` framing table
  and `active_release()` around every test): `info` plain → rc 0, stderr `""`,
  the basement level listed; `info --json` → `ok true`, no `error` /
  `release_note`, `release.input == year`; `set-level --json` → rc 0, stderr `""`,
  `gates.hard_gates_passed`, `release.input == release.output == year`,
  `report.replaced == [[102, 1351691]]`, `detect_release(out) == year`; plain
  `set-level` → rc 0, stderr `""`, `written: <out> (` and `"walker_errors": 0`
  printed. The exit-code constants and the docstring's `Exit codes:` paragraph
  are pinned. In the shard via the drop-in.

## 560.2 Evidence (final head; † = as nobody from a bare unzip of the rebuilt zip, `env -i PATH=/usr/bin:/bin python3` 3.11.15, no `olefile`, via `skills/tekton-edit/scripts/_bootstrap.py`; unmarked = repo `.venv/bin/python tools/rvt_edit.py`, which prints the same lines)

| input, `info` | plain, head | `--json`, head |
|---|---|---|
| `text.rvt` | **rc 2**, stderr exactly 2 lines: `[rvt_edit] warning: no release context for …/text.rvt: not a Revit container tekton can open (NotOleFileError: not an OLE2 structured storage file)` / `[rvt_edit] FAILED (cannot open as an .rvt container: not an OLE2 structured storage file)`; stdout empty; † identical | **rc 2**, stdout ONE 16-line object: `ok false`, `error "cannot open as an .rvt container: not an OLE2 structured storage file"`, `release {input/output/opens_in/line: null}`, `release_note`, `command`, `input`, `seconds`; **stderr 0 B**; † identical |
| `trunc4k_2025`, `trunc4k` | rc 2, 2 lines (`… (OleFileError: incomplete OLE sector)` / `FAILED (cannot open as an .rvt container: incomplete OLE sector)`); † identical | rc 2, ONE object, same `error`, stderr 0 B; † identical |
| `trunc64k_2025` | **rc 1**, 2 lines: the pin-mismatch warning (unchanged) / `[rvt_edit] FAILED (cannot open/plan …/trunc64k_2025.rvt: ValueError: unexpected Partitions header: v=9 cls=0x391)`; † identical | rc 1, ONE object, `error "cannot open/plan …: ValueError: …"`, `release.input 2025`, `release_note`; stderr 0 B (main: 1 line); † identical |
| `trunc64k` (2026, no note) | rc 1, **ONE** line `[rvt_edit] FAILED (cannot open/plan …/trunc64k.rvt: RuntimeError: Partitions/21: walker errors ['no trailer for block at 30575 (B=8408)'])`; † identical | rc 1, ONE 15-line object (no `release_note`), `release.input 2026`; stderr 0 B; † identical |
| `schema_dmg_2025` / `_2024` | rc 1, 2 lines: `… its Formats/Latest class schema cannot be read (ParseError: parse error at 0x603b …)` / `FAILED (cannot open/plan …: ValueError: unexpected Partitions header: v=9 cls=0x391)` (`0x5c1b` / `0x37b` for 2024); † identical | rc 1, ONE object, stderr 0 B; † identical |
| `no_elemtable` (opens; native; no note) | rc 1, ONE line `[rvt_edit] FAILED (cannot open/plan …/no_elemtable.rvt: OSError: file not found)` — an `OSError`, yet classed *opened-but-unplannable* because the container probe succeeds (the first head said rc 2 "cannot open as an .rvt container: file not found", release null) | rc 1, ONE 15-line object, `release.input 2026`; stderr 0 B |
| impossible edits on `G_ABPD.rvt`: `deps --id 999`, `set-level --id 999 …`, `retype --id 1351691 --symbol 5 …` | rc 1, ONE line each: `[rvt_edit] FAILED (deps impossible: element 999 is not a host-document element (not in Global/ElemTable) -- cannot delete)` · `(set-level impossible: element 999 is a None, not a Level)` · `(retype impossible: 5 is not a FamilySymbol)`; nothing written | unchanged (already clean) |
| † the skill's one call `go edit <bad> info` | — | `text`: **rc 2**, `go.exit_code 2`, `go.exception` absent, `result.ok false`, `result.error "cannot open as an .rvt container: …"`, stderr 0 lines (main: rc 1, `result null`, `go.exception NotOleFileError…`, 1 line); `schema_dmg_2025`: rc 1, `result.error "cannot open/plan …"`, 0 lines (main: 1); `trunc64k`: rc 1, a real `result` (main: `result null` + `go.exception RuntimeError…`) |

**Identity — every readable file behaves byte-identically** (head vs the
`origin/main` worktree, same interpreter, same pins):

* `set-level --id 1351691 --elevation-ft 5 -o …` on the three pins, md5 of the
  written `.rvt`: 2026 `7ba3ff193bdb8b4450db0f7a73912d12` = `7ba3ff19…2d12`;
  2025 `e408fa955d41af027eb217fc046498ff` = same; 2024
  `314fe7a63f8f8c0bffd3c7464ce0274a` = same — **×3 identical**, repo CLI *and* †
  `go edit … set-level …` from the two unzips (same three md5s on both sides;
  `go.ready true`, `result.ok true`, `structural PASS … | validation PASS`, stderr
  0 B ×3 on both sides). The plain change report + `structural verify:` +
  `written:` lines: identical modulo the `-o` path; stderr 0 B.
* `info` on the three pins: plain stdout **`cmp`-identical** ×3 (repo and †),
  stderr 0 B both sides; `info --json`: identical modulo `seconds` (and, between
  the two unzips, the `input.path` of the unzip dir) ×3, stderr 0 B both sides.
* `-X importtime`, native pin, `info`: repo **173 = 173 modules, sorted sets
  diff-empty**; † `_bootstrap.py run rvt_edit.py assets/genesis/G_ABPD.rvt info`
  **180 = 180 modules (32 `rvt.*` each), diff-empty** — nothing new is imported
  (the two exception classes and `_open` live in the tool; `release_ctx` was
  already imported at module level on `main`). Wall, † same command, 5+5
  interleaved on the final zip: main 202 205 207 209 210 ms · head 203 204 207
  209 214 ms (medians 207 = 207; no work was added to the happy path: `_open` is
  the same call inside a `try`, the container probe runs only after a failure).

**Gates:** `tests/test_rvt_edit_refusal.py` **11 passed** (2.0 s); neighbours
`test_edit_own_release test_frontdoor_json_strict test_go_edit
test_release_ctx_refusal test_plugin_sync test_bootstrap test_edit_text_release
test_selfcheck_release` + mine: **105 passed** (14.1 s); `tools/sync_plugin.py` →
synced 3 files (the three `rvt_edit.py` mirrors), deny-audit clean, validation
passed, zip rebuilt (5294 KB), identity scan 0 mismatches; `--check` → `plugin in
sync with source`; `plugin/scripts/validate_plugin.py` PASS (25 assertions);
`tools/dev/check_portable_paths.py` ok (2970 paths); **whole merged shard**
(`RVT_SKIP_LARGE=1 … -p no:cacheprovider $(shard_list.py --print)`, 97 files):
**1943 passed, 134 skipped, 3 xfailed** in 6 min 05 s on the first head and
again **1943 passed, 134 skipped, 3 xfailed** in 5 min 55 s on the final
(post-/simplify) head, both over `origin/main` @ 4bd7ecf. `/verify` (the edits +
plugin rows): `rvt_edit.py G_ABPD_2025.rvt set-level --id 1351691 --elevation-ft
12 -o out/verify/e25.rvt --json` → rc 0, stderr 0 B, `Revit 2025 in, Revit 2025
out …`, `structural PASS … | validation PASS (0 errors, 0 warnings)`; standalone
`tools/rvt_validate.py out/verify/e25.rvt` → counts `{'error': 0, 'warning': 0,
'info': 2}`, rc 0 (validates 0 errors under its own release — not a viewer
claim); `tools/surface_bench.py --zip tekton-plugin.zip`: cowork / codeexec /
local all `preflight` · `author-prompt` · `go-author-prompt` (1.8 / 2.2 / 1.7 s)
· `go-author-6panels` (3.6 / 4.0 / 3.6 s) · `edit-roundtrip` · `go-edit` (job
0.40 / 0.65 / 0.42 s, both gates PASS) · `validate` **PASS**; the one non-PASS is
the pre-existing numpy-less `author-ifc` BLOCKED on the two bare surfaces (#553's
honest classification), untouched here.

## 560.3 Findings / follow-ups

* **/simplify (4 angles) applied:** (altitude + reuse) the 2-vs-1 discriminator
  is positional — the siblings' `open_rvt` probe, run only on the failure path —
  instead of `except OSError` (which misfiled a stream-less container, above);
  (simplification) ONE `_failure` classifier over `REFUSED` replaces the two
  copied except-ladders; ONE `Unopenable(sentence, rc)` instead of a class +
  subclass carrying class-attribute codes; `_release_block` made total on its own
  instead of `_release_block(a.file if rc != EX_NOT_RVT else None, …)` keyed on
  another step's exit code; docstrings trimmed to the file's density (the module
  docstring is the single statement of the exit codes). **Skipped with reason:**
  reordering `main()` to probe the container *before* `enter_host_release` so a
  non-container is described once (today: the note line + the FAILED line in
  plain mode, `release_note` + `error` in `--json`) — it needs the probe result
  threaded from `main()` into both runners (the strict-JSON contract test calls
  `_run_json` directly with a fake `Document` and a non-existent path, so the
  probe cannot live at the top of `_open`), i.e. a third parameter and a changed
  release-entry order in `main()` for a cosmetic gain the brief explicitly allows
  ("one or two stderr lines"); both lines are true and the second is the
  verdict. Dropping `written` (plain mode's ` -- written: <out>` suffix) — kept:
  three tokens that make the DONE's "a written file is still named" literally
  true if `verify_manipulated` ever raises a `REFUSED` type after the commit.
  Trimming the `set-level` ×3 controls (≈1.4 s, partly duplicating
  `test_edit_own_release` / `test_go_edit`) — kept: the brief names them as this
  file's controls and they are the same-file proof that success output is
  untouched. (efficiency) nothing else: no work added to the happy path, module
  set identical, fixtures module-scoped (0.07 s).
* **The deeper home is #335, widened by a comment (Refs #560), not a new
  issue.** After this PR five sites hand-roll "user file → sentence + code"
  (`_route_rvt` via `input_release_block` + its `cannot open/plan` arm,
  `rvt_job.py edit` — rc **1** for a text file where every other door says 2 —,
  the three `open_rvt`-first native tools, `rvt_edit._open`). #335 (`ready`,
  unassigned) already charters `rvt_edit.py` / `rvt_job.py edit` sharing the front
  door's input-era gate; the altitude review's proposal — one
  `input_release.open_input(path) -> Document` raising ONE typed
  `UnusableInput(path, why, kind)` that all four doors catch — is now a comment on
  it naming this file's `_open` and the `no_elemtable` test row as the pins to
  keep green. Searched first ("open_input UnusableInput typed exception edit
  doors", "rvt_job edit traceback non-container"): #335 open; #433 / #448 / #559
  closed.
* **Test scaffolding is a seventh copy — commented on #579 (Refs #560).**
  `_no_leak`, `NATIVE_LAST` / `FOREIGN`, `_rewrite_stream` and the
  `raw[:2000] + bytes(64) + raw[2064:]` recipe are copied from
  `test_release_ctx_refusal` on purpose (importing another test module's private
  helper is worse; `conftest.py` was not this stream's to edit); #579 already
  charters folding all of them into `conftest.py`, and now lists this file.
* **The other two edit doors already refuse cleanly — checked, nothing to file.**
  `tools/rvt_job.py edit <text.rvt | schema_dmg_2025> --ops … -o …` (the ops
  door, direct from a shell): rc 1, its manifest stub on stdout and ONE
  `[rvt_job] FAILED (edit: NotOleFileError: not an OLE2 structured storage file)`
  / `(edit: ValueError: unexpected Partitions header …)` line, no traceback
  (#433's guard); the front door's `--rvt … --edit` answers with `FAILED (cannot
  open/plan …)` (#563/#568; #573 in flight shortens the path in it). Searched
  issues ("rvt_job edit traceback non-container", "open boundary"): #433 / #448
  closed, #573 open and unrelated to this tool. So after this PR all three edit
  doors (`rvt_edit.py`, `rvt_job.py edit`, `frontdoor --rvt --edit`) refuse an
  unusable file in one line / one JSON.
* `_run_json` sets `res["id"]` only after the open succeeds (pre-existing
  ordering, kept): a write verb refused at the open boundary reports `command`
  but not `id`. Harmless (the caller passed the id), noted so a reader of the
  JSON is not surprised; moving the assignment above the `try` would be a
  gratuitous key-order change for the success shape's consumers.
* Not filed: the `[rvt_edit] warning:` line now exists only in plain mode; if a
  future reader wants the note on stderr in `--json` mode too, it is one line in
  `_run_json` — but then "stderr empty" stops being the `--json` contract, which
  is the property `go`'s one-JSON consumers actually want.

## 560 BRANCH STATE

* Branch `cam/560-rvt-edit-refusal` cut from `origin/main` @ 4bd7ecf, rebased
  onto 9fc890c (#571/#578, conflict-free) and then onto 6ee6f27 (#577/#580 —
  the record conflict with eng #567's section resolved by keeping both, theirs
  first as it landed, nothing inside it touched); PR #581, `Closes #560`. Review
  @ 06b34cc: 🟡 nits — applied: the container probe binds its own exception in
  the `cannot open as an .rvt container: <e>` sentence (literal parity with the
  siblings; identical text), and this base line.
* Files: `tools/rvt_edit.py`; regenerated mirrors
  `plugin/skills/tekton-edit/scripts/rvt_edit.py`,
  `plugin/skills/tekton-native/scripts/rvt_edit.py`, `plugin/lib/tools/rvt_edit.py`
  (byte-identical, `sync_plugin.py --check` clean); `tests/test_rvt_edit_refusal.py`
  (new), `tests/ci_shard.d/560-rvt-edit-refusal.txt` (new), this section.
* Gates: listed in 560.2 (final head: merged shard 1943 passed / 134 skipped /
  3 xfailed, 97 files; stream-local 11 passed; neighbours + mine 105 passed;
  sync `--check` clean; validate_plugin PASS; portable paths ok; surface bench
  PASS on all three surfaces but the pre-existing `author-ifc` BLOCKED).
* Follow-ups: no new issue; #335 widened by a comment (the shared `open_input` / `UnusableInput` home) and #579 told about this file's copied scaffolding — both `Refs #560`.
* Staged vs shipped: nothing staged for the viewer (no output byte changes — md5
  ×3 identical); the plugin zip is a regenerated, git-ignored artifact.
