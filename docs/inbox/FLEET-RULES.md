# ORCHESTRATOR FLEET RULES (binding, 2026-08-05 ~00:20)
1. IDENTITY: never write in another stream's voice. Additions to a record
   you do not own must sit under a header naming YOUR stream (the
   'ORCHESTRATOR RELAY' pattern). Unheaded insertions are attribution bugs.
2. ADJUDICATION convert-b: exactly ONE convert-b was chartered (the
   COMBINATION-INPUTS agent; its transcript holds task bg8zh5iun ->
   B2_merge_rst_devonly). The paragraph claiming bsb65kfwu in convert-b's
   voice was NOT convert-b; the only other agent chartered for
   combination-route demos is the perm-matrix stream — attributed there
   pending its own report. The neutral RUN ATTRIBUTION section stands.
3. SUITE: docs/inbox/SUITE-COORDINATION.md remains binding — canonical run
   only (pid 27375, near completion); stream-local test FILES are fine.
   Four more full-suite violators were culled at ~00:15 (ps-verified).

## TICKETS (orchestrator triage queue)
- GraveyardRec ElemTable codec gap (convert-b C3: foreign-.rfa extract
  partial) -> stream-encoders/ElemTable owner; blocks full RFA->RFA on
  foreign files only.
- convert-b viewer batch: B1/B2 (sample-base merges) uploadable anytime;
  A1/A2 + C1 (genesis-lineage targets) HELD until the instance-on-genesis
  audit fix lands — they compose the exact operation under investigation;
  uploading now would duplicate known-failing shapes.
