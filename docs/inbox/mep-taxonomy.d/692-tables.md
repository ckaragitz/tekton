# #692 slice 1 — the taxonomy and vendor tables, their verbs and their gates

Stream index: `docs/inbox/mep-taxonomy.md`. Issue #692 (P0, from steer #685). Branch
`cam/692-mep-taxonomy`, PR #735.

## What was built

| File | What |
|---|---|
| `src/rvt/famgen/taxonomy.py` (new) | `Kind` rows: key, label, discipline, the `skeleton._resolve_category` key (or `None` + `pending` = the Revit category a pending row waits for), `via` = build mechanisms in priority order (`famspec:<kind>[/<sub>]`, `archetype:<key>`, `house:<module>:<fn>`), aliases, note. Derived: `lane` (catalog / archetype / none), `revit_category` (`INTENDED_LABEL[key]`). API: `kinds() keys() get() resolve(text) by_discipline() archetype_registry() category_status(row) facts_tier(kind) builder_available(row, strict=) describe(text) table() check_row(row) check()`. |
| `src/rvt/famgen/vendors.py` (new) | `Vendor` → `Line` rows: label, the kinds a line covers, `record=True` only when `rvt.famgen.catalog` holds `facts/<vendor>/<line>.json`. API: `vendors() get() resolve(text) lines_for_kind(kind) records_for_kind(kind) record_tier(vendor, line) describe(text, kind=) table() check_line(v, ln) check()`. |
| `tools/make_family.py` | verbs `taxonomy [KIND|text] [--discipline D] [--json] [--check]` and `vendors [VENDOR] [--kind K] [--json] [--check]`; the three `--check` gates (standards, taxonomy, vendors) share one `_gate()` printer. |
| `tests/test_taxonomy_692.py` + `tests/ci_shard.d/692-mep-taxonomy.txt` | 38 tests, mechanism-pinned (below); 0.5 s, in the CI shard. |
| plugin mirror | `plugin/lib/src/rvt/famgen/{taxonomy,vendors}.py`, `plugin/skills/tekton-author/scripts/make_family.py` (generated by `tools/sync_plugin.py`). |

## The three computed truths (nothing below is declared where it can be measured)

1. **Category status** — every row's *intended* Revit label is cross-checked against the ids
   Revit-written files show (`rvt.inventory.BUILTIN_CATEGORIES_VERIFIED`, sample-corroborated;
   `…_ASSUMED` are hints and never grounds for conflict): `confirmed` / `inferred` / `conflict`
   / `pending`. A row in conflict or pending **never claims placeability**, whatever builder it
   has, and names #516. On `main` today: 52 confirmed, 12 inferred, **13 conflict** (the six
   fire-alarm kinds — resolver `fire_alarm_device` → −2008013, which sample files show hosting
   'M_Supply Diffuser' = Air Terminals; the three lighting devices — −2008080 vs the verified
   −2008087; cable tray section + fitting — −2008131 vs −2008126; conduit section + fitting —
   −2008133 vs −2008128), 5 pending (air terminals, sprinklers, duct run, pipe run, grease
   interceptor). PR #698 (open, closes #516) corrects exactly these resolver ids from Revit's own
   family templates; the day it merges the 13 rows flip with no change here — the tests pin the
   mechanism (a monkeypatched verified table turns `panelboard` into a conflict and back), not
   today's counts.
2. **Facts tier, per member** — a catalog row's worth is counted from the `field_provenance`
   flags of the variant(s) the kind actually builds (`vendors.record_tier(vendor, line,
   model=)`; a device sub-kind selects its variant through the factory's own `DEVICE_KINDS`,
   imported lazily for those four rows only): `fact` when that member carries at least one
   fact-tier field, `assumed` when every one of its values is search-summary. Findings:
   `lithonia/ldn6-led-downlight` holds **0 fact-tier fields** (11 `assumed`); in
   `generic/devices-and-mounting` all 4 fact fields sit on `box-4in-square`, so the two
   receptacles and the switch are `assumed`-tier members while the junction box is `fact` —
   `describe()` says "variant duplex-receptacle-5-15R carries NO fact-tier field: the values are
   search-summary `assumed`, and the family says so" instead of "sourced facts".
3. **Availability** — the first mechanism in `via` that is available wins; `describe()` probes
   cheaply (`importlib.util.find_spec`, registry probe, record presence) so a surface can describe
   a kind without loading the factory; `check()` proves strictly (imports every constructor via
   `famspec.constructor_name`, every house builder). The archetype registry (#674) is probed on
   each call, never imported at load: absent → "not on this build", present (a fake module in the
   tests) → the rows light up.

## Evidence (numbers)

- `make_family.py taxonomy --check` → warning line naming the 13 conflict kinds (#516), then
  `82 kinds, 0 problems`; `vendors --check` → `50 vendors, 118 lines, 7 with a catalog record,
  0 problems`; `standards --check` → `27 categories, 0 problems` (unchanged, now via `_gate`).
- Taxonomy: 82 rows — electrical 25, lighting 12, fire_alarm 6, technology 7, mechanical 17
  (2 pending), plumbing 13 (2 pending), fire_protection 2 (1 pending); lanes catalog 8,
  archetype 5, none 69. **Buildable on `main` today (9):** panelboard (138 fact fields across
  its two records), transformer_dry (155), junction_box (4), troffer (11) at `fact` tier;
  receptacle, receptacle_20a, light_switch, downlight from `assumed`-only members (said so);
  switchboard as the house model ("prompt-default or given dimensions — no manufacturer member").
  With a #674-shaped registry present, wireway and strut_channel light up; cable_tray and conduit
  stay blocked by their category conflict until #698.
- Vendors: 50 makers, 118 lines, 59 of 82 kinds have at least one maker listed; records held for
  exactly the 7 catalog lines (line-level tiers: 6 `fact` — Eaton ×2, Square D, HPS, generic
  devices, Lithonia BLT — and 1 `assumed`, Lithonia LDN6). A maker with no record never reads as
  if its product were built: `describe("siemens", kind="panelboard")` → "known by name only …
  Panelboard: buildable here only from the records held (eaton/…, square-d/…) — never presented
  as a Siemens product"; `describe("carrier", kind="chiller")` → "Chiller: not buildable here yet
  — no lane builds it".
- Import weight (efficiency review, `-X importtime`, warm stdlib): taxonomy 2.1 ms, vendors
  1.9 ms; nothing from standards/skeleton/catalog/factory/ifc is imported at module load.
- Tests: `tests/test_taxonomy_692.py` 44 passed / 0.5 s (CLI verbs in-process via
  `make_family.main`, one real-process smoke); neighbours `test_famgen_standards`,
  `test_famgen_catalog`, `test_famgen_factory`, `test_plugin_sync`, `test_bootstrap`,
  `test_records_layout` — 186 passed, 5 skipped together. `sync_plugin.py --check` clean; `validate_plugin.py` PASS; portable paths ok
  (3133). `/verify`: `make_family.py device --kind switch` and `--kind duplex-receptacle` still write
  their `.rfa` (229,376 bytes each), provenance ok on all 11 checks, validator `VALID (no errors)`
  in `--family` mode (a fact about the file, not a claim that Revit opens it).

## Review passes and what they changed

`/simplify` (four independent reviewers) turned v1 into v2: the redundant `standards` column
went (always == category); `builder` + `famspec` + `archetype` + declared `lane` collapsed into
one `via` tuple with a derived lane, which also fixed two v1 defects the altitude reviewer
proved — junction_box's archetype fallback was dead code, and switchboard was labelled
"nominal" although `make_house_switchboard` takes given/prompt-default dimensions; the
`NOT_YET` side table became pending rows (aliased, discipline-filed, collision-checked); one
shared `_alias_index()` builds both indexes and reports clashes; the registry probe has one
contract (`ARCHETYPES`); `describe()` no longer imports the factory or `rvt.ifc.intent`;
`Line.facts` (always == key) became `record: bool`; the reuse reviewer's id/label drift finding
became the computed category status above; six of the prompt grammar's kind names now resolve
(all but `luminaire`, a family rather than a kind — pinned by a test); tests pin mechanisms with
negative cases (`check_row` / `check_line` on broken rows) instead of content counts, and the
"no numbers" guard uses `typing.get_type_hints`.

The independent review of head `a75e8e0` (🛑, PR #735 comment) found six honesty gaps, all fixed
on the next head: the vendor line for a maker with no record asserted "a family is generated"
even for kinds nothing builds → now computed per kind from `taxonomy.builder_available` (and
never phrased as building that maker's product from another maker's record); `check_row`
forgave every archetype failure → it forgives only an ABSENT registry, and with a registry
present it checks the key exists and its `category` matches the row; famspec sub-kinds were
checked for presence, not value → validated against the factory's `DEVICE_KINDS` /
`_LUM_KINDS`, and a record's `revit_category` (OST name) must be the row's intended label;
the facts tier was per line → per member (above); `category_status` now also hints where the
published-constant table puts the label, and an `inferred` id is caveated whether or not the
kind builds; `taxonomy --check` runs the gate before touching `table()` so a broken row yields
its message, not a traceback.

## What this does NOT do yet (slice 2 = DONE 3 relay + DONE 5)

The prompt resolver (`src/rvt/frontdoor/prompt_intent.py` / `router.py`) and the #684 interview
still carry their own product lists; slice 2 replaces them with `taxonomy.resolve()` /
`vendors.lines_for_kind()` and relays `describe()["line"]` for a kind that is placed but not
built. Two things this slice deliberately leaves to their owners: the resolver ids (#516 / PR
#698) and `skeleton.category_label` — writer state feeding the PartAtom that knows only the
five desktop-verified ids, so families in the other categories carry a `Category -20011xx`
label there; worth its own issue once #698 lands (noted on #516).

## BRANCH STATE

- Files: `src/rvt/famgen/taxonomy.py`, `src/rvt/famgen/vendors.py`, `tools/make_family.py`,
  `tests/test_taxonomy_692.py`, `tests/ci_shard.d/692-mep-taxonomy.txt`, this fragment + the index
  `docs/inbox/mep-taxonomy.md`, plugin mirror (3 generated files).
- Gates: as listed under Evidence; sandboxed session CI + independent review recorded on PR #735.
- Nothing staged for the viewer; no `.rvt`/`.rfa` bytes change (tables + CLI only).
