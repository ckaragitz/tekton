# electrical-fixtures — the third electrical category: wiring devices generate, load unplaced, and are prompted as planned devices (issue #166)

Stream: `electrical-fixtures` (eng #166, cloud engineer session, 2026-08-09) · Refs #108 (requirements
sweep wave 2, axis RFA / families), #101 (prompt grammar neighbour) · program goal PG6 · follow-ups
filed: **#359** (front door LOADS + PLACES Electrical Fixtures), **#361** (famspec kind `device`).

## Why

The facts for NEMA 5-15R / 5-20R receptacles, the single-pole switch, the 4 in square box and the code
mounting heights were already in the store (`facts/generic/devices-and-mounting.json`), the skeleton
already knew `OST_ElectricalFixtures`, and the prompt parser already recognised "receptacles / outlets" —
but only to report them under `not_built`. This stream makes the category real where our own code can:
a generated family, a CLI verb, an honest load lane, and a prompt/intent that plans and lays the devices
out instead of shrugging.

## The decision the issue asked for (made first): placement is a follow-up (#359)

The DONE said: *"if placement of the new category needs loader work beyond a template-instance mapping,
ship generation + `--no-place` load first and file the placement as a `Refs` follow-up."* It does —
three pieces outside this stream's territory pin the front door's family pipeline to Electrical
Equipment:

| Piece | Where | What pins it |
|---|---|---|
| constructor dispatch | `tools/ifc_intent.py::_constructor_for` (:110-118) | only `make_panelboard` / `make_transformer` / `make_house_switchboard`; a `make_device` plan raises `KeyError` in stage F/L |
| host survey category | `rvt.famgen.loader.load_family_into_project` (:1561) and `load_families_into_project` (:1725) | both call `survey_host(host_rvt)` with its **default** `category=CAT_ELECTRICAL_EQUIPMENT` (the function itself is parameterised) → surrogates / `FamilySymbol` header / tracking row of a fixtures family would carry the equipment category; the batched loader surveys ONE category for N families |
| placement specimen + scrub | `tools/ifc_intent.py::stage_equipment` (:833-868) | clones the certified ancestor's Electrical **Equipment** instance and scrubs with `category=OST_ELECTRICAL_EQUIPMENT` hard-coded; the issue's own measurement (`LoaderError 'no template instance for placement'`) is this |

`tools/ifc_intent.py` / `build.py` were held by #146 during this session, and `loader.py` is not this
stream's territory. So: generation + unplaced load + prompt planning shipped here; load-and-place in the
room build is #359 (P1, ready, body written with the exact file:line evidence above and a no-regression
clause for mixed prompts).

## What was built

| Piece | Where | What |
|---|---|---|
| **Facts gap filled** (facts only, every leaf flagged) | `src/rvt/famgen/facts/generic/devices-and-mounting.json` | `options.plate_in {w,h,t}` on all four variants (single-gang wall plate 2.75 × 4.5 × 0.25 in envelope; 4 × 4 × 1/16 in blank cover for the box) — `assumed`; `options.unit_load_va {180, NEC 220.14(I)}` on the two receptacles — `assumed` (recalled code figure); `options.mounting_height_aff_in {typical 18}` on the box — `assumed`. `python -m rvt.famgen.catalog validate` → `[OK] generic/devices-and-mounting`, 4 variants, **4 fact / 37 assumed / 0 unsourced / 0 missing flags** (was 4 / 30). |
| **Facts resolver** | `rvt.famgen.factory.resolve_device_facts(kind, mounting_height_in=None, voltage=120, va=180)` + `DEVICE_KINDS` / `device_kind()` (aliases: receptacle/outlet/5-15r → duplex-receptacle, 5-20r, toggle-switch/light-switch → switch, jbox/j-box/box → junction-box) | box envelope ← `dims_in` (flag from the record: `assumed` for devices, `fact` for the 4 in square identity); plate ← `options.plate_in`; `mounting_height_in` = given (`given`) else the record's typical (18 receptacle & box / 48 switch, `assumed`, note carried); `ada_reach_range_in [15, 48]` from `line_facts` (`fact`, ADA 308.2.1); `device_rating_v/a` (record); `voltage_v` / `load_va` = the JOB's connector values (`given`; the load note cites the NEC 220.14(I) 180 VA unit load, and says out loud that a switch / box carries no code unit load); `configuration` / `manufacturer='generic'` (`ours`), `model` = the variant. Unknown kind → `FactoryError` naming the known kinds. |
| **The constructor** | `rvt.famgen.factory.make_device(kind, mounting_height_in=None, voltage=120, va=180, solid=True, name=None, start_id=1000, shared_params=None)` | `OST_ElectricalFixtures`, part type 0, **work-plane-based** exactly like the panelboard (family XY = the wall face; *no face-/wall-hosting claim*); parameters `Voltage` (voltage), `Load` (apparent power, electrical-loads group), `MountingHeight` (length, constraints) + the identity built-ins on the ONE type row (`Manufacturer='generic'`, `Model=<variant>`, `Description`); geometry = `G.plate` **faceplate proud of the face** (z 0..t) + `add_box_form` **device box recessed** (z −d..0); ONE connector via the #323 API: `add_connector(face='bottom' = the back of the box, poles=1, voltage_v=120, apparent_load_va=180, primary=True, bind_voltage_param='Voltage', bind_load_param='Load', load_class 'Receptacle' | 'Power')` → Power-Unbalanced (31), load on phase 1, `m_bIsConnectorPrimary True`; `FamilyProduct(kind='device', forms=[plate, box])`, notes surface every assumed field. |
| **CLI** | `tools/make_family.py` | new verb **`device --kind duplex-receptacle|duplex-receptacle-20a|switch|junction-box [--height IN] [--voltage 120] [--va 180] [--shared-params FILE] [--dummy] -o OUT [--json]`** and new verb **`load-device --kind … [--height] [--voltage] [--va] [--host HOST] -o OUT.rvt`** (shared `_device_flags`): loads the family **UNPLACED** through the certified four-registry loader (`rvt.famload` via `famfrom_ifc.load_into_project`, `core_categories=(the family's own category,)` — exactly the famspec→rvt lane; this verb is the stand-in for a famspec `device` kind, #361), default host = the pinned genesis base (bundled → runs on a fresh clone), one bound constructor (`functools.partial`) for the probe and the above-watermark rebuild, prints one JSON with `placed: false` + the #359 pointer (rule 1: delivered, labelled). The panelboard `load` verb is untouched. |
| **Prompt grammar** | `src/rvt/frontdoor/prompt_intent.py` | `receptacle_device` moved from `_UNBUILT_PATTERNS` to `_KIND_PATTERNS` (tag prefix `R`; duplex / convenience / quad / gfci / general-purpose receptacles, (power|wall) outlets, never "receptacle panel"); `PLANNED_ONLY_KINDS = {receptacle_device: "issue #359 …"}`; `_RE_AFF` reads a mounting height ('at 18 in AFF', '44 inches above the finished floor', '1100 mm mounting height' — unit **and** an AFF phrase required, so a count or the room's 'N ft high' is never taken; digits ≤ 4); `PromptItem.height_in`; `_apply_defaults` device branch (120 V default, else `_device_voltage()` = the L-N of a parsed wye system so '277 V receptacles' → 277 on contract AND plan; 180 VA; height from OUR facts via `_device_height_default()` — ONE coverage line per clause, stating the `assumed` convention and the ADA 15..48 in FACT); `_device_facts()` is `lru_cache`d (42 devices → 3 resolves, was 3 per device); `_contract_for` → a `DeviceSchedule` pset (Voltage 120 V, Phases 1, Wires 2, Load, MountingHeight, Mounting, DeviceType); `_default_dims` from the facts (plate w × h, box depth + plate); layout = **the wall-panel law** (west/east interior faces, upright frame into the room, alternating W/E) with devices sorted after every panel kind and `z = _wall_mount_z(item)` = the AFF height (panels keep 1.42 m); `prompt_to_intent`: `IfcOutlet` / `POWEROUTLET`, description names the height, disposition says "generated-family, NOT loaded/placed by this route yet (#359)", and **`_plan_device(eq)` + `_resolve_device_facts(fp)`** — shaped exactly like `intent.plan_family_for`'s branches (kwargs from the equipment's own `DeviceSchedule` pset, then the resolver twin of `_resolve_panel_facts`) so #359 moves the pair verbatim — replace the resolver's `unmapped` plan with `FamilyPlan(constructor='rvt.famgen.factory.make_device', kwargs, catalog/variant/compact facts_summary ≈1.3 KB, status='planned', refusal="not a refusal: … #359")`; `_type_name_for` gains `RCPT-<V>V-<VA>VA-<H>IN`; scene brief: `receptacle_R-n` groups, `DeviceSchedule` pset with typed Load / MountingHeight / DeviceType. The unbuilt-reason prose no longer claims devices are unbuildable and points luminaires at #150. Clause boundaries no longer split on a decimal point inside a number (`\.(?!\d)`) — '1.1 m above the floor' / '7.5 kVA' stay one clause. |
| **IFC intent constants** (only these, per territory) | `src/rvt/ifc/intent.py` | `KIND_BY_CLASS['IfcOutlet'] = 'receptacle_device'`; `GENERATED_KINDS += ('receptacle_device',)`; `WALL_KINDS += ('receptacle_device',)` (so the IFC route frames an outlet upright on its wall like the prompt route, not as floor gear) (disposition `generated-family`; audit `load_equipment_unfed` now lists devices honestly). `plan_family_for` untouched (its device branch is #359's, so the IFC route's outlets stay `unmapped` with the generic reason until then). |
| **Docs** | `plugin/skills/tekton-author/references/CATALOG-FACTS.md`, `docs/product/PERMUTATION-MATRIX.md` | store row rewritten (envelopes / unit load / heights / ADA fact); constructor table gains the device row with its honest status (+ fixed the stale `make_lightfixture` name → `make_luminaire` / `make_downlight`); prompt→rvt and prompt→rfa caveat text say what happens to prompted receptacles today. `matrix.py` untouched — no cell's truth changed (`verify_evidence()` green, doc test agrees). |
| **Tests** | `tests/test_famgen_factory.py` (+3 kinds in `ALL_KINDS`, `test_device_facts_resolve_from_the_generic_record`, `test_device_family_composition[3]`, `test_make_family_cli_device_verb_writes_a_valid_rfa`), `tests/test_prompt_intent.py` (+3), `tests/test_frontdoor.py` (+`test_e2e_prompted_receptacles_are_delivered_as_planned_devices`) | all fresh-clone-safe (bundled base + catalog only); all three files are already in the CI shard. |

## Evidence (fresh cloud clone: no `samples/`, no `extracted/`; bundled pinned base G_ABPD)

**Generation — `tools/make_family.py device --kind K -o out.rfa`, then `tools/rvt_validate.py out.rfa --family` and `tools/make_family.py provenance out.rfa`:**

| kind | family name | elements | forms (in) | connector decode ON THE FILE (poles / V / VA ph1 / primary / system) | family-mode validate | `rvt_validate --family` | provenance |
|---|---|---|---|---|---|---|---|
| duplex-receptacle | Duplex Receptacle NEMA 5-15R 120V | 52 | plate 2.75×4.5×0.25 @z0; box 1.9×4.19×2.5 @z−2.5 | 1 / 120 / 180 / True / 31 | VALID 0 err 0 warn | 0 errors, 0 warnings (159 records, 52 elements) | ok, suspects [] (11/11 v2 checks; standalone scan 4/4) |
| switch | Single Pole Switch Single Pole 120V | 52 | same envelope | 1 / 120 / 180 / True / 31 | VALID 0 / 0 | 0 / 0 | ok |
| junction-box | Junction Box 4in Square 120V | 52 | plate 4×4×0.0625; box 4×4×1.5 (facts) | 1 / 120 / 180 / True / 31 | VALID 0 / 0 | 0 / 0 | ok |

Unverified fields surfaced per family: receptacle/switch 11 (`box_*`, `plate_*`, `device_rating_*`, `mounting_height_in`, `voltage_v`, `load_va`); junction box 6 (the box dims are facts).

**Unplaced load — `tools/make_family.py load-device --kind duplex-receptacle -o dev_loaded.rvt`** (host = `plugin/assets/genesis/G_ABPD.rvt`): loader `rvt.famload.load_family_document (four-registry, L1a mechanism)`, plan category **−2001060**, host Family 1472577 / symbol 1472583, `placed: false`, project validator **VALID, 0 errors / 1 warning**, wall time **2.2 s**.

**The DONE prompt — `tools/frontdoor.py author --prompt 'a room with 4 duplex receptacles' --out out/x --json`:** exit **0**, 1.4 s, status `PROOF-ONLY (self-checks PASS …)`; `intent.summary.equipment_by_kind = {receptacle_device: 4}`, `family_plans_by_status = {planned: 4}` (constructor `make_device`, variant `duplex-receptacle-5-15R`, kwargs `{kind duplex-receptacle, mounting_height_in 18.0, voltage '120', va 180.0}`); insertions (−4.47 | +4.47, y, **0.457 m = 18 in**) upright on W-W / W-E, `equipment_inside_room_ring 4/4`; coverage `not_built []`, `ignored_words []`, defaults state the 120 V / 180 VA NEC line and the 18 in convention + ADA 15..48 in fact; build: `errors []`, combination verdict `single` (n_instances 0 — the open cell is not exercised, so no open-cell stamp; the standing PROOF-ONLY status-gate stamp applies), `elements_created = 4 × wall`, **4 degradation lines** `R-n (receptacle_device): NOT built -- family plan planned: not a refusal: … loads UNPLACED … issue #359`, `prompt_room.rvt` project validator **VALID 0 errors / 1 warning**; MANIFEST.md carries `equipment (4): receptacle_device×4` and both default lines. *Not* 4 placed instances — that half of the DONE is #359, by the DONE's own fallback clause.

**No regression on mixed prompts** (the reason device plans are `planned`, not `resolved`: `stage_load_batched` stops the batch at the first un-authorable family, and `R-1` sorts before `RP-1` / `T1` at equal feeder depth): `'… 150 kVA transformer, a 225 A 208Y/120 receptacle panel and 6 receptacles at 1.1 m above the floor'` → buildable plans `[RP-1, T1]`, `combination_check.n_instances == 2`, feeders `[(T1, RP-1)]`, devices at z 1.1 m after the panel on the same faces; `'… 1600A switchboard, twelve 2x4 LED troffers …'` parses byte-for-byte as on `main` (luminaire still `not_built`).

**Gates (after the `/simplify` cleanup pass — 4 review angles, fixes applied: one device-constants row, AFF gating decoupled from the delivery-status table, memoised facts, `_plan_device(eq)` from the equipment's pset with the contract/plan voltage disagreement fixed, `_type_name_for` device branch, ratings loop + dims lists in the factory, `load-device` as its own verb):** `tests/test_famgen_factory.py` + `tests/test_prompt_intent.py` + `tests/test_frontdoor.py` (RVT_SKIP_LARGE=1) **139 passed / 9 skipped**; `tests/test_router.py -k matrix` 8 passed; `tools/sync_plugin.py` → 4 mirrors regenerated, `--check` clean (deny-audit clean, identity scan == allowlist); `plugin/scripts/validate_plugin.py` PASS (25 assertions); `tools/dev/check_portable_paths.py` ok (2833 paths). `tests/test_famgen_skeleton.py` does not exist (nothing to run). Full-suite not run (SUITE-COORDINATION).

## Review round 1 (tech-lead review of head 80bfbe6): the IFC emit blocker, fixed

The independent review found ONE PG1 defect: `src/rvt/frontdoor/ifc_out.py` whitelisted four
entity classes and coerced the new `IfcOutlet` equipment into `IfcElectricDistributionBoard` with a
`PanelSchedule` pset — `route.py run --prompt "… a 100 A lighting panel and 4 duplex receptacles"
--output ifc` wrote FIVE boards, and re-entry (`author --ifc`) read them as `receptacle_panelboard`
×4 with refused `make_panelboard` plans. Territory was extended to `ifc_out.py` for exactly this.

| Fix | Where | What |
|---|---|---|
| honest emit | `src/rvt/frontdoor/ifc_out.py` | `_CLASSES` gains `ifcoutlet`; `_PDT_BY_KIND['receptacle_device'] = 'POWEROUTLET'` (IfcOutletTypeEnum) applied to `IfcOutlet` too (`_PDT_CLASSES`); `_PSET_BY_KIND['receptacle_device'] = 'DeviceSchedule'` and a device writes its OWN schedule pset verbatim (Voltage / Phases / Wires / Load / MountingHeight / Mounting / DeviceType — Load and MountingHeight are not tagging-contract keys, so the normalised contract alone would drop them); the front-feature nameplate is clamped to the body (`min(0.12, 0.4·w)` × `min(0.022, 0.15·h)`) so a 70 mm device does not carry a 240 mm plate — **no change for any existing kind: a device-free intent emits byte-for-byte what `main`'s emitter does** (checked against `git show origin/main:…ifc_out.py`). |
| both routes agree on the plan (review nit 2) | `prompt_intent.plan_planned_devices(model)` (public) + a one-line hook in `src/rvt/frontdoor/intent.py::intent_from_ifc` (the single seam every `--ifc` entry uses: CLI, router, merge_ifc) | every `receptacle_device` — prompt-built or read back from `IfcOutlet` + DeviceSchedule — gets the SAME `planned` make_device plan (kwargs from the pset: kind, mounting_height_in, voltage, va) and the same disposition text; the prompt route now calls the same helper (one code path). Before: IFC re-entry gave disposition `generated-family` with an `unmapped` plan. |
| round-trip test | `tests/test_frontdoor.py::test_prompted_receptacles_round_trip_through_our_ifc_as_outlets` | 4 `IFCOUTLET(` / 4 `.POWEROUTLET.` / exactly 1 `IFCELECTRICDISTRIBUTIONBOARD(` / 4 `'DeviceSchedule'`; re-entry `equipment_by_kind == {lighting_panelboard: 1, receptacle_device: 4}`, plans `{resolved: 1, planned: 4}`, LP-1 `make_panelboard` resolved, each R-n `IfcOutlet` / upright / z = 44 in / plan kwargs `{duplex-receptacle, 44.0, '120', 180.0}` / refusal names #359; a device-free intent has 0 outlets, 1 board, no DeviceSchedule. |
| matrix doc | `docs/product/PERMUTATION-MATRIX.md` prompt → ifc row | evidence + caveat sentence (status unchanged; `matrix.py` untouched; doc test agrees). |

Reproductions of the reviewer's three cases on the fixed tree: `route.py run --prompt "…lighting panel and 4 duplex receptacles" --output ifc` → status OK, `prompt_intent.ifc` **4 IFCOUTLET / 1 board**; the DONE prompt with `--target-version 2023` → `prompt_room.ifc` **4 outlets / 0 boards**; `author --ifc <that ifc>` → `equipment_by_kind {receptacle_device: 4}`, plans `{planned: 4}` (make_device), walls built, VALID 0 errors, no `make_panelboard` refusal.

Review nit 1 (the planned-device degradation line ends with `build.py`'s *"the facts store never invents
dimensions/ratings …"* tail — the wrong reason for a #359 deferral) is composed in
`src/rvt/frontdoor/build.py:440-444`, outside this territory: noted on #359 (its load/place work removes
the line altogether; until then the plan's own `refusal` text leads with "not a refusal: …").

## Findings

- `survey_host(category=…)` is already parameterised; only its two callers pin the default — #359 is mostly plumbing (`product.doc.category_id`), plus the stage-E specimen/scrub category and `_constructor_for`. Do the category generalisation once for #150 (luminaires) and #359.
- `rvt.famload` (the T2a/L1a-certified mechanism) takes the category from the family document; that is why the device load lane here is `famfrom_ifc.load_into_project`, not `rvt.famgen.loader`.
- Router nit seen while measuring (recorded in #361's Context): `router._families_from_model` reads `f['status']`/`f['refusal']` where `stage_families` writes `reason` → "NOT built -- None" caveats for every unbuilt plan on `route.py run --output rfa --prompt …`.
- The clause-boundary regex treated the '.' in '1.1 m' as a sentence stop; fixed narrowly (`\.(?!\d)`), suites unchanged.

## Open questions

- Face-hosting: real Revit receptacle content is usually wall-/face-hosted; ours is work-plane-based like the panelboard (the certified placement law). Whether an engineer expects hosted behaviour is a PG8 beta question, not assumed here.
- The junction box's "mounting height" has no code meaning; 18 in is an editable placeholder (`assumed`, said in the record's note).

## BRANCH STATE

- Branch `cam/166-electrical-fixtures` from `main` @ fba7efb; PR #358 (`Closes #166`).
- Files written: `src/rvt/famgen/factory.py`, `src/rvt/famgen/facts/generic/devices-and-mounting.json`, `tools/make_family.py`, `src/rvt/frontdoor/prompt_intent.py`, `src/rvt/frontdoor/ifc_out.py` + `src/rvt/frontdoor/intent.py` (review round 1), `src/rvt/ifc/intent.py` (three constants), `plugin/skills/tekton-author/references/CATALOG-FACTS.md`, `docs/product/PERMUTATION-MATRIX.md` (caveat prose only), `tests/test_famgen_factory.py`, `tests/test_prompt_intent.py`, `tests/test_frontdoor.py`, this record; regenerated mirrors `plugin/lib/src/rvt/{famgen/factory.py, famgen/facts/generic/devices-and-mounting.json, frontdoor/prompt_intent.py, frontdoor/ifc_out.py, frontdoor/intent.py, ifc/intent.py}`.
- Not touched: hot files (`tools/frontdoor.py` used only, `SKILL.md`, `versions/`, `base.py`, TRACKER/KNOWLEDGE, ledger), #146's files (`standalone.py`, `mutate.py`, `mep/devices.py`, `tools/ifc_intent.py`, `build.py`, `matrix.py`), `loader.py`, `router.py`, the #349 census.
- Shipped: generation (3 kinds), CLI verbs, unplaced load lane, prompt/intent planning + layout, docs, tests. Staged for viewer: nothing (no "loads in Revit" claim; validator-green only — rule 4). Follow-ups: #359 (load + place in the room build), #361 (famspec kind `device` + the router caveat-key nit).

---

# eng #359 — 2026-08-10: the front door LOADS + PLACES Electrical Fixtures

Stream: `place-fixtures` (eng #359, cloud engineer session `cse_01XE8mZKuQygrgeiaWbzaiK4`, started by the
tech-lead session) · Closes #359 · Refs #166 (this record's first half), #150 (luminaires: the same
category generalisation, done once here), #16 (the open cell the placed instances ride) · PG6 / PG1.

## What was built (the three pins #166 named, pulled — plus the sharing law and the manifest rows)

| Piece | Where | What |
|---|---|---|
| **constructor dispatch** | `tools/ifc_intent.py::_constructor_for`, `_is_device_plan` | `make_device` plans dispatch to `rvt.famgen.factory.make_device`; a device plan is recognised by its constructor, never by kind string. |
| **the sharing law** (DONE: “one family, one type, 4 instances — not 4 families”) | `tools/ifc_intent.py::_family_name_for` (device branch), `family_key(plan)`, `family_groups(model)`, `_load_order`, `_loaded_group`, `n_families` | A board’s family name carries its tag (per-board families, unchanged); a device’s carries **no tag** — `Duplex Receptacle 5-15R 120V 180VA 18in AFF` (kind / model / V / VA / height) — so `family_key = (constructor, family name)` groups every device with the same kwargs into ONE family whose representative is the first tag. `stage_families` writes one `.rfa` per family (a device file is named by its family — `duplex_receptacle_5_15r_120v_180va_18in_aff.rfa` — so two heights never overwrite each other; a shared entry lists its `tags` via `_group_tags`); `_load_order(model, groups)` orders FAMILIES (device tier last, then feeder depth, then tag) — **device families LAST**, even a lone receptacle; stage L (chained `stage_load` and the front door’s batched `stage_load_batched`) loads the representative once and `_loaded_group` records it with its `tags` plus every member tag with `shared_with: <rep>` (the one writer of both fields), so stage E still looks instances up by equipment tag. `n_loaded` / `n_planned` now count families. The intent keeps one resolved `FamilyPlan` per tag (the model’s `plan_for(tag)` contract lives in `rvt.ifc.intent`, NO-GO this wave) — the FAMILY is one. Two mounting heights in one prompt = two device families, never one family silently at one height (test). |
| **loader category from the product** | `src/rvt/famgen/loader.py`: `bind_category(ctx, category)`, `product_category(product)`, `_AuthoredLoad.category`, `survey_host(category=None)` = the category-neutral survey (default still binds Electrical Equipment for every existing caller), `_load_family_into_project` (surveys with the product’s category), `_load_families_into_project` (surveys unbound, then one lazy `bind_category` per distinct family category on the ONE surveyed host — no wasted equipment binding on a devices-only job), `_edit_host_registries` (`register_in_host_adocument(category=a.category)`) | The category-specific half of `survey_host` (projection `GStyleElem`, specimen instance) moved into `bind_category`, which returns a NEW `HostContext` (`dataclasses.replace`; the survey is untouched, an already-bound context is returned as is). `product_category` = the family document’s own `FamilyDoc.category_id` (default Electrical Equipment). Result, read back off the written file: a fixtures family among equipment families carries **−2001060** on its `Family` object, `FamilySymbol` header, `FamilySurrogate`, `FamSymSurrogate` and its `ElementTrackingData` symbol row, the panel beside it −2001040 (test 2b). Equipment-only loads are byte-for-byte the loads they were (same category, same code path; `tests/test_famload_batch.py` chain == batch still holds). |
| **stage E per-instance category + labels** | `tools/ifc_intent.py::stage_equipment`, `_instance_labels(eq, cat)` | header category = `product_category(loaded product)` per instance (was `OST_ELECTRICAL_EQUIPMENT` hard-coded); the instance row records `category`, `shared_with` and — decided by the ONE producer so every harvester just copies — `created_kind` (`fixture-instance` for −2001060, else `equipment-instance`) and, for a device, `schedule` = its `DeviceSchedule` pset; `instances_by_category` (via `rvt.famgen.skeleton.category_label`) on the record; notes reworded (devices stand upright at the wall face exactly like wall panels — the certified free-standing work-plane law; face-hosting stays the fidelity follow-up). Placement itself is the existing machinery unchanged: `rvt.mutate.add_family_instance` on OUR symbol at the intent’s WORLD insertion, `_apply_frame` (upright 3×3), our connector manager (the device’s one 1-pole connector, 0 panel slots), `_scrub_instance`, `m_assocLevelId` = the room’s datum via the level map. |
| **plans resolve** | `src/rvt/frontdoor/prompt_intent.py`: `_plan_device` (no more `planned` downgrade / #359 refusal text), `plan_devices` (was `plan_planned_devices`; disposition `generated-family`), `PLANNED_ONLY_KINDS` removed, grammar comment + unbuilt-reason prose updated; `src/rvt/frontdoor/intent.py::intent_from_ifc` calls `plan_devices` (the `--ifc` re-entry seam) and `combination_check(…, n_families=)` takes the FAMILY count from the caller (the build passes `len(family_groups)` at plan time and `n_families(loaded)` after stage L, so 4 receptacles read “1 loaded family” in both verdicts; other callers keep one-per-plan) | Devices are `buildable_family_plans` material on both routes → the open-cell verdict counts them (`stamp-proof-only`, stamped, delivered). |
| **manifest rows** | `src/rvt/frontdoor/build.py`: `_harvest_created` copies stage E’s `created_kind` (+ `category` / `shared_with` / `schedule` on a `fixture-instance` row), `_harvest_loaded_families` (one `loaded-family` row per FAMILY — member entries skipped, the representative’s `tags` copied), the `family(.rfa)` row carries a shared family’s `tags`, `INSTANCE_KINDS` for the release-context note (the #358 review nit about the `planned` degradation tail dissolves: no plan is `planned` any more, the line is gone); `src/rvt/frontdoor/manifest.py`: `_CATEGORY_FOR['fixture-instance'] = 'electrical_devices'` (coverage-matrix cell), MANIFEST.md “created: … N wiring devices …” + a **device schedule** table (tag / id / device / V / VA / height / at (ft) / level), CRUD `not_available` wording names the shared-family tag law | CRUD affordances (move / retype / delete) apply to every device row unchanged (same ops vocabulary). |
| **docs (honesty prose only)** | `docs/product/PERMUTATION-MATRIX.md` prompt→rvt / prompt→ifc / prompt→rfa rows; `plugin/skills/tekton-author/references/CATALOG-FACTS.md` device row | say what happens now (loaded + placed, stamped, validator-green on three releases, not viewer-certified); `matrix.py` untouched — no cell’s truth changed (`tests/test_router.py -k "matrix or doc"` 11 passed). |
| **tests** | NEW `tests/test_place_fixtures.py` (8, fresh-clone-safe: bundled base + catalog) + drop-in `tests/ci_shard.d/359-place-fixtures.txt`; `tests/test_frontdoor.py` (round-trip test → resolved plans; the #166 e2e rewritten as `test_e2e_prompted_receptacles_are_loaded_and_placed`); `tests/test_prompt_intent.py` (2 device tests → resolved / buildable / devices unfed) | see Evidence. |

Not touched: every NO-GO file of this wave (`src/rvt/ifc/intent.py`, `verify*` / container / framing, `tekton_schema.py`, `rvt_job.py` / `tekton_env.py`, `famgen/skeleton.py` + famdoc), every hot file (`tools/frontdoor.py` used only), `tools/make_family.py` (its `load-device` verb still loads UNPLACED and still says so — true for that verb), `rvt.mep.devices`, `rvt.convert.add_to_project` (see follow-ups), `router.py`.

## Evidence (fresh cloud clone: no `samples/` / `extracted/`; bundled pinned bases; numbers off the written files)

**The DONE prompt — `tools/frontdoor.py author --prompt 'a room with 4 duplex receptacles on a 20A circuit' --out out/x --json`:** exit **0**, 2.7 s, ONE JSON, status `PROOF-ONLY (self-checks PASS …)`, stamps `[OPEN_CELL_STAMP, 'PROOF-ONLY, NOT-DELIVERABLE']`, `errors []`, `degradations []`.
- intent: `equipment_by_kind {receptacle_device: 4}`, `family_plans_by_status {resolved: 4}` (constructor `make_device`, variant `duplex-receptacle-5-15R`, kwargs `{duplex-receptacle, 18.0, '120', 180.0}`), coverage `not_built []`, `ignored_words []` (the “20A” rides as the clause’s rating; no circuit is invented for an unfed device — `feeder_edges []`).
- stage F: ONE `families/duplex_receptacle_5_15r_120v_180va_18in_aff.rfa` “Duplex Receptacle 5-15R 120V 180VA 18in AFF” (named after the family since the `/simplify` pass; family-mode VALID, `rvt_validate --family` 0 errors / 0 warnings, `make_family.py provenance` → `ok: true`).
- stage L: `order ['R-1']`, batched, `host_passes 1`, `n_loaded 1 / n_planned 1`, 7 host elements (Family 1472579, 3 ParamElemFamily, FamilySurrogate, FamilySymbol 1472584, FamSymSurrogate), file_ok, structure layer VALID 0/0.
- stage E: 4 `FamilyInstance` 1472590–93, `instances_by_category {'-2001060': 4}`, new elements clean per seq `{101: 4, 102: 4, 103: 4}`, 0 dangling refs, 0.15 s. **Readback of `prompt_room.rvt`** (`rvt.mutate.Document` on the file’s own schema): each instance header category **−2001060**, `m_assocLevelId` **311** (= the L1 datum the level stage bound), `m_hostId −1`, `m_symbolId == m_masterSymbolId == 1472584` (category −2001060), origin (∓14.667, y, **1.500 ft = 18 in**), 3×3 = `[[0,±1,0],[0,0,1],[±1,0,0]]` (family +Y up, +Z into the room from the W / E face), 1 connector; Family / surrogates `m_categoryId −2001060`; `ElementTrackingData.m_symbols[−2001060] ∋ 1472584`.
- manifest: `combination_verdict {mode stamp-proof-only, n_walls 4, n_loaded_families 1, n_instances 4}`; `elements_created` = 1 `family(.rfa)` + 4 `wall` + 4 **`fixture-instance`** (R-1..R-4: category −2001060, level L1 / 311, `z_above_level_ft 1.5`, upright, symbol 1472584, `shared_with` null/R-1/R-1/R-1, `schedule {Voltage '120 V', Phases 1, Wires 2, Load 180.0, MountingHeight 18.0, Mounting, DeviceType 'NEMA 5-15R Duplex Receptacle'}`) + 1 `loaded-family` (`tags [R-1..R-4]`); CRUD rows for all four (move / retype / delete); coverage-matrix cells `electrical_devices:create, families:create, walls:create`; MANIFEST.md carries “4 wiring devices” + the device-schedule table.
- gates on the file: project validator **VALID, 0 errors / 1 warning** (the known Extensible-Storage decode gap), registries coherent, identity PASS; `tools/rvt_validate.py` 0 errors; status gate vs the pinned base: `provenance_totals {ours-created: 15, ours-composed 2680, autodesk-sample 422 (= the #21 residue)}`, the 15 created = **Family 1, ParamElemFamily 3, FamilySurrogate 1, FamilySymbol 1, FamSymSurrogate 1, SWall 4, FamilyInstance 4**, all `ours-created`, none transitive-cloned.
- **per release**: `--target-version 2025` → exit 0, same rows, `combined VALID 0 errors / 0 warnings`, plus the standing release-independent KNOWN-LIMIT line; `--target-version 2024` → the same. (The famgen loader lane is native on both — nothing device-specific was needed.)
- **bare unzip, system Python** (`tekton-plugin.zip` → `python3 skills/tekton-author/scripts/_bootstrap.py go author --prompt '<DONE prompt>' --out out/j359 --json`): `READY`, `one_call true`, exit 0, 2.96 s, 4 `fixture-instance`, VALID 0 errors.

**Other routes:** `route.py run --prompt '… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF' --output ifc` → `author --ifc <that>`: `{lighting_panelboard: 1, receptacle_device: 4}`, plans `{resolved: 5}`, 1 `equipment-instance` + 4 `fixture-instance` at **3.667 ft = 44 in**, 2 loaded families, VALID 0 errors, no degradation. `route.py run --prompt 'a room with 4 duplex receptacles' --output rfa` → `rfa:R-1` (the one shared device family).

**No regression — `'an electrical room with 6 panels'` on this branch vs `main` @ e5b7864 (same machine, back to back):** identical status / stamps / errors / mode (`stamp-proof-only`) / `n_instances 6` / `n_loaded_families 6` / created kinds `{equipment-instance 6, family(.rfa) 6, loaded-family 6, wall 4}` / degradations `[]` / validation `VALID 0/1` / load `(6, 6, 1 pass, order PP-1..PP-6)` / file size 684,032 B; a structural readback of both files (every created Family / FamilySymbol / surrogate / SWall / ParamElem / FamilyInstance id + category, each instance’s level / symbol / 3×3 / connector count) is **identical** (the bytes differ run-to-run on `main` itself — per-run GUIDs, #9). **Mixed prompt** (`… 150 kVA transformer, a 225 A 208Y/120 receptacle panel, two lighting panels and 6 receptacles at 1.1 m …`): load order `[LP-1, LP-2, T1, RP-1, R-1]` (devices last), 5/5 families in 1 host pass, 4 `equipment-instance` + 6 `fixture-instance` (z 3.609 ft = 1.1 m; boards at 4.659), circuit `T1>RP-1` wired and read back (`ok`, 1/1), VALID 0 errors, no degradation. **Device failure costs devices only** (test: `make_device` monkeypatched to raise): order `[LP-1, R-1]`, LP-1 loaded + placed, blocker `R-1: family 2/2: FactoryError …`, no `fixture-instance`, one “family LOAD blocked at R-1” degradation, VALID 0 errors, delivered. **`--strict`**: shell (4 walls, 0 instances) + equipment (4 device instances), both VALID 0 errors.

**Gates (after the `/simplify` pass — 4 review angles; applied: producer-side `created_kind` / `schedule` / `tags` in stage E / `_loaded_group` instead of harvest-time re-derivation, `combination_check(n_families=)` instead of type-sniffing the loaded map, `_load_order(model, groups)`, lazy category binding, `Counter` for `instances_by_category`, the dead house-switchboard key branch and `planned` guard dropped, and the reviewer-found `.rfa` path collision for two device heights fixed by naming device files after the family; skipped as churn: a constructor registry replacing the four `endswith` chains, re-keying `loaded` by family with a placements map):** `tests/test_place_fixtures.py` **8 passed** (13 s); `tests/test_prompt_intent.py` 22 passed; `tests/test_frontdoor.py tests/test_famload_batch.py tests/test_hostsym_product.py tests/test_famgen_loader.py` 109 passed / 18 skipped (rme-sample cases); `tests/test_famload_2025.py test_target2025.py test_router.py test_convert_combo.py test_frontdoor_standalone.py test_ifc_intent.py test_mep_devices.py test_famgen_factory.py test_router_release.py test_frontdoor_wallsolid.py` 238 passed / 53 skipped (sample-gated); plugin: `tools/sync_plugin.py` → 7 mirrors regenerated, `--check` clean (deny-audit clean, identity scan == allowlist), `plugin/scripts/validate_plugin.py` PASS (25 assertions), `tests/test_plugin_sync.py test_bootstrap.py test_coldstart.py test_surface_perf.py` 28 passed / 5 skipped; `tools/dev/check_portable_paths.py` ok (2863 paths). Post-cleanup re-runs: the 13 neighbouring suites 347 passed / 58 skipped; the 6-panels job vs `main` again identical (status / stamps / verdict incl. reason text / every created row minus ids / load rows’ key sets / file size / structural readback). All with `RVT_SKIP_LARGE=1`; full suite not run (SUITE-COORDINATION).

## Findings

- The category generalisation #150 (luminaires) needs is done: `bind_category` / `product_category` + stage E’s per-instance category are category-agnostic; a `make_luminaire` plan needs only its `_constructor_for` line, a `family_key`-appropriate name and a placement law (ceiling plane, not the wall face).
- The composed base G_ABPD DOES carry the Electrical Fixtures projection `GStyleElem` (**125**; the load report’s `resolved {category −2001060, category_gstyle 125, core_ids [125]}`), so the device symbol’s geometry binds to its category style exactly like the equipment families do to theirs; the ONLY loader note for the category is `no host FamilyInstance of category −2001060: placement scaffolding unavailable (family loads unplaced)` — expected (the family-free base has no instance of any category; stage E supplies the constructed specimen). (An earlier draft of this line said the GStyle row was missing — wrong, corrected in review.)
- “20A circuit” in the DONE prompt is read as the receptacle clause’s rating (`BusRating 20` in the contract via the resolver’s name-text rule); no branch circuit is authored for unfed devices, and none is claimed. A prompted *branch* circuit (“4 receptacles on circuit LP-1/3”) would be `rvt.mep.devices.add_multi_load_circuit` territory — a separate story, not implied by this DONE.

## Follow-ups (searched first; filed task-shaped with `Refs #359`)

- Move `_plan_device` / `_resolve_device_facts` into `rvt.ifc.intent.plan_family_for` and drop the `plan_devices` seam in `frontdoor.intent.intent_from_ifc` — blocked this wave only because `src/rvt/ifc/intent.py` is held by eng #348.
- `rvt.convert.add_to_project` (the `prompt+rvt` / `ifc+rvt` / `spec+rvt` routes, outside this territory) harvests stage E rows as `equipment-instance` and one `loaded-family` per `loaded` tag: prompted receptacles added INTO a user’s project now load + place there too (devices are resolved plans), but its record labels them `equipment-instance` ×N + N duplicate `loaded-family` rows — the fix is two copies: `kind = i.get('created_kind')` and skip `info.get('shared_with')` entries, exactly as `build._harvest_*` do.
- Face-hosting of placed devices on the created walls (`rvt.mep.devices` dummy-plane law) — the fidelity follow-up already named in stage E’s notes; PG8 decides whether engineers expect it.

## Review round 1 (tech-lead review of head a475fb1, 🟡): three fixes, in the squash

| Finding | Fix |
|---|---|
| `product_category` accepted ANY negative int, so an extracted family whose category could not be read (`convert.extract_family.RfaFamilyDoc.category_id == -1`) short-circuited `bind_category` and would load UNBOUND instead of defaulting to −2001040 as before | `BUILTIN_CATEGORY_CEILING = -2000000`: only a real BuiltInCategory (an int ≤ −2000000) is taken from the product; `-1` / None / bool / str / positive ids → the default equipment binding (the pre-#359 behaviour). Truthful widening now recorded: `extract_family.reload_family` (extract → `load_family_into_project(product=…)`) stamps a NON-equipment extracted family (e.g. a lighting fixture, −2001120) with its OWN category on surrogates / symbol header / tracking row instead of −2001040; an unreadable category still binds equipment. Test: `test_product_category_defaults_an_unknown_category_to_the_equipment_binding` (stub products: −1 / None / 0 / 125 / "−2001060" / True / no doc → −2001040; −2001060 / −2001120 / −2000011 → themselves). |
| the prompt→rvt matrix row cited `tests/test_place_fixtures.py` for 2025/2024 but the file pinned 2026 only | `test_done_prompt_places_the_devices_on_the_older_certified_bases_too[2025|2024]`: the DONE prompt with `target_version` → 1 family / 4 `fixture-instance` (−2001060) / 1 loaded family, `combined VALID 0 errors`, `rvt.versions.detect_release(output) == release`, manifest `target_version {requested, output_release, status 'match'}`, open-cell stamp present; the row now says the test pins all three. |
| record nits | the GStyle finding corrected (G_ABPD carries GStyleElem 125 for −2001060; only the specimen-instance note fires); the stage-F evidence line names the post-`/simplify` file. |

Gates re-run on the fix head: `tests/test_place_fixtures.py` **11 passed** (17.7 s); loader neighbours + plugin sync/validate re-run (see PR).

## BRANCH STATE (eng #359)

- Branch `cam/359-place-fixtures` from `main` @ e5b7864; PR **#400** (`Closes #359`); follow-ups filed #397 / #398 / #399.
- Files written: `tools/ifc_intent.py`, `src/rvt/famgen/loader.py`, `src/rvt/frontdoor/build.py`, `src/rvt/frontdoor/intent.py`, `src/rvt/frontdoor/manifest.py`, `src/rvt/frontdoor/prompt_intent.py`, `tests/test_place_fixtures.py` (new), `tests/ci_shard.d/359-place-fixtures.txt` (new), `tests/test_frontdoor.py`, `tests/test_prompt_intent.py`, `docs/product/PERMUTATION-MATRIX.md` + `plugin/skills/tekton-author/references/CATALOG-FACTS.md` (prose), this record (own header) + a pointer in `docs/inbox/mep-devices.md` (own header); regenerated mirrors `plugin/lib/src/rvt/{famgen/loader.py, frontdoor/build.py, frontdoor/intent.py, frontdoor/manifest.py, frontdoor/prompt_intent.py}`, `plugin/lib/tools/ifc_intent.py`, `plugin/skills/tekton-author/scripts/ifc_intent.py`.
- Shipped: load + place of Electrical Fixtures on all three input routes and all three certified releases, validator-green, stamped, manifest rows + schedule. Staged for viewer: **nothing** — placed instances of our families on the composed base are the open cell (#16) regardless of category; no batch number was requested; no “loads in Revit” claim anywhere (rule 4).

# eng #397 — 2026-08-10: the resolver maps `receptacle_device` itself; the front door's device seam is deleted

Refs #397 (follow-up of #359 / #166). Branch `cam/397-intent-device-branch` from `main` @ b253668.

## Why

#166 / #359 planned wiring devices in `rvt.frontdoor.prompt_intent` (`_plan_device` + `_resolve_device_facts` + `plan_devices`) and reached the `--ifc` route only through a seam (`frontdoor.intent.intent_from_ifc` → `plan_devices(resolve_intent(path))`), because `src/rvt/ifc/intent.py` was held by another engineer both times. Anything calling `rvt.ifc.intent.resolve_intent` directly — `tools/ifc_intent.py intent|families|room`, research probes — still saw `IfcOutlet` products as `unmapped` (measured on `main` @ b253668: `map R-1 -> unmapped - no generated-content constructor for this kind; recorded`).

## What was built

- `src/rvt/ifc/intent.py`: `plan_family_for(eq)` gains the `receptacle_device` branch — constructor `rvt.famgen.factory.make_device`, kwargs `{kind, mounting_height_in, voltage, va}` from the equipment’s own `DEVICE_PSET` (`DeviceSchedule`: `MountingHeight`, `Voltage` through `parse_voltage` → L-L, `Load`; a silent schedule books the default row 120 V / 180 VA duplex receptacle), `dims_modeled_m` like every branch — then `_resolve_device_facts(fp)`, a verbatim twin of `_resolve_panel_facts` (`F.resolve_device_facts(kind, mounting_height_in, voltage, va)`; a refusal → `status refused`, `refusal "<Exc>: <msg>"`; resolved → `catalog generic/devices-and-mounting`, `variant`, `facts_summary {subject, model, manufacturer, mounting_height_in, voltage_v, load_va, assumed_fields, unverified_fields}`, `dims_catalog_m` = faceplate w × h, box depth + plate thickness). New module constants `DEVICE_PSET`, `DEFAULT_DEVICE_KIND/VOLTAGE/VA` (one source); `plan_family_for` + `DEVICE_PSET` exported in `__all__`.
- `src/rvt/frontdoor/prompt_intent.py`: `_plan_device`, `plan_devices`, `_resolve_device_facts` **deleted** (−62 lines); the prompt route’s device plans now come from `I.plan_families(equipment)` like every other kind (the trailing `plan_devices(model)` call is gone); `DEVICE_PSET` / `DEFAULT_DEVICE_*` kept as names but bound to the resolver’s constants; `plan_devices` dropped from `__all__`. `_device_facts` (memoised, layout-time height default + envelope dims) stays — it is the layout’s reader, not the mapping.
- `src/rvt/frontdoor/intent.py`: `intent_from_ifc(path)` is `I.resolve_intent(path)` again (pure delegation).
- `tools/ifc_intent.py`: `_instance_labels` reads `I.DEVICE_PSET` — the room builder no longer imports the front door’s prompt layer at all (`grep prompt_intent tools/ifc_intent.py` → nothing). Stage behaviour identical.
- Tests: new `tests/test_intent_device_plan.py` (7, fresh-clone safe, drop-in `tests/ci_shard.d/397-intent-device-plan.txt`): `plan_family_for` on hand-built `IfcOutlet` equipment (schedule kwargs, catalog/variant, facts, the literal 2.75 × 4.5 in faceplate / 2.5 in box + 0.25 in plate envelope, buildable), silent schedule / `277 V` / `208Y/120` + `Load 360` parsing, the refusal path (monkeypatched resolver → `refused` with the resolver’s words, not raised, not buildable), the seam-is-gone guard (`prompt_intent` exposes no `plan_devices` / `_plan_device` / `_resolve_device_facts`; its `DEVICE_PSET` / `DEFAULT_DEVICE_*` names ARE the resolver’s objects), a fresh-interpreter layering guard (`tools/ifc_intent.py::_instance_labels` labels a fixture with its schedule row and `rvt.frontdoor.prompt_intent` is NOT in `sys.modules` afterwards — it was the importer on `main`), prompt-route parity (`model.family_plans == plan_families(model.equipment)` as JSON), and one shared module fixture (OUR IFC of a lighting panel + 4 receptacles at 44 in) driving the bare `resolve_intent` (`IfcOutlet` ×4 → `resolved make_device`, kwargs `{duplex-receptacle, 44.0, '120', 180.0}`) == `intent_from_ifc` plan for plan, plus the `tools/ifc_intent.py intent` CLI printing `map R-1..R-4 -> resolved make_device`. `tests/test_place_fixtures.py` untouched.

## Evidence (cloud clone, no `samples/`; bundled pinned bases; same machine, `main` @ b253668 vs this branch back to back — `scratchpad/run_evidence.sh` + `compare.py`, manifests normalised only for out-dir paths / timings)

| Job | Rows / kinds (both trees) | Verdict | manifest.json | stdout `--json` | intent.json |
|---|---|---|---|---|---|
| `author --prompt 'a room with 4 duplex receptacles on a 20A circuit'` (2026) | `family(.rfa) 1, wall 4, fixture-instance 4 (−2001060), loaded-family 1`; plans `{resolved 4}` | `stamp-proof-only`, n_instances 4, open-cell stamp, `combined VALID 0 errors / 1 warning` | identical except the per-run family `content_guid` (differs between two runs of the SAME tree too — #9) and stage seconds | **identical** | equipment values identical; +1 `notes` line per device (below) |
| same, `--target-version 2025` | same rows; `VALID 0/0`; release-independent KNOWN-LIMIT line | same | same class of diff only | **identical** | same |
| mixed prompt (`… 150 kVA transformer, a 225 A 208Y/120 receptacle panel, two lighting panels and 6 receptacles at 1.1 m …`) | `family 5, wall 4, equipment-instance 4, fixture-instance 6, circuit 1, loaded-family 5` | `stamp-proof-only`, n_instances 10, VALID 0/1 | guids + seconds only | **identical** | same |
| `route.py run --output ifc --prompt '… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF'` → `author --ifc <that>` | emitted `.ifc` **byte-identical** (168 lines); re-entry `family 2, wall 4, equipment-instance 1, fixture-instance 4, loaded-family 2`, plans `{resolved 5}` | `stamp-proof-only`, n_instances 5, VALID 0/1 | guids + seconds only | **identical** | **byte-identical** (2383 lines) — the resolver’s own branch reproduces the seam’s plan field for field, `facts` included |
| `tools/ifc_intent.py intent <that .ifc>` | — | — | — | `map R-1..R-4 -> resolved make_device duplex-receptacle-5-15R` (was `unmapped`) | R-1..R-4 plans: `unmapped {}` → `resolved make_device`, kwargs `{duplex-receptacle, 44.0, '120', 180.0}`, `catalog_m {w .0698, h .1143, d .0698}`, `delta_m 0/0/0` |

The one observable delta on the prompt route, stated rather than hidden: because a device plan is now `resolved` at `plan_families` time (not bolted on afterwards), `prompt_to_intent`’s existing “catalog facts REPLACE the prompt-default dims wherever a plan resolved” loop now visits devices exactly as it visits the transformer — the values it writes are the same floats the layout already had (the layout reads the same fact sheet; `delta_m` 0/0/0), and each device’s `Equipment.notes` in `intent.json` gains the line `dims replaced by catalog facts (generic/devices-and-mounting duplex-receptacle-5-15R)`. Manifest rows, kinds, census, verdict, validation, `MANIFEST.md` and the stdout result are unaffected. One deliberate tightening inside the twin: the fact sheet is resolved with the plan’s own `va` (the schedule’s `Load`) instead of always 180 — identical for every prompt job (the prompt books 180 VA) and for our IFC round trip; an IFC whose DeviceSchedule books another load now reports it in `facts.load_va` (test: `Load 360` → `load_va 360.0`).

**`/simplify` (4 angles — reuse / simplification / efficiency / altitude).** Applied: `_resolve_device_facts` calls `F.resolve_device_facts(**fp.kwargs)` (make_device’s kwargs are the resolver’s by construction); `prompt_intent._device_facts` is nullary again (`lru_cache(maxsize=1)` — its only parametrised caller was the deleted twin; the two surviving callers read the default row); the test file lost its source-text greps (brittle) for the fresh-interpreter layering guard, asserts the envelope against literal inches instead of re-deriving the implementation’s formula, and folds the CLI check onto the round-trip fixture. Measured, not changed: the un-memoised per-device facts call costs ~140 µs (the catalog file text is `lru_cache`d in `famgen.catalog`; 20 devices ≈ 2.8 ms of a 12 ms `prompt_to_intent`) — same shape as the per-board resolvers, no device-specific memo re-added; import time unchanged (constants only). Skipped, with the reason: a shared `device_envelope_m(sheet)` helper for the 3-line envelope formula that also lives in `prompt_intent._default_dims` (that copy is the pre-plan layout placeholder which the plan’s `dims_catalog_m` overwrites for every resolved device, so the two can never disagree in an output; unifying would only reorder `intent.json` keys); `frontdoor/ifc_out._PSET_BY_KIND` reading `I.DEVICE_PSET` (outside this territory, and its sibling schedule names are literals too); joining the device on `normalize_contract` aliases instead of its named pset (a join-key redesign — the issue specifies kwargs from the DeviceSchedule pset, and `ifc_out` round-trips that pset verbatim); dropping the `prompt_intent` constant aliases (the issue keeps the names there).

**Gates (post-`/simplify`, evidence table re-run on the final tree — same result classes):** `tests/test_intent_device_plan.py` **7 passed** (0.85 s; also green with `RVT_STEPLITE_FORCE=1`); `tests/test_place_fixtures.py tests/test_prompt_intent.py tests/test_intent_device_plan.py` **40 passed** under `RVT_STEPLITE_FORCE=1`; `tests/test_ifc_intent.py tests/test_ifc_intent_units.py tests/test_frontdoor.py` **106 passed / 5 skipped** (real ifcopenshell present in this VM); pre-simplify also `tests/test_frontdoor.py tests/test_router.py tests/test_frontdoor_standalone.py tests/test_ifc_census.py tests/test_convert_combo.py` **204 passed / 27 skipped** (sample-gated); plugin: `tools/sync_plugin.py` → 5 mirrors regenerated, `--check` clean (deny-audit clean, identity scan == allowlist), `plugin/scripts/validate_plugin.py` PASS (25 assertions), `tests/test_plugin_sync.py test_bootstrap.py test_coldstart.py test_surface_perf.py` 28 passed / 5 skipped; `tools/dev/check_portable_paths.py` ok; `tools/dev/shard_list.py --print` lists the new file. All with `RVT_SKIP_LARGE=1`; full suite not run (SUITE-COORDINATION).

## Findings / follow-ups

- None new. #398 (`add_to_project` row labels) and #399 (face-hosting) stand as filed by eng #359; this change does not touch either.

## BRANCH STATE (eng #397)

- Branch `cam/397-intent-device-branch` from `main` @ b253668 (rebased on d2da397); PR **#437** (`Closes #397`).
- Files written: `src/rvt/ifc/intent.py`, `src/rvt/frontdoor/prompt_intent.py`, `src/rvt/frontdoor/intent.py`, `tools/ifc_intent.py`, `tests/test_intent_device_plan.py` (new), `tests/ci_shard.d/397-intent-device-plan.txt` (new), this record (own header); regenerated mirrors `plugin/lib/src/rvt/{ifc/intent.py, frontdoor/prompt_intent.py, frontdoor/intent.py}`, `plugin/lib/tools/ifc_intent.py`, `plugin/skills/tekton-author/scripts/ifc_intent.py`.
- Not touched (held elsewhere): `docs/product/PERMUTATION-MATRIX.md`, `src/rvt/frontdoor/manifest.py`, `src/rvt/ifc/_fallback.py`, famgen factory bodies.
- Shipped: one device mapping owned by the intent layer; both routes and the bare resolver/CLI yield the same resolved plan. Staged for viewer: nothing (no writer bytes changed; placed instances remain the open cell #16).

# eng #438 — 2026-08-10: a hand-typed DeviceSchedule `Load` degrades one device, never the `--ifc` route; a 1-pole device books L-N on both routes

Refs #438 (independent-review follow-up of #437 / #397; #400). Branch `cam/438-device-load-coerce` from `main` @ f05db8b.

## Why

`plan_family_for`'s `receptacle_device` branch did `va=float(dev.get("Load") or DEFAULT_DEVICE_VA)` **outside** the recorded-refusal `try`, so a DeviceSchedule pset a person hand-authors from `PROMPT_TO_IFC.md` — `IFCPROPERTYSINGLEVALUE('Load',$,IFCLABEL('180 VA'),$)` — raised `ValueError: could not convert string to float: '180 VA'` out of `plan_families`; `author --ifc` ended `FAILED (IFC intent failed: ValueError …)`, **rc 3, no `.rvt`** (reproduced on `main` @ f05db8b, below). Rule 1: one bad schedule cell must degrade one device, not withhold the file. Second: the same branch booked a wye system voltage's **line-to-line** for a 1-pole receptacle (`'208Y/120'` → `'208'`, enshrined by `tests/test_intent_device_plan.py`), while the prompt route's `_device_voltage` booked line-to-neutral (`120`) — two rules for one connector.

## What was built

- `src/rvt/ifc/intent.py` (device branch + two helpers beside `_resolve_device_facts`; nothing else):
  - `parse_device_load(load) -> (va, note)`: a real measure rides as is; a numeric **text label** with the unit a person types (`'180 VA'`, `'180VA'`, `'0.18 kVA'`, `'1.5e2 va'`; regex `_RE_LOAD_VA`, `k` prefix ×1000) coerces to VA with ONE note (`DeviceSchedule.Load '180 VA' is a text label, not a measure -> read as 180 VA`); absent / blank / zero → the NEC 220.14(I) unit load silently (as before); unparseable (`'abc'`, `'180 W'`, `'nan'`, a bool / list), negative, non-finite, or above `MAX_DEVICE_VA = 100 000` VA (a 100 A 480 V 3-pole pin-and-sleeve receptacle is ~83 kVA; `1e12` is a typo) → `DEFAULT_DEVICE_VA` with ONE note naming the property, the value and the reason (`DeviceSchedule.Load 'abc' is not a usable apparent load (not a number) -> this device books the default 180 VA (NEC 220.14(I) receptacle unit load) instead; fix the schedule cell to book another load`). Never raises. The plan stays `resolved` (degraded, not refused: the device is generated, loaded and placed at the default load), the note rides in `FamilyPlan.notes` → `intent.json familyMapping[].notes`.
  - `device_voltage(voltage, poles=None) -> str`: ONE rule for the connector voltage — a 1-pole device on a wye system takes the **line-to-neutral** leg (`'208Y/120'` → `'120'`, `'480Y/277'` → `'277'`); only a schedule that says multi-pole (`Poles` or `Phases` ≥ 2; unreadable → 1-pole) takes the line-to-line (`'208'`); a plain figure as is (`'277 V'` → `'277'`); nothing parseable → the `'120'` default row. The device branch calls it with `poles=dev.get("Poles") or dev.get("Phases")`; our own prompt route writes `Phases 1, Wires 2`, so every prompted / round-tripped device is unchanged (`'120 V'` → `'120'`).
  - both exported in `__all__`.
- `src/rvt/frontdoor/prompt_intent.py`: `_device_voltage(item)` is now `I.device_voltage(item.voltage)` — same figures it always produced (it already booked L-N), one implementation.
- `tests/test_intent_device_plan.py` 7 → **11**: the wye case updated **deliberately** (comment cites #438: `'208Y/120'` → `'120'`, `voltage_v 120.0`; `Poles 2` / `Phases '2'` → `'208'`; `Phases 1, Wires 2` → `'120'`); new: prompt-route/plan agreement (`PP._device_voltage(PromptItem(voltage=s)) == I.device_voltage(s)` for `208Y/120`, `480Y/277 V`, `277 V`, `120`, `None`, junk); `parse_device_load` table (`'180 VA'`/`'180VA'`/`'180'` → 180.0, `'0.18 kVA'` → 180.0, `'1.5kva'` → 1500.0, `' 360 va '` → 360.0 each with the text-label note; `180`/`180.0`/`360.5` silently; `None`/`''`/`0`/`'0 VA'` → default silently; `'abc'`/`'180 W'`/`'nan'`/`True`/`[180]` → not a number, `-5`/`'-5 VA'` → negative, `nan`/`inf` → not finite, `1e12`/`'1e12'`/`'101 kVA'` → sanity cap — each default + exactly one note; the cap itself is a load); `plan_family_for` never raises on any of those and records exactly one note, `facts.load_va` follows (`'0.36 kVA'` → 360.0), `plan_families` over a mixed list plans every item; and the **route** test on a hand-edited copy of the module's OUR-IFC fixture (first `Load` cell → `IFCLABEL('180 VA')`, second → `IFCLABEL('abc')`): bare `intent_from_ifc` → 4 resolved device plans, `va` 180 ×4, exactly the two notes; `FD.author(ifc=…)` → `route ifc`, `ok`, `errors []`, `PROOF-ONLY`, combined `.rvt` on disk, `build.errors [] / degradations []`, `validation.combined n_errors 0`, 4 `fixture-instance` rows, plans `{resolved 5}`, and the two notes present in the written `intent.json`. Fresh-clone safe (bundled steplite + pinned base; skips if the base is absent); already in the shard via `tests/ci_shard.d/397-intent-device-plan.txt` (no new drop-in needed).

## Evidence (cloud clone, no `samples/`, bundled pinned bases; `main` @ f05db8b in a `git worktree` vs this branch, same venv, back to back)

| Job | `main` @ f05db8b | this branch |
|---|---|---|
| `author --ifc hand.ifc` — OUR IFC of `… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF` with R-1's `Load` retyped `IFCLABEL('180 VA')` (`RVT_STEPLITE_FORCE=1`) | **rc 3**, `FAILED (IFC intent failed: ValueError: could not convert string to float: '180 VA')`, `files {}` — no `.rvt`, no intent.json | **rc 0**, `PROOF-ONLY (self-checks PASS …)`, `errors []`, `rt_bad.rvt` 622 592 bytes delivered, `rvt_validate` **VALID, 0 errors / 1 warning** (the known DataStorage decoder-gap warning every build carries), `degradations []`, plans LP-1 + R-1..R-4 all `resolved`, R-1..R-4 kwargs `{duplex-receptacle, 44.0, '120', 180.0}`, R-1 carries the ONE note `DeviceSchedule.Load '180 VA' is a text label, not a measure -> read as 180 VA`, R-2..R-4 none |
| `author --prompt '… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF'` (2026) | rows `family(.rfa) 2, wall 4, equipment-instance 1, fixture-instance 4, loaded-family 2`; plans `{resolved 5}`; VALID 0 | **identical**: manifest.json equal key for key except `generated_at`, stage `seconds`, the combined file's sha and the per-run family `content_guid`s (which differ between two runs of the SAME tree too — #9); `intent`, `crud`, `coverage_matrix`, `honesty`, `status`, `prompt_coverage` sections equal; `intent.json` **equal** (whole document); `scene-brief.json` **byte-identical**; `MANIFEST.md` differs only in the timestamp, the base's absolute path and the combined sha |
| IFC round trip: `write_intent_ifc(prompt_to_intent(that prompt))` → `intent_from_ifc` | 168-line `.ifc` | emitted `.ifc` **identical** (modulo FILE_NAME / owner-history stamps); re-entry `summarize()` + every `FamilyPlan.as_json()` **identical** (only the `source` path differs) — every round-tripped device still books `'120'` / 180.0 because our emitter writes `Voltage '120 V'`, `Phases 1`, `Load IFCREAL(180.)` |

## Findings / follow-ups

- **Filed #442** (`Refs #438`, P2 ready area:frontdoor, searched first — nothing open covered it): the sibling branches of `plan_family_for` have the same bare casts outside their recorded-refusal `try` (`float(MainsRating)` / `float(RatingkVA)` / `int(NumberOfCircuits)` / the switchboard's `int(Phases)`…), so `PanelSchedule.MainsRating IFCLABEL('400 A')` still fails the route exactly as `Load` did; `MountingHeight IFCLABEL('44 in')` reaches `resolve_device_facts(float(…))` *inside* the try and marks that device `refused` (not built, listed under degradations, file delivered — rule-1-compliant but coarser than a coerce); and `plan_families` has no per-item backstop. #442's DONE = one shared `parse_schedule_scalar` that `parse_device_load` becomes a wrapper of + a per-item recorded-refusal guard in `plan_families`. Deliberately not widened here: the issue's territory is the device branch, and the generalisation touches every branch.
- A schedule that says `Poles 2` now books `'208'` on the plan, but `make_device` still builds ONE 1-pole connector (factory bodies are outside this territory and held by the desktop-Revit campaign) — the voltage figure is right, the pole count on the connector is not yet schedule-driven. No prompted device can reach that case (the grammar only makes 1-pole duplex receptacles).

**`/simplify` (4 angles).** Applied: `parse_device_load`'s exit logic flattened (the two good returns first, then one conditional `why` feeding the single degrade return — same values and note strings); the load note is passed as `FamilyPlan(notes=[…])` instead of build-then-append; `device_voltage`'s docstring no longer names the private prompt-route helper by path (a reverse cross-reference that rots); the plan-level bad-load loop trimmed to the issue's four values and asserts "one note naming the value" instead of re-pinning the wording the parser table already pins. Reuse: nothing in the tree already parsed `'180 VA'`-style cells or picked L-N vs L-L for a device (`_si_factor` works on IfcNamedUnit entities, `prompt_intent._RE_KVA` is a prose `search` that needs the `k`, `factory._number`/`_voltage_number` raise and are off-limits, `convert.modify_family._convert_value` converts to internal units and defaults unitless to kVA) — the diff removes the one prior duplicate (`_device_voltage` now delegates). Efficiency: one more module-level `re.compile` beside the file's existing eight (µs at import), one anchored match per device; the route test reuses the module-scoped `our_ifc` fixture and adds one ~3 s build to the shard. Skipped, with the reason: inlining `I.device_voltage(item.voltage)` at `_apply_defaults` and deleting `_device_voltage` (the call site is outside "`_device_voltage` only"; the shim is one line); pre-shaping `parse_device_load`'s signature for #442's generalisation (speculative until that issue lands).

**`/verify` (front-door CLI, final tree, `RVT_STEPLITE_FORCE=1`, out/verify/):** `author --ifc rt_bad.ifc` (R-1 `Load IFCLABEL('180 VA')`) → rc 0, PROOF-ONLY, `errors []`, `degradations []`, 4 fixture-instances, 2 loaded families, `rvt_validate` VALID 0 errors / 1 warning, R-1 note `… '180 VA' is a text label … read as 180 VA`; `author --ifc rt_nasty.ifc` (`'abc'`, `IFCREAL(-5.)`, `'1e12 VA'`, `180.`) → rc 0, PROOF-ONLY, VALID 0, 4 fixture-instances, three notes (`not a number` / `negative` / `above the 100000 VA sanity cap`), all four devices at 180 VA; `author --ifc rt_wye.ifc` (R-1 `Voltage IFCLABEL('208Y/120')` + `Load IFCLABEL('0.36 kVA')`) → rc 0, VALID 0, R-1 plan `{voltage '120', va 360.0}`, `facts voltage_v 120.0 / load_va 360.0`, its own family (3 loaded), note `… '0.36 kVA' is a text label … read as 360 VA`, R-2..R-4 unchanged.

## Gates

- `RVT_SKIP_LARGE=1 .venv/bin/python -m pytest tests/test_intent_device_plan.py -q -rs` → **11 passed** (3.1 s; also 11 passed under `RVT_STEPLITE_FORCE=1`).
- neighbours: `tests/test_frontdoor.py -k 'receptacle or device or voltage or round_trip'` 4 passed; `tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_place_fixtures.py` 28 passed.
- `tools/sync_plugin.py` → mirrors `plugin/lib/src/rvt/{ifc/intent.py, frontdoor/prompt_intent.py}` regenerated, zip rebuilt; `--check` clean (deny-audit clean, identity scan == allowlist); `plugin/scripts/validate_plugin.py` PASS (25 assertions); `tools/dev/check_portable_paths.py` ok (2897 paths).
- bare surface: `tests/test_coldstart.py tests/test_surface_perf.py` 11 passed / 5 skipped (no bare python3-with-numpy on this host — the standing skip); `tools/provenance.py out/verify/bad/rt_bad.rvt --baseline all --streams` prints the same ledger as `main`'s build of the same job (author `rvt-writer`; the G1/G2 lines are the pinned base's standing #19 identity items, unchanged by this diff).
- whole merged CI shard on the final tree (`RVT_SKIP_LARGE=1 .venv/bin/python -m pytest -q -p no:cacheprovider $(python3 tools/dev/shard_list.py --print)`): **1498 passed, 134 skipped, 3 xfailed** in 292.7 s.
- Full suite not run (SUITE-COORDINATION).

## Review round 1 (tech lead, 🟡 on 5b7cb67) — fixed on the same branch

- **Finding (new to this PR):** `device_voltage` caught `(TypeError, ValueError)` only, so a hand-typed pole count `Phases IFCREAL(1.E400)` (inf → `int(float(inf))`) or a >308-digit `IFCINTEGER` raised `OverflowError` out of `plan_families` → route FAILED. **Fix:** the pole count is read as `float` with an `isfinite` guard and `OverflowError` in the except (non-finite / unreadable = 1-pole); a string pole count now takes its leading digits so `'2P'` / `'2-pole'` / `' 3 ph'` honour the "≥ 2 → L-L" rule (`'P2'` / `'n/a'` stay 1-pole). `parse_device_load` gets the same one-word guard (`float(load)` on a >308-digit int → treated as inf → the "not finite" degrade note, pre-existing on `main` too), and the value echoed in a note is clipped to 60 chars so a 400-digit cell does not become a 400-char note.
- Tests 11 → **35** (one parametrised `device_voltage` table of 24 pole spellings incl. `inf` / `-inf` / `nan` / `10**400` / `'1e400'` / `'2P'` / `'2-pole'` / `True` / `[2]`; the plan-level `Phases inf + Load 10**400` case resolves at `'120'` / 180 VA with one note; the clipped-repr assertion).
- Re-driven (`RVT_STEPLITE_FORCE=1`): `rt_inf.ifc` (R-1 `Phases IFCREAL(1.E400)` + `Load IFCINTEGER(10**400)`) → on 5b7cb67 rc 3 `FAILED (… OverflowError: int too large to convert to float)`; now **rc 0**, PROOF-ONLY, VALID 0, R-1 `{voltage '120', va 180.0}` + one clipped `not finite` note; `rt_bad` / `rt_nasty` / `rt_wye` unchanged (rc 0, `errors []`).
- Gates on the fix tree: `tests/test_intent_device_plan.py` 35 passed (also under `RVT_STEPLITE_FORCE=1`); `sync_plugin.py` run + `--check` clean; `validate_plugin.py` PASS; portable paths ok; whole merged CI shard **1522 passed, 134 skipped, 3 xfailed** in 290.0 s.

## BRANCH STATE (eng #438)

- Branch `cam/438-device-load-coerce` from `main` @ f05db8b; PR (`Closes #438`).
- Files written: `src/rvt/ifc/intent.py`, `src/rvt/frontdoor/prompt_intent.py` (`_device_voltage` only), `tests/test_intent_device_plan.py`, this record (own header); regenerated mirrors `plugin/lib/src/rvt/ifc/intent.py`, `plugin/lib/src/rvt/frontdoor/prompt_intent.py`.
- Not touched: `tools/rvt_job.py`, `release_ctx.py`, `validate.py` / `objects.py`, `manifest.py` / `frontdoor/__init__.py`, famgen skeleton / famdoc / geometry / factory bodies, every hot file, `tests/ci_shard.txt`.
- Shipped: the `--ifc` route delivers on a hand-typed `Load`; one device-voltage rule. Staged for viewer: nothing (no writer bytes changed; placed instances remain the open cell #16).

# eng #442 — 2026-08-10: a hand-typed board / transformer / switchboard schedule scalar no longer FAILS the `--ifc` route (one `parse_schedule_scalar`, contract-level coercion, a per-item backstop, no bare `Infinity` in any JSON)

Refs #442 (follow-up filed by eng #438; the tech-lead comment on #442 added the `Infinity` keeper from the review of #446). Branch `cam/442-schedule-scalars` from `main` @ 855f764.

## Why

#438 made `DeviceSchedule.Load '180 VA'` coerce-or-degrade one device. Its siblings still raised out of `resolve_intent` and ended `author --ifc` **FAILED, rc 3, no `.rvt`** — and, measured on `main` @ 855f764 (table below), most of them raised **before** `plan_family_for` was ever reached: `_build_feeder_tree` does `int(con.get("Phases") or 3)` and `float(kva)`, `_classify_equipment` does `float(bus)`, and only then do the branch casts (`float(MainsRating)`, `int(NumberOfCircuits)`, `float(RatingkVA)`, the switchboard's `int(Phases)`/`int(Wires)`/`float(ShortCircuitRatingkA)`/`int(Sections)`) get their turn; a device's `MountingHeight '44 in'` reached `resolve_device_facts(float(…))` inside the try and left that device `refused` (not built). Separately, a non-finite cell (`Wires IFCREAL(1.E400)` → `inf`) was echoed by `json.dump` as a bare `Infinity` token into `intent.json` (and, via the room builder's schedule labels, `manifest.json`), which strict JSON parsers reject.

## What was built (`src/rvt/ifc/intent.py` only, + tests + this record; mirror regenerated)

- **`parse_schedule_scalar(pset_key, raw, kind, *, fallback, default=None) -> (value, note)`** — THE one rule for a schedule cell: a number rides as is (a >308-digit `IFCINTEGER` is treated as `inf`, no `OverflowError`); a numeric text label with the unit a person types coerces with ONE note (`PanelSchedule.MainsRating '400 A' is a text label, not a measure -> read as 400 A`); blank / zero → `default` silently; unparseable / negative / non-finite / fractional-for-a-count / above the sanity cap → `default` with ONE note naming the property, the (clipped) value, the reason and the `fallback` wording. `kind` is a frozen `ScalarKind(what, unit, rx, factors, cap, cap_for, integer=False, zero_is_empty=True)`; the presets: `LOAD_VA` (`k` ×1000, cap `MAX_DEVICE_VA`), `AMPS` (`'400 A'`/`'400A'`/`'400 amps'`/`'0.4 kA'`, cap `MAX_BOARD_A` 20 000), `KVA` (`'75 kVA'`/`'75kva'`/`'75'`, cap 100 000), `KA` (`'65 kA'`/`'65 kAIC'`, cap 300), `INCHES` (`'44 in'`/`'44"'`/`'44 in AFF'`/`'1100 mm'`/`'110 cm'`/`'1.1 m'`/`'3.5 ft'`/`"4'"` → inches, cap `MAX_MOUNT_IN` 600, zero is a value), `_count(what, cap, cap_for)` (`'42 ckt'`/`'3 ph'`/`'3-phase'`/`'4W'`/`'4 sections'`, whole numbers returned as `int`; one shared `_RE_COUNT`). All built by one `_scalar_rx(tail)` (number + optional unit tail; group 2 = the scaling token; the open-ended repeats are **possessive** so a space-stuffed junk cell fails in linear time — measured below).
- **`parse_device_load`** is now a three-line preset call (`LOAD_VA`, default `DEFAULT_DEVICE_VA`, #438's fallback sentence) — every value and note string it produced is unchanged (`tests/test_intent_device_plan.py` **35 passed untouched**; a new seam test asserts `parse_device_load(x) == parse_schedule_scalar('DeviceSchedule.Load', x, …LOAD_VA)` over the awkward inputs). **`parse_mounting_height`** is the `INCHES` preset (None + note lets the facts' typical height stand in, flagged 'assumed').
- **Contract-level coercion (the altitude decision).** `CONTRACT_SCALARS = {BusRating: AMPS, MainsRating: AMPS, ShortCircuitRatingkA: KA, RatingkVA: KVA, NumberOfCircuits/Phases/Wires/Sections: _count(…)}`; `normalize_contract` — the fold point BOTH routes go through (`prompt_intent._contract_for` calls it over synthetic psets) — reads each present cell through the rule right after the direct-key/alias loops: a usable value replaces the cell (counts become `int`), an **unusable cell is dropped** (with its provenance) so it reads exactly as an empty one — the existing name-text mining (`'LP-2 30-space …'` → 30), the `MainsRating = BusRating` single-rating alias and the voltage-derived `Phases`/`Wires` then stand in as they always did for a blank — and a blank / zero cell stays put untouched (a switchboard's `NumberOfCircuits 0` still round-trips as `0`). The two figures mined from the NAME text (`NNN A`, `NN space`) clear the same caps through the same local `read()` before they are `put`, so `'MSB 30000 A'` cannot re-inject what an absurd cell just dropped (found by the altitude review; test added). Notes collect on `con["_notes"]` (only when non-empty, so a numeric prompt-route contract gains no key). Because the coercion happens where the contract is made, `_classify_equipment`, `_build_feeder_tree`, `plan_family_for`, `prompt_intent`'s feeder/brief code and `tools/ifc_intent.py` all see a number or no cell — never `'400 A'` — without touching any of them.
- **`plan_family_for`**: the board branches read `MainsRating`/`BusRating`/`NumberOfCircuits`/`ShortCircuitRatingkA`/`RatingkVA`/`Phases`/`Wires`/`Sections` through a local `cell(key, default)` (the same rule via `CONTRACT_SCALARS`; idempotent on the numbers `normalize_contract` left, and the reason a **hand-built** contract dict — tests, research callers of this public function — cannot raise either); the contract's `_notes` + any the branch earns ride on `FamilyPlan.notes` (→ `intent.json familyMapping[].notes`) for every kind, the switchboard's house note after them; the device branch books `mounting_height_in=parse_mounting_height(…)[0]` and appends its note beside the load note. Defaults are the branches' existing ones (`0.0`/`2500.0` A, 42 spaces, `0.0` kVA → the resolver's recorded refusal, `v.phases or 3`, `v.wires or 4`, `0.0` kA, 1 section).
- **`plan_families` backstop**: each `plan_family_for(e)` in its own `try`; an exception yields `FamilyPlan(tag, kind, constructor='', kwargs={}, status='refused', refusal='<Type>: <msg>', notes=['planning this item raised (a resolver fault, not a fact refusal) -> it is NOT built; every other item still is and the file is delivered'])` for THAT item — `frontdoor.build` already lists every non-buildable plan under `build.degradations` and `tools/ifc_intent.stage_families` records it `built: False` — and the loop continues.
- **No bare `Infinity`**: `_psets` passes every value through `_finite_or_text` — a non-finite float (`IFCREAL(1.E400)` → `inf`, NaN; also inside list values) becomes its text `'inf'`/`'-inf'`/`'nan'` at read time, so every echo (`Equipment.as_json` psets/contract in `intent.json`, the room builder's device-schedule label → `manifest.json`, `ifc_out`'s verbatim pset re-emission) is strictly parseable, and the scalar rule then drops the cell with a note naming `'inf'` (`… Wires 'inf' is not a usable wire count (not a number) -> ignored: Wires reads as an empty cell …`). Chosen over `allow_nan`-safe writers because the writers live in four modules, three outside this territory (`manifest.py`, `standalone.py`, `router.py`), and a non-finite measure is not a value anything downstream can build from anyway.
- `__all__` += `ScalarKind`, `parse_schedule_scalar`, `parse_mounting_height`, `CONTRACT_SCALARS`, `LOAD_VA`, `AMPS`, `KVA`, `KA`, `INCHES`. Not touched: `prompt_intent._type_name_for` (optional in the issue: it formats the prompt route's own always-numeric psets for the scene brief only — zero benefit, and touching it risks the "emitted `.ifc` unchanged" evidence), `famgen/factory.py`, `frontdoor/manifest.py`, `build.py`, `tools/ifc_intent.py`, every hot file.
- Tests: new `tests/test_intent_schedule_scalars.py` (**8**, fresh-clone safe; drop-in `tests/ci_shard.d/442-schedule-scalars.txt`): the per-kind coerce + degrade tables (amps incl. `'0.4 kA'`, `'400 AF'`→not a number, `10**400`→not finite + clipped, 25 000→cap; kVA; kA; counts incl. `'42.5'`→not a whole number, `'400 ckt'`→cap, `Phases '4'`→None; the Load preset's cap/unit/factors; three 20 000-space junk cells through count/inches/amps returning promptly; inches incl. metric/feet/AFF spellings, zero kept, `'eye level'`/`-18`/`'4400'`/`'30 m'`); `normalize_contract` on hand-typed psets (six coerced cells, six notes, provenance kept), on junk (`MainsRating 'n/a'` → dropped → `= BusRating`, `NumberOfCircuits 'lots'` → dropped → name-text 30, `Phases 7` → dropped → derived 3; three notes), on an absurd figure in both the cell and the name (`BusRating 30000` + `'MSB 30000 A'` → neither `BusRating` nor `MainsRating`, two notes) vs a sane name (`'DP-2 400 A 42-circuit'` → 400/400/42, no notes), zero/blank cells stay, `RatingkVA '75 kVA'` via the alias loop; the prompt route's numeric contract is value- and type-identical with no `_notes`; `plan_family_for` board branches from hand-typed cells (DP-1 400 A/42/65 kA resolved with 3 notes riding into `as_json`, LP-1 `BusRating '100 A'` → PRL2X, T1 `'75 kVA'` resolved, `'big'` → kva 0.0 → the resolver's `FactoryError` refusal + 1 note, MSB six cells → house kwargs `{2500.0, 3, 4, 65.0, 4}` + 6 notes, a HAND-BUILT string contract → same numbers through `cell()`, devices `'44 in'`/`'1100 mm'`/`'eye level'`); the **backstop** (`_resolve_xfmr_facts` monkeypatched to raise `RuntimeError`: `plan_family_for` itself raises, `plan_families` returns DP-1 resolved / T1 refused `RuntimeError: …` with constructor `''` + the note / R-1 resolved, `buildable_family_plans` = DP-1 + R-1, `summarize` `{resolved 2, refused 1}`); the **route** on OUR IFC with `MainsRating IFCLABEL('100 A')` (bare `intent_from_ifc`: LP-1 resolved, `mains_a 100.0`, PRL2X, exactly the one note, contract `MainsRating 100.0`; `FD.author(ifc=…)`: route ifc, ok, `errors []`, PROOF-ONLY, combined `.rvt` on disk, `build.errors [] / degradations []`, `validation.combined n_errors 0`, 1 equipment-instance + 4 fixture-instances, plans `{resolved 5}`, the note + `mains_a 100.0` in the written `intent.json`); and each other cell retyped once on OUR IFC (`BusRating '100A'`, `NumberOfCircuits '30 ckt'` → spaces 30, `Phases '3 ph'`, `Wires IFCREAL(1.E400)`, `MountingHeight '1100 mm'`, `Load IFCREAL(1.E400)`) → `resolve_intent` never raises, the plan is resolved with exactly one schedule note, `json.dumps(intent_to_json(model), allow_nan=False)` succeeds, and the two non-finite cells are echoed as `'inf'`.

## Evidence (cloud clone, no `samples/`, bundled pinned bases, `RVT_STEPLITE_FORCE=1`; `main` @ 855f764 = this branch with the change stashed, same venv, back to back; `scratchpad/probe.py`)

**The issue's before/after** — OUR IFC of `… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF` with LP-1's `MainsRating` retyped `IFCLABEL('100 A')`, `tools/frontdoor.py author --ifc … --json`:

| | `main` @ 855f764 | this branch |
|---|---|---|
| rc / status | **rc 3**, `FAILED (IFC intent failed: ValueError: could not convert string to float: '100 A')`, `ok false`, no `.rvt`, no intent.json | **rc 0**, `PROOF-ONLY (self-checks PASS; …)`, `ok true`, `errors []`, stderr 0 B |
| delivered | — | `mains_label.rvt` on disk; `tools/rvt_validate.py` → **VALID (no errors); warnings=1** (the standing DataStorage decoder-gap warning every build carries) |
| LP-1 | — | `resolved`, `make_panelboard`, variant **PRL2X**, kwargs `mains_a 100.0 / spaces 42 / sccr_ka None`, ONE note `PanelSchedule.MainsRating '100 A' is a text label, not a measure -> read as 100 A`; contract `MainsRating 100.0` |
| build | — | `errors []`, `degradations []`, 13 rows: `family(.rfa)`, `loaded-family`, `wall`, `equipment-instance` ×1, `fixture-instance` ×4; plans `{resolved 5}`; no `Infinity`/`NaN` token in manifest.json + intent.json |

**Every sibling cell retyped once** on OUR full-lineup IFC (`… a 2500 A switchboard MSB, a 75 kVA transformer T1 fed from MSB, a 400 A distribution panel DP-1 fed from MSB, a 100 A lighting panel LP-1 fed from T1 and 2 duplex receptacles at 18 in AFF`), bare `resolve_intent`:

| Cell retyped | `main` @ 855f764 | this branch (plan status, kwargs, schedule notes on that plan) |
|---|---|---|
| MSB `MainsRating IFCLABEL('2500 A')` | RAISED `ValueError … '2500 A'` | house, `mains_a 2500.0`, 1 note (+ the standing house note) |
| MSB `BusRating IFCLABEL('2500A')` | ok (the switchboard path never cast it) | house, 1 note |
| MSB `ShortCircuitRatingkA IFCLABEL('65 kA')` | RAISED `ValueError … '65 kA'` | house, `sccr_ka 65.0`, 1 note |
| MSB `Sections IFCLABEL('4 sections')` | RAISED `ValueError: invalid literal for int() … '4 sections'` | house, `sections 4`, 1 note |
| MSB `Phases IFCLABEL('3 ph')` | RAISED (`int()` in `_build_feeder_tree`) | house, `phases 3`, 1 note |
| MSB `Wires IFCLABEL('4W')` | RAISED `… '4W'` | house, `wires 4`, 1 note |
| MSB `Wires IFCREAL(1.E400)` | RAISED `OverflowError: cannot convert float infinity to integer` | house, `wires 4` (derived), note `… Wires 'inf' is not a usable wire count (not a number) -> ignored …`; **no bare `Infinity`** in `intent_to_json` (was `true` for the one non-finite case that resolved on main, `Load IFCREAL(1.E400)`) |
| DP-1 `MainsRating IFCLABEL('400 A')` | RAISED `… '400 A'` | resolved, `mains_a 400.0`, 1 note |
| DP-1 `Phases IFCLABEL('3 ph')` | RAISED | resolved, 1 note |
| DP-1 `NumberOfCircuits IFCLABEL('42 ckt')` | RAISED `… '42 ckt'` | resolved, `spaces 42`, 1 note |
| DP-1 `NumberOfCircuits IFCLABEL('lots')` | RAISED `… 'lots'` | resolved, `spaces 42` (default), note `… 'lots' is not a usable circuit count (not a number) -> ignored: NumberOfCircuits reads as an empty cell …` |
| LP-1 `MainsRating IFCLABEL('100 A')` / `BusRating IFCLABEL('100 A')` | RAISED / RAISED (`float(bus)` in `_classify_equipment`) | resolved `mains_a 100.0`, 1 note each |
| T1 `RatingkVA IFCLABEL('75 kVA')` | RAISED `… '75 kVA'` (`float(kva)` in `_build_feeder_tree`) | resolved, `kva 75.0`, feeder `MSB>T1 90.2 A`, 1 note |
| T1 `RatingkVA IFCLABEL('big')` | RAISED `… 'big'` | **refused** (kva 0.0 → the resolver's `FactoryError: no variant match …`, recorded), 1 note; the other five plans resolved/house |
| R-1 `MountingHeight IFCLABEL('44 in')` / `('1100 mm')` / `('eye level')` | ok but R-1 **refused** `ValueError … '44 in'` (not built) ×3 | resolved `mounting_height_in 44.0` / `43.307…` / `None` (facts' 18 in, assumed), 1 note each |
| R-1 `Load IFCREAL(1.E400)` | resolved, note `… Load inf … (not finite)`, **bare `Infinity` in intent JSON** | resolved, note `… Load 'inf' … (not a number)`, no bare `Infinity` |

Front door (`FD.author`) on seven of those (`msb_sccr_65kA`, `dp_ckt_42ckt`, `dp_phases_3ph`, `t1_kva_75kVA`, `t1_kva_abc`, `r1_height_44in`, `msb_wires_inf`): all **rc 0, PROOF-ONLY, combined `.rvt` delivered, `validation.combined n_errors 0`**, no bare `Infinity` in manifest.json / intent.json; degradations = the lineup's standing `UTILITY>MSB … panel not placed` circuit line, plus for `t1_kva_abc` the expected `T1 (transformer): NOT built -- family plan refused: FactoryError …` line.

**Unchanged vs `main`** (same two trees, back to back): `author --prompt 'an electrical room with 6 panels'` and `author --prompt '… a 100 A lighting panel and 4 duplex receptacles at 44 in AFF'` → `intent.json` **byte-identical**, `scene-brief.json` **byte-identical**, `HANDOFF.md`/`PROMPT_TO_IFC.md` identical, `manifest.json` identical in status / rows / kinds / census / plans-by-status / validation (`VALID 0`) and, after scrubbing, differs only in `generated_at`, stage seconds, the combined file's sha/md5 and the per-run family `content_guid`s (which differ between two runs of the SAME tree — #9); the emitted `.ifc` of both prompts and of the full-lineup prompt (`write_intent_ifc`) is **identical except the `FILE_NAME(...)` header line** (it carries the output filename).

## Findings / follow-ups (searched first; all three filed task-shaped, `Refs #442`, from the `/simplify` altitude pass — each is outside this issue's territory)

- **#461** — a shared `json_safe()` walk in front of the four front-door JSON writers (`write_intent`, `manifest.py`, `router.py`, `standalone.py`) so a non-finite from ANY source, not only psets, never lands as a bare `Infinity`; `_psets`' read-time transform stays as the IFC-writer guard.
- **#462** — a planning FAULT distinguishable from a fact REFUSAL: the backstop's `status='refused'` is what #442's DONE specified, but `build.py:463` then appends the "supply the missing facts" hint to what is really a traceback; `status='faulted'` + its own wording + a "zero faulted plans" shard assertion (needs `build.py`, held by PR #453 this wave).
- **#463** — devices at the boards' altitude: `Load`/`MountingHeight` are coerced at plan time while the raw `DeviceSchedule` pset still rides verbatim into the manifest's device-schedule row (`tools/ifc_intent._instance_labels`) and OUR re-emitted IFC (`ifc_out`), so a hand-typed row can read `'180 VA'` beside a family that booked 180.0.
- Considered and left: `prompt_intent._type_name_for`'s `float(dev.get('Load') …)` (prompt-only, always numeric — see above); a `MountingHeight` authored as `IFCLENGTHMEASURE` in a mm project is already converted to **metres** by `_psets`' #348 unit scaling before any parser sees it (`1.1` would then read as 1.1 in) — pre-existing, our writer and `PROMPT_TO_IFC.md` author it as `IFCREAL` inches, no input in hand exercises it, and #463's device fold is where a `_DIMENSIONED`-aware read would live.

**`/simplify` (4 independent angles — reuse / simplification / efficiency / altitude; findings deduplicated, then applied on the branch).** *Reuse:* nothing in the tree was reusable — `prompt_intent._RE_AMP/_RE_KVA/_RE_KA/_RE_SPACES/_RE_AFF` are prose `search` extractors with mandatory units and the wrong import direction, `famgen.factory._number`/`_voltage_number` and `convert.modify_family._convert_value` raise (exactly what #442 removes) and are off-limits, `_si_factor` works on IfcNamedUnit entities, no non-finite JSON sanitiser or repr-clipper exists anywhere (the diff removes the one prior duplicate by folding `parse_device_load`'s body into the shared rule); `_INCH` is a fourth spelling of length factors (`prompt_intent._to_metres` defaults a bare number to feet, `modify_family._UNIT_TOKENS` returns feet and lives in `convert/`) — left, with the reason. *Applied:* the eight always-together keyword parameters became one frozen `ScalarKind` passed positionally (`parse_schedule_scalar(key, raw, kind, *, fallback, default)`; presets typed, `CONTRACT_SCALARS: Dict[str, ScalarKind]`, no `**` splats); the failure predicates are written once (`why` chain ending in `else None`, one branch) instead of a success guard mirrored by the chain; `cell()` passes `default` through instead of re-deriving it; `src[key].removeprefix("pset:")` replaces a dead `else key` arm; `repr(raw)` computed once; `_RE_LOAD_VA` inlined into `LOAD_VA`, `float(cap)` dropped, one shared `_RE_COUNT`; the presets joined `__all__` beside `CONTRACT_SCALARS`; **the one material efficiency item** — every `_scalar_rx` pattern went O(n²) on a non-matching space-stuffed cell because the post-number `\s*` / the count and AFF tails overlapped the closing `\s*$` (measured: `'42 ckt' + 5000 spaces + '!'` 96–104 ms per parse, `'44' + 5000 spaces + '!'` 100–160 ms, i.e. ~10 s at 50 k chars, twice per cell) → the open-ended repeats are now possessive (`\s*+`, `[-a-z. ]*+`, `.*+`, Python ≥ 3.11 which the repo requires): worst n = 5000 case **0.10 ms**, every pinned spelling unchanged (43 tests green before and after), a 20 000-space row added to the shard test; **the altitude item** — name-text-mined `BusRating`/`NumberOfCircuits` now clear the same caps as a cell (above); `MAX_MOUNT_IN` 240 → 600 in so the only cap whose fallback changes a modelled dimension cannot disagree with a placement the prompt grammar legitimately produces (`'receptacles at 8 m AFF'` = 315 in places at 8 m; at 240 the family would have booked the facts' 18 in) while `'4400'` (mm typed as inches / a slipped decimal) is still caught. Test file: the tautological wrapper-equals-wrapped test dropped for the one non-derivable line, `_catalog_ok()` evaluated once, a duplicate `as_json` pin and a redundant pre-loop fixture check removed. *Measured, left:* +5 distinct compiled patterns ≈ 1.1 ms of a 31–34 ms module import (< 1 % of a cold start); `cell()` re-read ≈ 1.2 µs per already-coerced cell (~8 µs on the 7-cell switchboard branch of a 200–380 µs `plan_family_for`); `_finite_or_text` ≈ 0.15 µs per pset value; the `cell()` double read itself stays (altitude verdict: a legitimate totality guard for a public function over a hand-buildable dict, re-applying the same table idempotently — the branches lost their `float(… or … or 0.0)` chains rather than gaining try/excepts). *Declined, with the reason:* `status='faulted'` for the backstop (the issue's DONE names `'refused'`; the honest fix needs `build.py` → #462); dropping `.get` on the factor table (kept, commented: a tail the table lacks must read "not a number", never raise).

**`/verify` (front-door CLI on the final tree, `RVT_STEPLITE_FORCE=1`, `out/verify/`):** `author --ifc mains_label.ifc` (the issue's case) → **rc 0**, stderr 0 B, `PROOF-ONLY (self-checks PASS …)`, `errors []`, `degradations []`, rows `family(.rfa) 2 / loaded-family 2 / wall 4 / equipment-instance 1 / fixture-instance 4`, plans `{resolved 5}`, LP-1 `mains_a 100.0` with the one note, `tools/rvt_validate.py` **VALID (no errors); warnings=1**, no `Infinity`/`NaN` token in manifest.json + intent.json; `tools/provenance.py … --baseline all --streams` prints the same ledger classes as `main`'s build of the same room (author `rvt-writer`, username empty, 2 embedded family documents `ours-created`; the `x` identity/residue lines are the pinned base's standing #19/#21 items, unchanged by this diff). `author --ifc nasty.ifc` (full lineup with EIGHT cells retyped at once: MSB `MainsRating '2500 A'` + `ShortCircuitRatingkA '65 kAIC'` + `Wires IFCREAL(1.E400)`, DP-1 `MainsRating '400A'` + `NumberOfCircuits 'lots'`, T1 `RatingkVA '75 kVA'`, R-1 `MountingHeight '1100 mm'` + `Load '0.36 kVA'`) → **rc 0**, stderr 0 B, PROOF-ONLY, `errors []`, **VALID 0**, rows `family 6 / loaded-family 6 / wall 4 / equipment-instance 4 / fixture-instance 2 / circuit 3`, plans `{resolved 5, house 1}`, kwargs MSB `{2500.0, 3, 4, 65.0, 4}` / DP-1 `{400.0, 42}` / T1 `{75.0}` / R-1 `{43.307 in, 360.0 VA}` (its own family, R-2 on the default one), exactly one note per retyped cell on the right plan (8), the one degradation = the lineup's standing `UTILITY>MSB` external-source circuit line, no bare `Infinity`.

## Gates

- `RVT_SKIP_LARGE=1 .venv/bin/python -m pytest tests/test_intent_schedule_scalars.py -q -rs` → **8 passed** (2.9 s; also under `RVT_STEPLITE_FORCE=1`); `tests/test_intent_device_plan.py` → **35 passed** (untouched); together 43 passed.
- neighbours (final tree): `tests/test_intent_device_plan.py tests/test_intent_schedule_scalars.py tests/test_ifc_intent.py tests/test_ifc_intent_units.py tests/test_prompt_intent.py tests/test_ifc_census.py tests/test_census.py tests/test_place_fixtures.py tests/test_rvt_to_ifc_kind_agreement.py tests/test_rvt_to_ifc_param_carrier.py tests/test_convert_combo.py` → **150 passed / 15 skipped** (sample-gated skips) in 27 s.
- `tools/sync_plugin.py` → mirror `plugin/lib/src/rvt/ifc/intent.py` regenerated, zip rebuilt; `--check` clean (deny-audit clean, identity scan == allowlist, assets verified); `plugin/scripts/validate_plugin.py` PASS (25 assertions); `python3 tools/dev/check_portable_paths.py` ok (2908 paths); `tools/dev/shard_list.py --print` lists the new file (75 entries).
- the scratch probe (19 single-cell variants, bare resolver) re-run on the post-`/simplify` tree: rows **identical** to the pre-simplify run; the two prompt jobs re-run: `intent.json` / `scene-brief.json` byte-identical to `main`'s, manifest status/rows/kinds/degradations/census/plans/VALID identical.
- whole merged CI shard on the final tree (`RVT_SKIP_LARGE=1 .venv/bin/python -m pytest -q -p no:cacheprovider $(python3 tools/dev/shard_list.py --print)`): **1564 passed, 134 skipped, 3 xfailed** in 302.1 s.
- Full suite not run (SUITE-COORDINATION).

## Review round 1 (tech lead, 🛑 on 69def0d — one blocking hole) — fixed on the same branch

- **Finding:** a TEXT cell that parses to "empty" — `'0 A'`, `'  '`, `'0 ph'`, `'0 kVA'`, `'-0 kVA'`, `'0.0'`, `'0W'`, `'0 ckt'` — returned `(None, None)` silently, so `normalize_contract`'s "unusable → drop" arm (keyed on a note having been earned) left the **raw string** in the contract; the untouched downstream casts then reproduced `main`'s failure (`BusRating '0 A'` / `Phases ' '` / `RatingkVA '0 kVA'` → rc 3 FAILED via `float(bus)` / `int(Phases)` / `float(kva)`), and `MainsRating '0 A'` leaked into `FeederEdge.rating_a` so stage E died and the delivered file had 0 equipment instances. **Fix (`normalize_contract`, in territory):** after the read, a cell is kept only if it parsed to a usable value **or is an authored numeric 0** (`isinstance(raw, (int, float)) and not bool and raw == 0` — the switchboard's `NumberOfCircuits 0` round trip); everything else the rule returned `None` for — unusable (noted), blank, or a text zero (silent, like a numeric zero) — is dropped with its provenance, so no raw string is ever left for a consumer to cast. The `n_notes` bookkeeping went with it.
- **Nit (a) taken:** `_scalar_rx`'s number accepts thousands commas (`\d{1,3}(?:,\d{3})++`, stripped before `float`) so `'1,200 A'` / `'12,000A'` / `'1,200.5'` coerce (`'1,20 A'`, `'1,2000 A'`, `',200 A'` stay "not a number"); the name-text `NNN A` miner takes `1,200` whole and refuses a digit/comma/dot-prefixed match (`(?<![\d,.])(\d{1,3}(?:,\d{3})+|\d{2,5})\s*A\b`), so `'DP-1 - 1,200 A …'` mines 1200, never the `200` after the comma. This deliberately extends `parse_device_load` too (`'1,200 VA'` → 1200 VA with the text-label note; it was "not a number" before — no pinned case changes). **Nit (b)** (`'400 AF'`, `'65 kA RMS'`, `'0.075 MVA'`, `4 ft 6 in` dropped with a note) left as is — honest. **Nit (c)** filed as **#465** (plan notes into `manifest.json` summary + `MANIFEST.md`; `manifest.py`/`frontdoor/intent.py` are outside this territory).
- Tests 8 → 8 (rows added, no new functions): the empties table (`'0 A'`/`'  '`/`'0 ckt'`/`'0.0'`/`'0 ph'`/`'0W'`/`'-0 kVA'`/`' '` → all dropped, `Phases`/`Wires` re-derived from the voltage, no `_notes`), the invariant "no `str` value remains for any `CONTRACT_SCALARS` key after `normalize_contract`" over the typed / junk / empties schedules and over every equipment item of every retyped-IFC case (+ every `FeederEdge.rating_a` numeric-or-None), the comma rows (cell + five name texts), three silent IFC retypes (`MainsRating '0 A'`, `Phases '  '`, `NumberOfCircuits '-0 ckt'` → resolved, zero notes), and the route retype the review asked for: OUR IFC with `BusRating IFCLABEL('0 A')` → `intent_from_ifc` contract has no string BusRating (re-mined from the name text) / `MainsRating 100.0` / no notes; `FD.author` → ok, `errors []`, delivered, `degradations []`, VALID 0, LP-1 built (1 equipment-instance).
- Re-driven as nobody (`RVT_STEPLITE_FORCE=1`, full-lineup IFC, one cell each): bare resolver — `BusRating '0 A'` / `'  '`, `MainsRating '0 A'` (LP-1 and the MSB-fed DP-1), `Phases '0 ph'` / `' '`, `RatingkVA '0 kVA'` / `'-0 kVA'`, `NumberOfCircuits '0 ckt'`, `Wires '0W'`, `MainsRating '1,200 A'` → **all resolve**, zero schedule notes for the empties (one for `'1,200 A'`), no string scalar left in any contract, every feeder `rating_a` numeric (`MSB>DP-1 400.0` where 69def0d carried `'0 A'`; `MSB>T1 None` for the kVA-less transformer, as for a blank cell on `main`), no bare `Infinity`; front door on seven of them → **rc 0, PROOF-ONLY, delivered, VALID 0, 4 equipment-instances + 3 circuits** each (T1 with `'-0 kVA'` and DP-1 at `1,200 A` are honest fact refusals — 3 instances — exactly what a blank kVA / an uncatalogued 1200 A member do on `main`); the original 19 probe rows are identical to the pre-review run; the two prompt jobs' `intent.json` + `scene-brief.json` byte-identical to `main`'s.
- Gates on the fix tree: `tests/test_intent_schedule_scalars.py` 8 passed (also under `RVT_STEPLITE_FORCE=1`) + `tests/test_intent_device_plan.py` 35 passed = 43; neighbours (`test_ifc_intent`, `_units`, `test_prompt_intent`, `test_ifc_census`, `test_place_fixtures`, `test_rvt_to_ifc_kind_agreement`, `_param_carrier`, `test_convert_combo`) 101 passed / 9 skipped; `sync_plugin.py` run + `--check` clean; `validate_plugin.py` PASS; portable paths ok; whole merged CI shard on the fix tree **1564 passed, 134 skipped, 3 xfailed** (387.5 s).

## BRANCH STATE (eng #442)

- Branch `cam/442-schedule-scalars` from `main` @ 855f764; PR **#457** (`Closes #442`).
- Files written: `src/rvt/ifc/intent.py`, `tests/test_intent_schedule_scalars.py` (new), `tests/ci_shard.d/442-schedule-scalars.txt` (new), this record (own header); regenerated mirror `plugin/lib/src/rvt/ifc/intent.py`.
- Not touched: `src/rvt/frontdoor/prompt_intent.py` (optional in the issue; reason above), `tools/rvt_job.py`, `objects.py`/`encode.py`, `manipulate.py`/`validate.py`/`rvt_edit.py`, `frontdoor/build.py`/`standalone.py`/`manifest.py`, famgen skeleton / famdoc / geometry / factory bodies, `tools/ifc_intent.py`, every hot file, `tests/ci_shard.txt`, `tests/test_intent_device_plan.py`.
- Shipped: the `--ifc` route delivers on any hand-typed board / transformer / switchboard / device schedule scalar; one rule reads them all; the contract is numeric for every consumer; a planning fault refuses one item, never the file; no pset-borne `Infinity` in any JSON; no raw string ever left in a numeric contract cell. Staged for viewer: nothing (no writer bytes changed; placed instances remain the open cell #16). Follow-ups #461 #462 #463 #465 filed.

# eng #461 — 2026-08-10: every JSON the front door writes is strictly parseable (one shared `rvt._jsonsafe` walk in front of `write_intent`, `manifest.write_manifest`, the router's four writers and `standalone`'s report writer; `allow_nan=False` armed behind it)

Refs #461 (follow-up filed by eng #442's `/simplify` altitude pass). Branch `cam/461-json-safe` from `main` @ 2767197 (after #457/#466/#468/#469/#471 landed in these files today).

## Why

#442 closed the pset-borne case at the source (`_finite_or_text` in `_psets`), which was the deepest fix inside that issue's territory. The requirement — no JSON the front door writes carries a bare `Infinity`/`-Infinity`/`NaN` token (browsers' `JSON.parse`, jq, Go reject them) — has N sinks, not one, and each was `json.dump(..., default=str)`: `default=` is never consulted for a float, and `allow_nan=False` on its own would *raise* mid-delivery (rule 1). A non-finite from any non-pset source (a computed ratio, a degenerate extent through `_f`/`_vf`, a timing) would still have landed as a bare token.

## What was built

- **`src/rvt/_jsonsafe.py`** (new, 46 lines, stdlib-only LEAF beside `rvt._logsink`; imported by `rvt.ifc.intent` and three `rvt.frontdoor` modules, pulls neither package): `json_safe(obj)` — a non-finite `float` → `str(obj)` = `'inf'`/`'-inf'`/`'nan'` (the same spellings `_finite_or_text` uses); `dict` → new dict, same key order; `list`/`tuple` → list; every other leaf returned as the same object (so `default=str` still renders it at dump time). `dump(obj, fh, *, default=str, **kw)` = `json.dump(json_safe(obj), fh, allow_nan=False, default=default, **kw)` — the walk first, THEN the strict flag, so a value the walk ever misses fails in `tests/`, never as an unparseable file in a user's run.
- Call sites (writer lines only, nothing else in those files): `manifest.write_manifest` (`manifest.json`), `router._extract…` `families.json`, the prompt→ifc route's `prompt-coverage.json`, the rfa-load route's `<name>_load-report.json`, `_write_route_manifest` (`route.json`), `standalone.standalone_family_write`'s report → `_jsonsafe.dump(x, fh, indent=1)`; `rvt.ifc.intent.write_intent` (`intent.json`) → `_jsonsafe.dump(intent_to_json(model), fh, indent=indent)`; `intent.py`'s diff is exactly that call plus the import block (`from .. import _jsonsafe` replaces `import json`, whose only use was this writer). Seven sites, one spelling. `_psets`' read-time `_finite_or_text` stays (it also guards `ifc_out`'s pset re-emission, which no JSON-side walk covers).
- Tests: new `tests/test_frontdoor_json_strict.py` (**11**, 0.11 s, fresh-clone safe; drop-in `tests/ci_shard.d/461-json-strict.txt`): the walk on inf/-inf/nan at four depths inside dicts, lists and tuples (finite floats / ints / bools / None come back as the same objects); **byte-for-byte no-op on finite data** — the 6-panel prompt's `intent_to_json`, its prompt-coverage block and a timings-shaped dict dump to the identical text through `_jsonsafe.dump` and plain `json.dump(…, default=str)` at the writers' indents 1 and 2; each writer round-trips a poisoned payload and `json.loads(text, parse_constant=<raise>)` + a `\bInfinity\b|\bNaN\b` regex accept the file — `write_intent` (poison in `audit`, `census`, `notes`, i.e. NOT psets), `write_manifest` (poison in `inputs`, `build.seconds/timings`, a nested block; `MANIFEST.md` still renders; the caller's dict is not edited), `router._write_route_manifest` (poison in `seconds`, `steps`, `cell.evidence`, `opts`; `ROUTE.md` still renders); `dump` renders a non-float oddity through `default=str` and, with the walk monkeypatched to a pass-through, raises `ValueError` on `inf` (the strict flag is armed *behind* the walk); two coarse tripwires — none of the four switched modules contains a bare `json.dump(` (each has `_jsonsafe.dump(`), and `_jsonsafe` imports only `__future__`/`json`/`math`/`typing`.

## Evidence (cloud clone, no `samples/`, bundled pinned bases; `main` @ 2767197 in a `git worktree` vs this branch, same venv, back to back)

- **The walk is a no-op on every JSON `main` writes today.** Every `*.json` under `main`'s three job dirs — `author --prompt 'an electrical room with 6 panels'` (10 files: `manifest.json`, `intent.json`, `scene-brief.json`, `_stages/stage_L_loaded.rvt.load.json`, 6 × `families/pp*_eaton_prl2x_225a_42sp_480y_277.json`), `route.py run --output rvt --prompt '…6 panels'` (+ `route.json` = 11), `author --ifc usecases/eaton-panelboard/panelboard.ifc --target-version 2025` (4) — was `json.load`ed and re-dumped through `_jsonsafe.dump` at the writer's indent: **byte-identical to the file on disk, 25/25** (`cmp`-equal text; the 4 496-line `manifest.json` included). That is the "6-panel manifest byte-for-byte unchanged" proof in its strongest form: two live runs of the SAME tree already differ in `generated_at`, stage `seconds`, the built file's `sha256`/`md5` and the per-run family `content_guid`s (#9), and the branch-vs-main `diff` of the two live manifests shows exactly and only those lines (52 hunks, every one a timestamp/seconds/digest/guid); `intent.json` and `scene-brief.json` branch vs main: **`cmp` identical** (prompt job and route job); the `--ifc` job's `intent.json` differs only in `source.path` (the worktree's vs the repo's copy of the input).
- **`grep -rcE 'Infinity|NaN'` over the job dirs' JSON = 0** — branch: prompt job 0/10 files, route job 0/11, `--ifc` job 0/4 (main: also 0 — none of these inputs produces a non-finite; the synthetic round trips are what exercise the transform).
- Every job **rc 0**, `PROOF-ONLY (self-checks PASS; …)`, `ok true`, same file set as `main` (`prompt_room.rvt` / `panelboard.rvt` delivered).
- **Wall time unchanged** (same VM, back to back, `date +%s.%N` around the CLI): `author --prompt '…6 panels' --json` main 4.47 s / branch 4.19 s (first runs, cold caches on both); `author --ifc panelboard.ifc --target-version 2025` main 3.29 s / branch 3.38 s; repeated timings in *Gates* below. The walk over the 6-panel manifest dict (the largest document, 4 496 lines) costs **≈ 0.9 ms** (`timeit`, 200 reps) against a 4 s job; `intent.json`’s dict ≈ 0.3 ms.

## Findings / follow-ups (searched first)

- Filed **#475** (`Refs #461`, task-shaped): the same one-line switch for the job-dir JSON writers *outside* this issue's territory — `src/rvt/frontdoor/prompt_intent.py:1920` (`scene-brief.json`), `src/rvt/frontdoor/edit.py:386` (the edit spec echo), `tools/ifc_intent.py:_jdump` (`families/*.json`, `_stages/*.load.json`), `src/rvt/ifc/famfrom_ifc.py:353/895/1010` (load report / manifest / summary), `tools/rvt_job.py` (its own manifest, `default=_jsonable`), and the `--json` stdout documents of `tools/frontdoor.py`/`tools/route.py` (hot file → its own tiny PR). None of them carries a non-finite today (grep above), so this is armour, P2.
- Considered and left: an in-place walk (no copy) — rejected, `write_manifest`'s caller keeps using its dict (`MANIFEST.md` is rendered from it next; the front door returns it in `--json`), and the copy of a 4 496-line manifest is under 1 ms; walking `set`/generators — no writer passes one (`default=str` would have rendered them as text before, and still does).

**`/simplify` (4 independent angles — reuse / simplification / efficiency / altitude).** *Reuse:* nothing existing does a dict-walking non-finite sanitise or a strict dump — `objlint._json_safe` sorts sets (no float handling), `manipulate._jsonable` / `rvt_job._jsonable` / `fifth_surface._json_default` are `default=` hooks `json` never calls for a float, the three `_write_json` helpers (`famload`, `genesis/y2024_b`, `y2025_b`) are plain `json.dump` wrappers; `intent._finite_or_text` overlaps on the float branch only (list/tuple, type-preserving, dict-blind, read-time) — left, it is #442's IFC-writer guard and outside this territory. *Applied:* `write_intent` uses `_jsonsafe.dump` like the other six instead of an inline `json.dump(json_safe(…), allow_nan=False)` re-spelling (the "keeps `import json` used" argument did not survive: that import's only use *was* this writer, so it goes and the file's diff is still import block + call site), which also retired a bespoke source-regex test; `str(float(obj))` → `str(obj)` (the `isinstance` guard makes `float()` a no-op; numpy floats `str()` to the same spellings); `dump(obj, fh, *, default=str, **kw)` instead of `kw.setdefault`; the three `# strict JSON …` call-site comments dropped (3 of 7 annotated read as if the other four differed; the callee's name and docstring say it); tests: the "refuses loudly" case now monkeypatches the walk to a pass-through and asserts `JS.dump` itself raises (it asserted stdlib trivia before), the per-file exact call counts became one coarse parametrised tripwire over the four switched modules (counts pinned syntax and would have gone red on a legitimate fifth router writer), the no-op test runs the writers' two indents instead of three, one module-scoped `prompt_to_intent` fixture. *Measured, left (efficiency):* copy-walk 0.27 ms on the 6-panel intent dict (1 267 nodes) vs 0.35 ms for a "same object if unchanged" walk (it still visits every node) — copy is also the correct trade because `write_manifest`'s caller renders `MANIFEST.md` from, and returns, its dict; `allow_nan=False` changes no encoder path (`json.dump` never takes `c_make_encoder`; 1.35 ms either way); no cold-start assertion counts modules (`test_coldstart` pins only `numpy` absence); a `type(obj) is str/int` fast path saves 0.05 ms/file — not worth the branch; `fh.write(json.dumps(…))` would be ~8 % faster than streaming `json.dump` (0.1 ms/file) — left for #475's `write()` helper. *Altitude:* sanitise-at-writer confirmed as the only depth that satisfies rule 1 and CPython's float formatting (an encoder subclass means overriding private `iterencode`/`floatstr`; monkeypatching `json` is global state on a shared surface; "fix every producer" has no choke point to arm the strict flag behind); the follow-up #475 was steered (comment on the issue) toward one path-owning `_jsonsafe.write(path, obj, indent)` (makedirs + `encoding='utf-8'` + walk + strict dump) rather than ten more two-line swaps.

**`/verify` (final tree, front-door CLI + router + bare unzip, `out/verify/`):** `author --prompt 'an electrical room with 6 panels' --json` → **rc 0**, stderr 0 B, `PROOF-ONLY (self-checks PASS; …)`, output release 2026, `prompt_room.rvt` → `tools/rvt_validate.py` **VALID (no errors); warnings=1** (the standing DataStorage decoder-gap warning), 10 JSON files in the job dir all accepted by `json.load(…, parse_constant=<raise>)`, 0 `Infinity`/`NaN` hits, `intent.json` + `scene-brief.json` `cmp`-identical to `main`'s; the same prompt `--target-version 2025` → rc 0, output release 2025, **VALID 0, warnings=0**, 10/10 strict; `author --ifc usecases/eaton-panelboard/panelboard.ifc --target-version 2025` → rc 0, `panelboard.rvt` **VALID 0**, 4/4 strict; `route.py run --output rvt --prompt '…6 panels' --json` → rc 0, PROOF-ONLY, 11/11 strict incl. `route.json`, `intent.json` `cmp`-identical to `main`'s; **bare unzip of the rebuilt `tekton-plugin.zip` + system `python3 skills/tekton-author/scripts/_bootstrap.py go author --prompt '…6 panels' --out out/j1 --json`** (the `standalone.py` path) → rc 0, stderr 0 B, `go.ready true`, result `PROOF-ONLY (self-checks PASS; …)`, 10/10 job JSON strict, 4.2 s wall. And on the final tree, `main`'s 25 job-dir JSON files re-dumped through `_jsonsafe.dump` are still **25/25 byte-identical**.

## Gates

- `RVT_SKIP_LARGE=1 .venv/bin/python -m pytest tests/test_frontdoor_json_strict.py -q -rs` → **11 passed** (0.11 s).
- neighbours (final tree): `tests/test_frontdoor_json_strict.py tests/test_frontdoor.py tests/test_frontdoor_standalone.py tests/test_frontdoor_manifest_pin.py tests/test_intent_device_plan.py tests/test_intent_schedule_scalars.py tests/test_ifc_intent.py tests/test_prompt_intent.py tests/test_router.py tests/test_router_load_release.py tests/test_router_release.py tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_coldstart.py tests/test_surface_perf.py` → **370 passed / 23 skipped** (145 s; skips = sample-gated cases + `test_surface_perf`'s 3 "no bare python3 with numpy on this host").
- wall time, final tree (same VM, one run each after the caches warmed): prompt 2026 3.85 s, prompt 2025 4.48 s, `--ifc` 2025 2.51 s, route 3.77 s, bare-unzip `go` 4.25 s — within the run-to-run spread of `main`'s 4.47 s / 3.29 s first runs above; the change adds < 1.5 ms of walking per job.
- `tools/sync_plugin.py` → 5 files mirrored (`plugin/lib/src/rvt/_jsonsafe.py` new + the four writers), zip rebuilt, deny-audit clean, identity scan == allowlist; `--check` clean; `plugin/scripts/validate_plugin.py` PASS (25 assertions); `python3 tools/dev/check_portable_paths.py` ok.
- Full suite not run (SUITE-COORDINATION).
- whole merged CI shard on the final tree (`RVT_SKIP_LARGE=1 .venv/bin/python -m pytest -q -p no:cacheprovider $(python3 tools/dev/shard_list.py --print)`, 79 files incl. the new one): **1628 passed, 139 skipped, 3 xfailed, 1 failed** in 331 s — the one failure is #476 below, red on `main` itself. A first run on the pre-`/simplify` tree: **1627 passed, 139 skipped, 3 xfailed, 2 failed** in 344 s — one failure was this stream's own since-retired source-regex test caught mid-edit, the other is `tests/test_manipulate.py::test_job_set_param_op_lands_an_elementid_row_via_holder` (`ImportError: cannot import name '_load_job' from 'test_job'`), which **fails identically on `main` @ 2767197** (reproduced in the `main` worktree: `1 failed, 18 deselected`) — #471 moved that helper to `tests/conftest.py` after #469 added the importer; already filed as **#476**, outside this territory (`tests/conftest.py` / `test_manipulate.py` are other engineers' this wave).

## BRANCH STATE (eng #461)

- Branch `cam/461-json-safe` from `main` @ 2767197; PR (`Closes #461`).
- Files written: `src/rvt/_jsonsafe.py` (new), `src/rvt/ifc/intent.py` (import block + the `write_intent` call), `src/rvt/frontdoor/manifest.py` / `router.py` / `standalone.py` (import + writer lines only), `tests/test_frontdoor_json_strict.py` (new), `tests/ci_shard.d/461-json-strict.txt` (new), this record (own header); regenerated mirrors `plugin/lib/src/rvt/_jsonsafe.py` (new) + the four writers.
- Not touched: `build.py`, `frontdoor/__init__.py`, `prompt_intent.py`/`edit.py`/`tools/ifc_intent.py`/`famfrom_ifc.py`/`rvt_job.py` (their writers → #475), `manipulate.py`/`validate.py`, `tests/conftest.py`, `base.py`, famgen, every hot file, `tests/ci_shard.txt`.
- Shipped: every JSON the front door's four writer modules produce is strictly parseable whatever non-finite value reaches it, with `allow_nan=False` armed behind the walk; finite output byte-identical to `main`'s. Staged for viewer: nothing (no `.rvt`/`.rfa` byte changes). Follow-up #475 filed (+ design note); #476 (pre-existing red shard test on `main`) noted, not mine.

# eng #475 — 2026-08-10: the remaining job-dir JSON writers and the `--json` stdout documents go through `rvt._jsonsafe` too (one path-owning `write()`, a `dumps()` twin, a walked `default=`), so no tekton-written JSON on the front-door path can carry a bare `Infinity`/`NaN` — `tools/frontdoor.py` (hot) excepted and filed

Refs #475 (filed by eng #461's `/simplify` altitude pass; design note on the issue). Branch `cam/475-jsonsafe-write` from `main` @ 676916a (= #461/#481 merged; after #443/#454/#456 in `tools/rvt_job.py`).

## Why

#461 armed the walk + `allow_nan=False` behind four writer modules. The same `json.dump(..., default=str)` (a non-finite float → a bare `Infinity`/`NaN` token strict parsers reject; `default=` is never asked about a float) sat at every other sink that lands in a user's job dir or on the `--json` stdout a skill session parses. The design note asked for ONE path-owning helper instead of N more two-line swaps, because what a future writer forgets is the whole `makedirs + open("w") + dump` stanza (the existing sites already disagreed on `makedirs`, none wrote `utf-8`).

## What was built

- **`src/rvt/_jsonsafe.py`** (77 lines, still a stdlib-only LEAF; `json_safe`/`dump` signatures unchanged): **`write(path, obj, *, indent=1, default=str, **kw) -> path`** = `os.makedirs(dirname(abspath(path)) or ".", exist_ok=True)` + `open(path, "w", encoding="utf-8")` + ONE `fh.write(dumps(...))` (not `json.dump`'s thousands of chunk writes; identical text) — the caller keeps any `OSError` where losing the file is tolerable; **`dumps(obj, *, default=str, **kw)`** = `json.dumps(json_safe(obj), allow_nan=False, default=…)` for the `--json` printers; and **a `default=` hook's result is walked too** (`_walked(default)` wraps it as `lambda o: json_safe(default(o))`, `None` stays `None`). That last one is not decoration: `tools/rvt_job.py` dumps with `default=_jsonable`, which expands a dataclass / `to_json()` object into a dict *at dump time*, after the top-level walk; a non-finite float inside such an expansion would otherwise have made the newly-armed `allow_nan=False` **raise mid-delivery** (rule 1) where `main` merely wrote a bare token. Docstring trimmed to the why + the three entry points (the caller inventory lives in the test's tripwire, not in prose that rots).
- Call sites (writer / printer lines only, plus one import line per module): `frontdoor/prompt_intent.write_handoff` → `bp = _jsonsafe.write(…/scene-brief.json, brief, indent=2)`; `frontdoor/edit.run_edit` → `ops_path = _jsonsafe.write(…/ops.json, {...}, indent=1)`; `tools/ifc_intent._jdump` → `_jsonsafe.write(path, obj, indent=1)` (its 6 callers untouched: `families/*.json`, `families_record.json`, `_stages/*.load.json`); `ifc/famfrom_ifc.py` ×3 (`write_rfa`'s report, `probes.json`, `ifc_family.json`) → `_jsonsafe.write(…, indent=1)`; `tools/rvt_job.py` — `validation_gate` (`<out>.validation.json`, inside its existing `try/except OSError`), `write_manifest` and `_write_stub_manifest` (`<out>.manifest.json`, **`default=_jsonable` kept**), `cmd_from_ifc`'s `<out>.spec.json` (indent 2) → `_jsonsafe.write`; its ONE `--json` stdout object → `print(_jsonsafe.dumps(doc, indent=1, default=_jsonable))` — the #456 contract (printed object == on-disk manifest + `exit_code`) holds by construction (both go through the same walk) and is asserted; `tools/route.py`'s three `--json` printers (`run`, `matrix`, `explain`) → one `_print_json(obj)` = `print(_jsonsafe.dumps(obj, indent=1))`, its now-unused `import json` dropped. Imports follow each file's own character: `tools/route.py` and `tools/ifc_intent.py` import the leaf at module top after their `sys.path` setup (they can always resolve `rvt` where they run); `tools/rvt_job.py` imports it function-locally at its five sites like every other `rvt` import in that file — its `skills/tekton-*/scripts/` copies are importable without the engine on the path, and `python3 scripts/rvt_job.py --help` run directly (bypassing `_bootstrap.py`) prints usage on `main`; a top-level import (tried in the `/simplify` round) turned that into a traceback, so it went back to the file's idiom (verified: usage prints again from the bare unzip).
- **Not touched (by charter): `tools/frontdoor.py`** — hot file; its two `--json` printers (`author … --json` at :184, `matrix --json` at :202) are the one remaining front-door sink → filed **#488** (`hot-file`, tiny dedicated PR; its Context also names the two adjacent non-hot printers of the same class: `tools/rvt_edit.py:212` and the plugin's `_shared/tekton_env.py go` combined-JSON printer, which is transparent today because it re-parses an already-sanitised inner document). Also not touched: the four modules #461 switched (`ifc/intent.py`, `manifest.py`, `router.py`, `standalone.py` keep `_jsonsafe.dump` + their own `open()`) → migration to `write()` filed as **#491**; `build.py`; `frontdoor/__init__.py`; every hot file.
- Tests: `tests/test_frontdoor_json_strict.py` **11 → 24** (0.26 s, fresh-clone safe, already in the shard via `ci_shard.d/461-json-strict.txt`): `write` owns the stanza (nested missing dirs made, utf-8 with a `µ`, returns the path, bare filename ok) and lands strict JSON; a `default=` expansion (a dataclass carrying inf/nan/-inf, nested two deep) lands as text through both `dump` and `dumps`, and `default=None` keeps `json`'s own `TypeError`; the byte-for-byte no-op test now also covers `scene_brief(...)`'s dict and asserts `dump == dumps == write`'s file content at indents 1 and 2; one synthetic round trip per new sink drivable without `samples/` — `write_handoff`'s `scene-brief.json` (poison injected through `scene_brief`), `run_edit`'s `ops.json` echo (poisoned `EditSpec`, missing input so the job refuses in one line after the echo), `tools/ifc_intent._jdump`, `tools/rvt_job.write_manifest` (poison + a dataclass gate with `elapsed_s=inf` through `default=_jsonable`) + `_write_stub_manifest` + **`main([... "--json"])` on a missing input: the printed document strict-parses, `exit_code == rc != 0`, `output.written is False`, and minus `exit_code` it equals the on-disk stub manifest**; `tools/route._print_json` via capsys. The tripwire is now spelling-agnostic (review: exact call counts pinned syntax): every switched module (the four from #461 + the six here) has no bare `json.dump(`, the two `--json` doors additionally no bare `json.dumps(`, and each still references `_jsonsafe`; the leaf's import allow-list gains `os`. `famfrom_ifc`'s three writers are covered by the tripwire only — its pipeline needs the vendored donor family (`FileNotFoundError: family container source not found` in a fresh clone, on `main` too).

## Evidence (cloud clone, no `samples/`, bundled pinned bases; `main` @ 676916a in a `git worktree` run TWICE for the noise class, then this branch, same venv, back to back; `scratchpad/run_jobs.sh` + `compare.py`)

- **Jobs** (all rc 0 on main×2 and on the branch, every stderr 0 B): `frontdoor.py author --prompt 'an electrical room with 6 panels' --json` (PROOF-ONLY (self-checks PASS; …), 3.9/4.1 s main, 4.1 s branch); `author --rvt <copy of plugin/assets/genesis/G_ABPD_2025.rvt> --edit "set level 311 elevation to 5 ft" --json` (PROOF-ONLY, NOT-DELIVERABLE (hard gates PASSED) — the standing label; ok true; 0.86/0.82 s main, 0.82 s branch); `author --ifc inputs/ifc/electrical-room-2500a.ifc --json` (PROOF-ONLY (self-checks PASS); 5.97/5.96 s main, 5.92 s branch); `rvt_job.py edit <G_ABPD_2025 copy> --ops ops.json(set-level 311 → 5 ft) -o …/edited.rvt --json` (exit_code 0, hard gates PASSED; 0.82/0.78 s main, 0.79 s branch); `route.py matrix --json`, `route.py explain --output rvt --inputs prompt --json`, `route.py run --output rfa --prompt 'a 75 kVA transformer' --json` (0.57 s both); `ifc_intent.py families inputs/ifc/electrical-room-2500a.ifc --dir …` (all_ok; 3.5/3.1 s main, 3.1 s branch).
- **47 JSON documents per tree** (every `*.json` in the four job dirs + the route/ifc_intent dirs + the 7 captured `--json` stdouts): branch **47/47 accepted by `json.loads(…, parse_constant=<raise>)`**, **`grep -rlE 'Infinity|NaN'` = 0 files** (main: also 0 — today's inputs produce no non-finite; the synthetic round trips exercise the transform).
- **Byte identity.** Noise class first: `main` run 1 vs `main` run 2 differ in 36/47 files, every difference one of {`generated_at`, `seconds`/`elapsed_s`/`timings`/per-stage gate seconds, built-file `sha256`, per-run GUIDs (`content_guid`, `document_guid`, `episode_guid`, census guid lists, `sample_strings`, `forge_vocabulary_ids` tails — #9), `footer_blob_hex`, the gzip'd lengths of the two GUID-bearing family streams `Global/History` 130↔134 and `Global/PartitionTable` 126↔122 (per-run GUIDs compress to ±4 B; History jittered in 5/69 of `main`'s own families across three runs, PartitionTable in 1/46 of the branch's — family emission, untouched here)}; masking exactly those keys (+ GUID-shaped strings, + the repo/out-dir path prefixes) makes main1 vs main2 vs main3 **47/47 identical**. With the same mask **main vs branch = 47/47 identical, 0 differing, same file set** (three `main` runs × two branch runs, every pairing). Unmasked `cmp`: `prompt/scene-brief.json` (19 923 B), `prompt/intent.json` (42 081 B), `edit/ops.json` (257 B), `route matrix --json` stdout (55 918 B), `route explain --json` stdout (1 852 B) **byte-identical** main vs branch; `ifcint/families/families_record.json` differs only in the embedded out-dir paths; **`rvt_job --json`'s printed object == its on-disk `edited.rvt.manifest.json` + `exit_code` (0)** on the branch. And the DONE's own phrasing: every JSON `main` wrote (46/46; the 47th is the `ops.json` my driver wrote) `json.load`s and re-dumps through `_jsonsafe.dumps` at its indent to **the identical bytes**.
- **Bare surface** (`tekton-plugin.zip` rebuilt from `main` and from this branch, unzipped into an empty dir, `env -i` + `/usr/bin/python3`, no repo on any path, two rounds each): `go author --prompt '…6 panels' --json` **rc 0, `go.ready true`, stderr 0 B**, 4.25/4.19 s main vs 4.11/4.15 s branch; `go edit host.rvt set-level --id 311 --elevation-ft 5 -o …` rc 0, 0 B, 0.56/0.54 vs 0.55/0.56 s; **`go rvt_job.py edit host.rvt --ops ops.json -o … --json` rc 0, 0 B, result = the manifest (hard gates PASSED), 0.69/0.67 vs 0.65/0.68 s**; `go author --rvt host.rvt --edit "set level 311 elevation to 5 ft" --json` rc 0, 0 B, 1.00/0.76 vs 0.83/0.71 s; all 22 JSON files under each run dir strict. Wall time unchanged within run-to-run spread.

## Findings / follow-ups (searched first; both filed task-shaped)

- **#488** (`hot-file`, Refs #475): `tools/frontdoor.py`'s two `--json` printers → `_jsonsafe.dumps` in a tiny dedicated PR (+ the adjacent `rvt_edit.py` / `tekton_env.py go` printers named in its Context — the review's one "gap above": `go` re-parses the job's stdout permissively and re-prints with plain `json.dumps`, harmless once every inner document is strict, belt-worthy anyway).
- **#491** (Refs #475/#461): migrate the seven #461 sites (`write_intent`, `write_manifest`, router ×4, standalone) from own-`open()` + `_jsonsafe.dump` to `_jsonsafe.write` — the design note's full migration; those modules were outside this territory.
- Behavioural notes for the reviewer, all toward rule 1 and all finite-output-neutral: `write()` makes the parent dir where three writers previously did not (`famfrom_ifc`'s report beside an already-written `.rfa`; `rvt_job`'s stub manifest — still inside its `try/except OSError`, so an unwritable location still costs only the stub; `write_manifest` beside the written `.rvt`); `validation_gate`'s and `cmd_from_ifc`'s dumps had no `default=` (a non-JSON-able leaf raised `TypeError`) and now render such a leaf through `default=str` like every other writer — neither payload (`ValidationReport.to_json()`, `ifc_to_spec.extract()`) carries one; `encoding="utf-8"` changes no byte because `ensure_ascii` stays on (output is pure ASCII either way) — it only stops a cp1252 host from mattering (O2).

**`/simplify` (4 independent angles).** *Applied:* the bespoke lazy `_jsonsafe()` accessor in `rvt_job.py` (a third import idiom) → the file's own function-local `from rvt import _jsonsafe` at each site (a top-level import was tried first on the reviewers' suggestion and reverted — it broke the direct `scripts/rvt_job.py --help` in the skill layout, above); `route._print_json`'s function-local import → top-level (route.py always has `src/` beside it); the test's private `_load_tool` → `conftest.load_tool` (byte-for-byte the same helper); the `(lambda b: …)(io.StringIO())` trick → a plain buffer; the exact-call-count `WRITERS` table → ban-plus-presence (counts would fail a legitimate fifth router writer and hard-coded the `_jsonsafe.`/`_jsonsafe().` spellings); `write()` → one `fh.write(dumps(…))` (measured by the efficiency pass: 3.34 → 2.89 ms on an 87 KB manifest, identical text); module docstring's caller inventory dropped. *Measured, left:* `makedirs`+`abspath` before each write ≈ 5.7 µs × ≤ 5 writes/job; `_walked`'s lambda ≈ 170 ns/dump and +250 ns per `default()` call (0 calls on a real manifest); the new top-level imports are `sys.modules` hits everywhere except `famfrom_ifc` (0.32 ms) — all negligible against 0.8–4 s jobs. *Skipped with reason:* dropping `_walked`'s `None` guard (keeps `default=None` meaning what it means to `json`); dropping the explicit `indent=1` at call sites (the indent is the file's format, stated at its writer); `write_intent` → `write()` (out of territory → #491). *Altitude verdicts:* writer-level helper confirmed as the only total depth (an encoder subclass cannot intercept float spelling; producers are open-ended); walking `default=` inside `dump`/`dumps` is the right layer (the one door back in, closed where the promise is made); `makedirs` in `write()` is the right generalisation.

## Gates

- `RVT_SKIP_LARGE=1 .venv/bin/python -m pytest tests/test_frontdoor_json_strict.py -q -rs` → **24 passed** (0.26 s).
- neighbours, final tree: `tests/test_frontdoor_json_strict.py tests/test_go_edit.py tests/test_ifc_intent.py tests/test_router.py tests/test_edit_own_release.py tests/test_prompt_intent.py tests/test_ifc_family.py tests/test_plugin_sync.py tests/test_bootstrap.py tests/test_coldstart.py tests/test_surface_perf.py tests/test_status_gate.py` → **278 passed / 21 skipped** (60 s; skips = `RVT_SKIP_LARGE`, sample-gated `test_ifc_family` cases, `test_surface_perf`'s "no bare python3 with numpy on this host", one chmod-as-root case).
- `tools/sync_plugin.py` → mirrors regenerated (`plugin/lib/src/rvt/{_jsonsafe,frontdoor/edit,frontdoor/prompt_intent,ifc/famfrom_ifc}.py`, `plugin/lib/tools/{ifc_intent,rvt_job}.py`, `plugin/skills/tekton-{author,edit,native}/scripts/rvt_job.py`, `plugin/skills/tekton-author/scripts/ifc_intent.py`), zip rebuilt (5 236 KB), deny-audit clean, identity scan == allowlist; `--check` → in sync; `plugin/scripts/validate_plugin.py` → PASS (25 assertions); `python3 tools/dev/check_portable_paths.py` → ok (2 917 paths).
- **Whole merged CI shard** (`RVT_SKIP_LARGE=1 .venv/bin/python -m pytest -q -p no:cacheprovider $(python3 tools/dev/shard_list.py --print)`, 79 files): pre-`/simplify` tree **1662 passed, 139 skipped, 5 xfailed, 0 failed** (354 s); **final tree 1662 passed, 139 skipped, 5 xfailed, 0 failed** (342 s).
- Full suite not run (SUITE-COORDINATION).

**`/verify` (final tree; front-door CLI, ops door, router, bare unzip):** the four delivered files from the branch jobs above → `tools/rvt_validate.py`: `prompt/prompt_room.rvt` **VALID (no errors); warnings=1** (the standing DataStorage decoder-gap warning), `edit/host2025.edited.rvt` **VALID; warnings=0**, `ifc/electrical-room-2500a.rvt` **VALID; warnings=1** (same), `job/edited.rvt` **VALID; warnings=0** — all exit 0 under their own release; every job rc 0 / stderr 0 B / 47 JSON strict / 0 bare tokens / 47/47 identical to `main` after the noise mask (two branch runs × three main runs); bare unzip of the FINAL `tekton-plugin.zip` (`env -i`, `/usr/bin/python3`): `go author --prompt` rc 0 READY 4.35–4.53 s, `go edit … set-level` rc 0 0.59–0.62 s, `go rvt_job.py edit … --json` rc 0 0.71–0.77 s (result = manifest, hard gates PASSED), `go author --rvt --edit` rc 0 0.77–0.84 s, stderr 0 B each, 22/22 run-dir JSON strict; and the direct `python3 skills/tekton-edit/scripts/rvt_job.py --help` (no bootstrap) prints usage again, as on `main`.

## BRANCH STATE (eng #475)

- Branch `cam/475-jsonsafe-write` from `main` @ 676916a; PR (`Closes #475`).
- Files written: `src/rvt/_jsonsafe.py` (`write`, `dumps`, walked `default=`; docstring), `src/rvt/frontdoor/prompt_intent.py` + `src/rvt/frontdoor/edit.py` (import + the one writer line each), `src/rvt/ifc/famfrom_ifc.py` (import + three writer lines), `tools/ifc_intent.py` (import + `_jdump`'s body), `tools/rvt_job.py` (four writer stanzas + the `--json` printer, each with a function-local import; nothing else), `tools/route.py` (`_print_json` + its three call sites, `import json` → `from rvt import _jsonsafe`), `tests/test_frontdoor_json_strict.py` (11 → 24), this record (own header); regenerated mirrors `plugin/lib/src/rvt/{_jsonsafe,frontdoor/edit,frontdoor/prompt_intent,ifc/famfrom_ifc}.py`, `plugin/lib/tools/{ifc_intent,rvt_job}.py`, `plugin/skills/tekton-author/scripts/{ifc_intent,rvt_job}.py`, `plugin/skills/tekton-{edit,native}/scripts/rvt_job.py`. No new shard drop-in (the test file is already listed by `ci_shard.d/461-json-strict.txt`).
- Not touched: `tools/frontdoor.py` (hot → #488), `ifc/intent.py` / `manifest.py` / `router.py` / `standalone.py` (→ #491), `build.py`, `frontdoor/__init__.py`, `manipulate.py`/`validate.py`, `tests/conftest.py` (imported, not edited), famgen, every hot file, `tests/ci_shard.txt`.
- Shipped: every job-dir JSON and `--json` stdout document on the front-door path except `frontdoor.py`'s own printer is strict JSON whatever non-finite value reaches it (including through a `default=` expansion), written `utf-8` through one helper; finite output byte-identical to `main`'s. Staged for viewer: nothing (no `.rvt`/`.rfa` byte changes). Follow-ups filed: #488 (hot-file printer + the `go`/`rvt_edit` printers), #491 (migrate #461's seven sites to `write()`).
