# inbox — family-genesis (the ADocument codec at the FAMILY level; TRACKER G4b)

Stream: FAMILY-GENESIS (2026-08-03). Charter (from `docs/inbox/asset-assay.md`):
every generated `.rfa`'s element layer is genuinely constructed, but each
container carried (1) the DONOR family's `Global/Latest` ADocument
byte-for-byte (68,508 B raw / 1.38 MB inflated, ~25 % of non-format bytes,
1,727 dangling refs to donor element ids), (2) the donor's opaque partition
footer / end record (the literal "Data generated by Autodesk" at tail
−257), (3) the four `G_*` proofs' pre-identity-fix `BasicFileInfo`. Fix
all three in a new module without editing famgen; re-forge the three
factory families + S0e; prove validator + provenance + a working load.
Module `src/rvt/famgen/famdoc_adoc.py`, tests `tests/test_famgen_adoc.py`,
deliverables `experiments/families/genesis2/`, writeup
`docs/writer/family-genesis.md`.

## Verdict in one screen

* **All three artefacts are RETIRED and every v2 file is provenance-clean
  at the byte level.** `provenance_scan_v2` — eleven checks, all TRUE on
  every emitted file: our `Global/Latest` decodes clean with the corpus
  codec, **0 dangling element ids** (schema census), **0 hits** for any of
  the donor's own element ids in an independent i64 byte scan (before the
  purge the donor's document showed 1,692 hits / 1,606 distinct), **0
  donor / employee name strings**, owner = OUR self-Family, footer blob
  ours, end record = the DECODED 10-byte constant, **no donor footer blob,
  no donor stale ECC parity**, `BasicFileInfo` author `rvt-writer`, no
  Windows user path, DIT usernames = ours only. The ONE carried stream is
  `Formats/Latest`, sha256-checked as the Revit-2026 corpus schema constant.
* **Certified validator, family mode: VALID, 0 errors, 0 warnings** on all
  four v2 families (+ the three attribution variants). Raw arbiter (project
  calibration): the 3 known family-shape residuals — and the calibration
  control settles it: the **authentic Autodesk donor `.rfa` scores WORSE**
  (5 raw errors, and even 2 family-mode errors — its own graveyard/DIT don't
  decode). The residuals are calibration gaps, not defects (the assay's
  reading, now with a control).
* **L_v2 PASSES: our clean equipment family LOADS into the working rst
  sample** through the proven four-registry loader (`rvt.famload`, the L1a
  mechanism) — a real electrical-equipment family (Eaton 400 A panel,
  solid geometry, a power connector, 14 parameters), not the level-head
  test family. Written project **VALID 0 errors** (1 warning = rst's
  pre-existing decoder gap), four registries coherent (54 / 53 / 53 / 51),
  our GUID in all four, host linkage OK (family + surrogates + symbol + 14
  param twins).
* **Counsel C5 is ANSWERED, and the format is DECODED.** Corpus proof over
  **2,072 units in 7 files** (six projects + the family archetype;
  `experiments/families/genesis2/footer_forensics.json`):
  - The string `"Data generated by Autodesk® Revit®"` (UTF-16LE, u32 char
    count 34, 72 wire bytes) is **byte-IDENTICAL in 2,072 / 2,073 units**
    (the 2,073rd = the degenerate empty form, n = 0, in `dach`'s tiny
    `Partitions/85`) — a **universal FORMAT SIGNATURE**, the analogue of a
    file magic, not donor expression. An **irreducible format token**: we
    write it ourselves from the constant `rvt.famgen.skeleton` already
    owns; those bytes were never copied. Frozen with offsets in
    `FOOTER_TOKENS`.
  - The **64-byte `0x0f3f` blob is a per-unit opaque NONCE**: 2,071
    distinct / 2,071, entropy 7.885–7.926 bits/byte at all 64 positions,
    zero constant positions, no standard digest (SHA-512/384/256, SHA-3,
    BLAKE2, MD5, SHA-1) of any content span, not RFC-4122 GUIDs — and **not
    reader-verified** (every re-blocked reduction that LOADED kept a stale
    blob; `dach` and our L1a units carry the 0-length form). We now write
    OUR value (`build_footer`, or the empty form) — zero donor bytes. (The
    one blob duplicate in the whole census was our factory `.rfa` copying
    the donor's.)
  - The **stream end record is a 10-byte universal FORMAT CONSTANT**
    `a3 03 00 00 00 00 ff ff ff ff` (u16 0x3a3, i32 0, i32 −1), identical in
    **8 / 8** exactly-unframed streams. The 48–276-byte *"opaque
    high-entropy tail"* the corpus and `rvt.partitions` recorded as
    `[undecoded]` is **NOT content**: it is the CRCIO pad-count + parity
    of the stream's **final block** (`container.depage` leaves the final
    partial page's trailer as trailing junk). Proved: `ecc.unframe_stream`
    byte-exact round trip on **every** file; the donor's 92-byte "end
    record" == the 10-byte constant + 82 bytes of the donor's own on-disk
    final-block parity. So **every `emit_family_rfa` output carried 82
    bytes of the donor's stale ECC parity as junk content** — retired by
    `FAMILY_END_RECORD`.
* **The family document object is CONSTRUCTED**, genesis-2's discipline at
  family scale: archetype SHAPE (the family-editor 239-slot
  `AppInfoManager`; slot classes identical to the project table at every
  index; `m_ownerFamilyId` = the owning Family; no worksharing) →
  schema-typed **PURGE** of every donor id (the SAME audited
  `AdocGraphEditor`, imported by path — complete by construction; on the
  panel 1,886 dangling → 1,778 registry entries dropped, 20 positional
  nulled, 7 scalars nulled, **0 after**) → **COHERENCE + CACHES** (no
  nested families; the archetype's naming memory 'Elevation 0'/'A100'/
  'Level 1', browser folders 'Elevations (Elevation 1)'/'3D Views', window
  states, param-set memory, 'Revit Default DB Server' descriptor, the
  sample-authoring machine's 3840×1600 window metrics — all emptied /
  neutralised) → **REPOPULATION over OUR inventory** (the family
  registries index only the DATUM / VIEW / SETTINGS layer: our level + ref
  planes, our project/plan views + sun settings, our drawings, our sketch
  plane with `m_famId` = our self-Family, our UnitsElem, level→plan-view,
  the load classification; **geometry, connectors, curves and parameters
  are indexed by NO registry** — verified) → our authorship (owner = our
  self-Family, product build constant) → re-encode / re-decode / equality
  gates. Three modes = the family-scale ladder: **candidate** (the
  deliverable; Forge corpus carried-and-flagged), **purity** (Forge corpus
  emptied: `Global/Latest` 1.34 MB → 9,441 B inflated / 75 KB → 2,625 B
  raw, still clean), **minimal** (every registry empty — the all-null
  shape our L1a-loading embedded documents carry).
* **The C4-class residual is characterised, not hidden.** The ONLY bytes
  our candidate `Global/Latest` shares with the donor's are the Autodesk
  Forge unit / spec / parameter JSON corpus (longest common run
  **1,333,534 B**, classified) — **verified byte-identical (same 893 +
  422 schema pairs) in the family archetype AND all six project samples**,
  i.e. a per-release PRODUCT constant of the Revit install, NOT donor
  expression; carried and flagged exactly as genesis-2's ruled candidate
  posture (counsel C4-class beside Formats/Latest). Purity mode removes
  it (shared run collapses to 1,727 B of empty-slot scaffolding). No mode
  shares any element id, name, GUID or memory.

## Findings (evidence)

- **The family ADocument shape [V]** — the archetype's `Global/Latest`
  decodes clean and re-encodes byte-exact with the UNCHANGED
  `rvt.adocument` codec (no new grammar). 239 AppInfo slots (project
  241); all 130 populated slots' classes = the project table's at the
  same index; slot 10 `SectionNaming` family-only; the family
  `UniqueElementsTracking` positional table is the SAME compiled-in table
  as the project's (14/15 archetype positions agree with the
  project-derived `UET_POS_TO_CLASS`; pos 85 `DrawOrder3dElem` merely
  never appears in the project samples — added to the map). `m_ownerFamilyId`
  = 17 (the archetype's self-Family); `FamilyMgr[0] = {m_surrogateId −1}`
  is the family's own self-entry (projects carry only real surrogates).
- **What the family registries index [V]** (each id attributed to its
  class via the archetype's own unit-0 records): DatumTracking = Level +
  RefPlanes; ETD m_elems = RefPlane→OST −2000530, Level→−2000240,
  ElectricalLoadClassification→−2008143, ParamElemElectricalLoad
  Classification→−2008148, ElectricalDemandFactorDefinition→−2008142;
  DBViewInfo = project view + views index + `m_aidSunAndShadowSettings`;
  DBDrawingInfo = drawings; LevelPlanViewTracking = level → plans;
  SketchPlaneInfo CurSPRec `{sp, view, famId = self-Family, type 0}` +
  WPRMap (SketchGrid elements); CategoryTracking = 1,477 GStyle + 70
  sub-category rows. Form elements / ConnectorElem / CurveElem /
  ParamElemFamily appear in **no** registry.
- **The 1,727 dangling ids by registry [V]:** CategoryTracking 1,547,
  ElementTrackingData 52, AppearanceAssetTracking 27, MaterialTracking
  24, SketchPlaneInfo 22, UniqueElementsTracking 15, LinePatternTracking
  14, DBViewInfo 9–10, DBDrawingInfo 9, DatumTracking 8, LoadCase 8,
  FillPattern 7, ViewTag / StructConnectionType 5, small others.
- **Per-file provenance [V]** (`<stem>.json` → `provenance_full`; all
  four families identical in kind): adocument decodes clean, references
  to our elements = 9 (schema-named leaves; the byte scan confirms all 13
  of our indexed ids present), dangling refs (> 99) = 0, donor-id byte-scan
  hits = 0 / universe 1,755, donor-name string hits = ∅, longest common
  run with donor = 1,333,534 B = the Forge product corpus; identity author
  `rvt-writer`, DIT usernames `['rvt-writer']`, no employee username /
  `C:\Users\` path anywhere in the container; partition footer: signature
  string ×1, our 64-byte blob, true end record = `a30300000000ffffffff`,
  donor footer blob absent, donor end parity absent; `Formats/Latest`
  sha256 = the corpus schema constant.
- **Certified validation [V]:** family mode VALID 0/0 on all seven v2
  files; raw arbiter 3 residuals each (`ProjectInformation` missing,
  `PartAtom` not CRCIO-framed, `PartAtom` no gzip member); donor parity
  = the Autodesk archetype `.rfa` itself: 5 raw errors + 2 family-mode
  errors (worse than ours). Read-back verify OK on every file (0 gzip CRC
  failures, 0 ECC page mismatches, walker clean, per-seq record counts,
  id sets identical across seqs).
- **L_v2 [V]** (`L_v2_panel_loaded_load.json`): host rstbasic, our panel
  document at ids ≥ the watermark (unit 53, 41 records, family 1472566,
  symbol 1472583, 14 param twins); registries after = 54 units / 53
  ContentDocuments / 53 ContentTable / 51 FamilyMgr, coherent, ours in all
  four; ElemTable 13,954 records, watermark 1472583 covers the embedded
  document; certified project-mode validation VALID, 0 errors, 1 warning
  (the known rst RebarShape/DataStorage ES-blob decoder gap).
- **Footer forensics [V]** (`footer_forensics.json`, 7 files / 2,072
  units): string universal (2 distinct texts = the signature + the empty
  form ×1); blob n=64 in 2,071 units, 2,071 distinct, 0 duplicates, mean
  entropy 7.907 bits/byte, 0 constant positions, no digest match; true end
  record constant in 8/8 streams, unframe→reframe byte-exact on all.

## What this stream did NOT establish (honest gaps)

- **Autodesk-reader ACCEPTANCE of the v2 standalone `.rfa`** — the viewer
  question this stream's probes exist to answer. The two named residuals
  are the leads if FG1 fails while FG2 passes: `UniqueElementsTracking` is
  **all-null** (we author none of the family-editor settings singletons —
  the family-scale analogue of the project **K5** finding) and
  `CategoryTracking` is **empty** (no built-in category / GStyle catalogue
  — the **K6** analogue). The attribution ladder (FGb-minimal, FGa-purity,
  FGc-emptyfooter) isolates registries / corpus / footer form.
- The **exact Autodesk semantics of the 64-byte footer blob** stay
  undecoded (a nonce/proprietary digest; proven only that its value is not
  reader-verified and that the 0-length form loads). We author it, we
  never fake a checksum.
- `AppearanceInfo.m_previewDBViewID` = −1 (the archetype's preview is its
  3D view; our documents carry a plan view only) — a possible cosmetic
  thumbnail question, recorded.
- The famload L_v2 embedded ADocument is the factory's all-null 239-slot
  object (unchanged, not this stream's territory). This module supplies
  `author_family_adocument_embedded()` = the POPULATED alternative for a
  future loader flag.

## Requests for the orchestrator

- **Book the viewer gate for the v2 set** (`experiments/families/genesis2/
  probes.json`): FG2 first (the load — our clean equipment family inside a
  working project), then FG1 (flagship standalone), FG3/FG4/FG5 to
  attribute (geometry-free / category / dual-connector); the optional
  FGa/FGb/FGc rungs attribute a FG1 result to the Forge corpus, registry
  population, or footer form.
- **Retire the assay's provenance debt entries**: standalone-`.rfa`
  `Global/Latest` and the "Data generated by Autodesk" footer item are
  closed by this stream (G4b); the four `G_*` donor-graft proofs remain
  QUARANTINED per the assay (untouched here; the v2 factory families ARE
  the clean geometry proofs — our container + our form + our ADocument).
- **Counsel C5 dossier item**: the "Data generated by Autodesk® Revit®"
  string is a universal format token (2,072 / 2,073 units, 7 files), the
  end record a universal 10-byte constant, the blob a per-unit unverified
  nonce — exact bytes / offsets in `FOOTER_TOKENS` and
  `footer_forensics.json`.
- **Route two format corrections to their owners** (not this stream's
  territory to edit): (a) `rvt.partitions` — the "[undecoded] opaque
  high-entropy tail" of the end record IS the final block's CRCIO
  pad+parity; the true end record is the 10-byte constant; (b) the
  assay's standing request to merge the family-mode diff as a certified
  `rvt_validate --family` mode (this stream's gate uses
  `skeleton.validate_family` + labels the raw-arbiter residuals as
  calibration, with a donor control).
- **Consider `emit_family_rfa_v2` as the factory's default emission**
  (the factory's `FamilyProduct.write` still calls `skeleton.emit_family_
  rfa`, i.e. donor Latest + donor footer + donor parity junk) — a
  one-line switch in the factory's territory once the viewer gate reads.

## Verification (how to re-run)

- Whole set + probes: `.venv/bin/python -m rvt.famgen.famdoc_adoc` (~3.7
  min incl. the corpus forensics; `--no-forensics --no-load` ≈ 8 s).
- Corpus proof only: `.venv/bin/python -m rvt.famgen.famdoc_adoc
  --forensics-only`.
- Any file's ledger: `FA.provenance_scan_v2("experiments/families/genesis2/
  <x>.rfa")` (11 checks) / `FA.validate_family_file(...)` (family mode +
  raw arbiter + donor parity).
- Tests: `.venv/bin/python -m pytest tests/test_famgen_adoc.py -q` → 19
  passed (footer grammar; all three authoring modes' gates on S0e; the
  clean emission's read-back, family-mode validation, provenance scan and
  empty-footer form).
- Full suite: `.venv/bin/python -m pytest tests/ -q --ignore=tests/oracle`
  → **727 passed, 3 failed** (12:55). The 3 failures are the SAME
  pre-existing cross-stream failures the asset assay recorded (its baseline
  was 708 passed / 3 failed): `test_plugin_sync` (the known plugin drift
  awaiting `tools/sync_plugin.py`) + two `test_provenance.py` G0 tests
  (`test_G0_resource_refs_are_counted`, `test_G0_identity_dit_usernames_
  still_leak` — the parallel provenance / genesis streams). None in famgen,
  none touched by this stream; 708 + this stream's 19 = 727 passed.

BRANCH STATE: no git repo (working tree); deliverables =
`src/rvt/famgen/famdoc_adoc.py` (new module, 2,332 lines; no existing
src/tests edited), `tests/test_famgen_adoc.py` (19 tests, PASS),
`experiments/families/genesis2/` {four v2 families
`eaton_prl_400A_42sp_480Y_v2 / xfmr_75kVA_v2 / troffer_2x4_v2 / S0e_v2
.rfa` (+ `.json` ledgers), the attribution ladder
`eaton_prl_400A_42sp_480Y_v2_{purity,minimal,emptyfooter}.rfa`,
`L_v2_panel_loaded.rvt` (+ `_load.json`), `probes.json` (5 probes + 3
optional), `footer_forensics.json`, `family_genesis.json`},
`docs/writer/family-genesis.md`, this memo. Verdicts — the standalone
`.rfa` Global/Latest is now OUR CONSTRUCTED family ADocument (schema-typed
purge of every donor id + registries repopulated over exactly our
elements; 0 dangling, 0 donor-id byte hits, 0 donor/employee names; the
only shared bytes = the Autodesk Forge product corpus, byte-identical in
all six project samples too, carried-and-flagged as the C4-class residual
and removable in purity mode); the partition footer/end record are DECODED
(string = universal format signature 2,072/2,073 units — counsel C5's
irreducible token, written by us from a constant; 64-byte blob = per-unit
unverified nonce, now ours; end record = 10-byte universal constant, the
"[undecoded] high-entropy tail" was final-block CRCIO parity — the 82
donor parity bytes every emit carried are gone); BasicFileInfo/DIT come
from rvt.identity (our authorship, no user path); every v2 file
family-mode VALID 0/0 with the authentic donor scoring worse; our clean
equipment family LOADS into rst via rvt.famload validating 0 errors
(L_v2 PASS); Autodesk-reader acceptance of the standalone v2 = the booked
viewer probes (FG1–FG5 + FGa/b/c), with UET-all-null (K5-scale) and
CategoryTracking-empty (K6-scale) named as the residual hypotheses;
full suite 727 passed / 3 failed (the assay's known plugin-drift + two
provenance-G0 cross-stream failures, none in famgen; 708 + this stream's
19 new tests = 727); READY.
