# OWN CONTENT — house standard (G1b own values + G1c resource identifiers)

Stream: **own-content** (2026-08-03). Charter: author OUR default content as
plain data (`src/rvt/genesis/house_standard.py`) driving the existing genesis
constructors; prove it by round-trip + self-containment + a **six-sample**
similarity report (target: nothing ≥ 0.85 to any sample element unless the
class leaves no free choice); inventory and disposition every Autodesk resource
identifier the constructors emit. Deliverables written:
`src/rvt/genesis/house_standard.py` (≈1,890 lines), `tests/test_house_standard.py`
(12 tests, all pass), `docs/writer/house-standard.md` (the standard + rationale
+ disposition table), this record. **No other file touched** — constructor
changes are recorded as exact diffs (§4), not made.

---

## 1. DONE — the three charter deliverables

**(a) The catalog.** `house_standard.build_catalog(start_id)` produces 234
elements (47 classes) purely from our tables via the public constructors of
`rvt.genesis.types` / `rvt.genesis.skeleton`; every value is labelled FACT /
STANDARD / OURS with its public source. **234/234 encode→decode byte-exact;
0 dangling references** (self-contained). Same `OurContent` shape the
assembler consumes.

**(b) The similarity report** (all six samples, the ledger's 16-byte-shingle
metric, payload-based so it includes `dach`):
`similarity_report(cat)` → **3 / 234 ≥ 0.85, 0 violations** — the two
BasePoints (0.946 / 0.892 vs rme) and the project view's Viewer (0.902 vs rac),
all in the `NO_FREE_CHOICE` registry with stated reasons (a base document's
survey/base point IS the origin; the project-view crop frame carries nothing
authorable). Distribution 98 < 0.50 / 133 in 0.50–0.85 / 3 ≥ 0.85 (old inline
content: 50 / 151 / 4). Every value-bearing class the audit named collapsed:
ElectricalSetting 0.79→**0.17**, ElectricalLoadClassification 0.81→**0.24**,
ProjectPhase 0.79→**0.18**, PhaseFilterElem 0.84→**0.39**, SunAndShadowSettings
0.71→**0.32**, DBViewType 0.71→**0.30**, MaterialElem 0.51→**0.36**,
DBViewProject 0.91→**0.70** (no longer over threshold). Full before/after table:
`docs/writer/house-standard.md §4`. At the ledger's own 0.50 heuristic (five
baselines; dach is id-unattributable): **99/234 read `ours-created` against
every sample** (was 51/205) and all 135 residual "clones" are the named
machinery classes (90 GStyleElem, 9 conductor name-cells, view satellites,
base points…) — the counsel/definition-field question, no longer a values gap.

**(c) The resource-id disposition table** — 17 rows in
`RESOURCE_ID_DISPOSITIONS` (rendered in the writer doc §6): `assetlibrary_base.fbx`
+ `Generic` asset descriptors (materials + every view's render settings) →
**drop**; the six `%1!s!` MACH load labels → **replace** (our phrasing; the
`%1!s!` token itself = required syntax); Autodesk-HQ site coords / weather
station / stock climate arrays → **replace** (neutral 0°/0°, ISO-2533); sun name
+ stock angles → replace; phase/filter terminology → replace (our set, from the
DECODED vector semantics); `'{3D}'`/`'Sim'` → replace; **required-token
arguments for counsel:** Forge unit typeIds (the reader's lookup vocabulary),
`'???'` project-view name (identical in every corpus file incl. German), `'Basic
Wall'` system-family designator, BuiltIn enums, `'Arial'` (third-party font
reference), partatom namespace, build string, Formats/Latest — and one NEW find:
the save-unit banner `'Data generated by Autodesk® Revit®'` inside our
`Partitions/21` (§5). After the shims a byte scan of our records finds ONLY: our
label templates, the required Forge typeIds, and our own `rev-revit` strings —
pinned by tests (the Autodesk identifiers are asserted forbidden from returning).

## 2. PROVEN — the content assembles and validates

Drove the **unmodified** `tools/genesis_assemble.py :: build_ladder` with
`build_our_content` monkeypatched to `house_standard.build_catalog` (the §4
integration diff), output to the session scratchpad — `experiments/` untouched.
All five rungs `tools/rvt_validate.py`: **VALID, 0 errors** (G0a: 1 warning =
the base's known ES-decode gap; G0b onward 0 warnings; G0 = 380,928 bytes, 705
records). Validator summary of the HS G0:

```
verdict: VALID (no errors); warnings=0 info=2; layers=structure,consistency,semantic
streams: 12  records: 705
```

The HS G0's `Partitions/21` carries none of the dropped identifiers; its only
Autodesk string is the save-unit banner (§5).

## 3. Findings for other streams (details: writer doc §9)

* **F-HS-1 (skeleton):** `DEFAULT_UNIT_FORMATS` typeIds do not occur in the
  2026 corpus (`…length-2.0.0`, `…current-2.0.0`, `feetFractionalInches`); the
  samples key `…length-1.0.0` / `…current-1.0.0` and feet-and-inches is the
  SYMBOL `autodesk.unit.symbol:feetAndInches-1.0.1`. `house_standard.UNIT_FORMATS_*`
  = a corpus-verified imperial (default) + metric registry; every string test-verified.
* **F-HS-2 (types):** `NOMINAL_VOLTAGE_RANGES` is Autodesk's rme template
  (110/130 …) mislabelled as NEC/ANSI; C84.1 Range A = ±5 % (114/126).
* **F-HS-3 (types):** demand `m_ruleType` is 0/3 (count)/4 (load ranges in
  internal power units, 10 kVA = 107,639.104); the `'load': 2` mapping is
  unobserved.
* **F-HS-4 (types):** load-class enums decoded (spaceLoadClass 1 lighting / 2
  receptacle; signature 1 motor / 2 other / 3 spare); `new_load_class`
  hard-codes signature 0 (shimmed here).
* **F-HS-5 (skeleton):** phase-filter vector semantics DECODED — slots
  2/3/4/5 = Existing/Demolished/New/Temporary; 0 = not displayed, 1 = by
  category, 2 = overridden (skeleton's comment has the order reversed).
* **F-HS-6 (skeleton):** GeoSite names are empty in the corpus (the names are
  free text on GeoLocations, localized); the default coords are Autodesk HQ.
* **F-HS-7 (commit / counsel):** save-unit banner `'Data generated by
  Autodesk® Revit®'` + app GUID `34b22600-…` in every `Partitions/N` (§5).

## 4. EXACT DIFFS requested (constructors I may not edit)

All eleven are enumerated as data in `house_standard.CONSTRUCTOR_DIFFS`; the
load-bearing ones:

**D1 — integration (genesis-assembler):** in `tools/genesis_assemble.py` replace
`build_our_content`'s inline values with the house standard:

```python
from rvt.genesis import house_standard as hs
def build_our_content(start_id, *, project_name=..., project_number=...):
    cat = hs.build_catalog(start_id)
    return OurContent(elements=cat.elements, manifest=cat.manifest, ids=cat.ids,
                      start_id=cat.start_id, last_id=cat.last_id,
                      roundtrip=cat.roundtrip(), dangling=cat.dangling())
```
(Proven this session — §2. `PROJECT_META` should then read from
`hs.PROJECT_INFO`; the manifest label "units registry (136 formats)" must state
the emitted count — audit accounting error B6.)

**D2 — types.new_material:** add `asset_name: str = "", asset_library: str = "",
asset_type: int = 0` and replace lines ≈995–999
(`mat["m_asset"]["m_sName"] = "Generic"` / `["m_sLibrary"] = "assetlibrary_base.fbx"`
/ `["m_eAssetType"] = 4`) with the parameters. Retires shim
material-asset-descriptor.

**D3 — types.new_load_class:** add `labels: Optional[dict] = None,
signature_type: int = 0`; when `labels` given overlay the six
`m_*Label` fields (keys as in `hs.LOAD_LABEL_TEMPLATES`); set
`obj["m_signitureType"] = int(signature_type)`. Retires load-class-labels +
F-HS-4.

**D4 — skeleton view constellations:** thread `render_env_name: str = ""` /
`asset_library: str = ""` (and background/sky/ground colours) from
`new_project_view` / `new_3d_view` / `new_plan_view` down through `dbview_base`
→ `view_display_mgr` → `_grender_settings`; add `excluded_categories:
Optional[Sequence[int]] = None` to the three (default: current
DRAW_EXCLUDED_CATEGORIES) and pass the sun name/azimuth/altitude to the
satellite `new_sun_and_shadow_settings`. Retires view-render-assets,
visibility policy, sun-defaults shims.

**D5 — skeleton.new_geo_site:** climate keyword parameters (12-slot summer
dry/wet-bulb, mean-daily arrays, winter dry bulb, clearness) defaulting to the
ISO-atmosphere neutral set. **D6 — skeleton.new_view_type:** `ref_label: str =
"Sim"`. **D7 — skeleton.new_true_north:** `poche_depth_ft`. **D8/D9** — the
F-HS-1 / F-HS-2 constant corrections.

**Proposal — types: `wire_sizes_element(...)` constructor** for
`RbsWireSizesElem` consuming `hs.WIRE_AMPACITY_TABLE`; target structure decoded
(writer doc §10). Beyond a diff to an existing constructor; queued for the
electrical-catalog stream. Likewise **dimension types** (`DimensionStyle`) have
no constructor yet (mapped-not-built per genesis-status §6.5) — the annotation
standard here covers text types only; a `new_dimension_type` constructor is the
prerequisite for adding our dimension styles (arrowhead / centreline / witness
conventions) to this house standard.

## 5. The one exhibit found outside my package (for the orchestrator + counsel)

A whole-file identifier scan of the assembled HS G0 (and the old G0) finds the
partition **save-unit terminator banner** at `Partitions/21` (HS G0 offset
15,838): `u32 34 | UTF-16 "Data generated by Autodesk® Revit®" | a3 03 … a2 03 |
counter | GUID 34b22600-3ed6-44b3-b4f1-6596f4d52b43` — the known framing record
of docs/streams/05-partitions.md, carried/emitted by the commit & framing layers
(not by any content constructor). It is a plaintext "generated by Autodesk®
Revit®" assertion with trademark marks inside OUR file. Two readings: (i) a
required framing token the reader validates (then it is an interface record
like the build string); (ii) a free-text authorship banner asserting something
false about a genesis file. **Requested probe (orchestrator):** a G0 whose
banner AString is replaced by ours (u32 length + our text, e.g. "Data generated
by rev-revit") to learn the reader's tolerance; **counsel item** alongside C1
(authoring string). Recorded as disposition row `save-unit-signature-banner`.

## 6. Files for the orchestrator to viewer-test (no `.rvt` written into the tree)

My territory writes no `.rvt`; the probes are cheap to (re)generate once D1
lands (`tools/genesis_assemble.py` then emits the HS ladder). Order and what
each isolates:

1. **HS-G0a** (R10b + our 234 elements) — is inserting the house-standard
   catalog/skeleton beside the sample's accepted? (same emission path as the
   accepted V20–V29 commits.) Reader-risk items concentrated here: the blanked
   asset descriptors (probe rows material-asset-descriptor + view-render-assets)
   and the empty view exclusion lists.
2. **HS-G0** (the fully-ours inventory after the ladder) — the ADocument-dangling
   question the assembler's status already framed, now with our values.
3. **HS-G0-safe** — same as 2 but `build_catalog(scrub="safe")` (asset
   descriptors + exclusion lists left at constructor defaults, only
   name/label/number replacements). If 2 fails and 3 passes, the descriptor
   blanking is the culprit → those rows flip from "drop" to "required
   reference" (fallback recorded in the table).
4. **G0-banner-probe** — G0 with the save-unit banner replaced (§5).

For an immediate test without D1: `hs/build_hs_ladder.py` in the session
scratchpad (`/private/tmp/claude-502/-Users-ck-dev-things/91c616fc-3cee-49e7-be61-74bc4edd8fdb/scratchpad/hs/`)
rebuilds `ladder/G0a.rvt … G0.rvt` in ~10 s (all validator-clean, §2); it is a
throwaway driver equivalent to D1.

## 7. Reproduction

```
.venv/bin/python -m rvt.genesis.house_standard                 # 234 elems, 234/234 byte-exact, 0 dangling
.venv/bin/python -m rvt.genesis.house_standard --similarity    # six samples: 3 >= 0.85, 0 violations
.venv/bin/python -m pytest tests/test_house_standard.py -q     # 12 passed
```

## BRANCH STATE

* Repo is not a git repository; no branch. Files written (all in territory):
  `src/rvt/genesis/house_standard.py`, `tests/test_house_standard.py`,
  `docs/writer/house-standard.md`, `docs/inbox/own-content.md`. No existing
  module, tool, doc, or `.rvt` modified; the plugin bundle is NOT re-synced
  (`tools/sync_plugin.py` is orchestrator-run and its allowlist/denylist
  decides whether `house_standard.py` ships — it reads no quarantined file).
* Verification: `tests/test_house_standard.py` **12 passed**; house catalog
  234/234 byte-exact, 0 dangling; six-sample similarity gate **0 violations**;
  HS-driven G0 ladder (scratch) **VALID 0 errors** on every rung. FULL SUITE
  (`pytest tests/ -q --ignore=tests/oracle`): **543 passed, 1 failed** — the
  failure is `tests/test_plugin_sync.py::test_plugin_is_in_sync_with_source`
  (the pre-existing plugin-drift check the audit already reported; it now also
  lists `lib/src/rvt/genesis/house_standard.py` plus 13 files from other
  concurrent streams — fix = the orchestrator-run `python tools/sync_plugin.py`,
  outside every agent's territory; `house_standard.py` reads no quarantined data
  so bundling it is safe).
* Open for the orchestrator: land D1–D9 (owning streams), viewer-probe the
  four files of §6, route the save-unit banner + Forge-typeId + '???'/'Basic
  Wall' required-token arguments to counsel with the audit's D-3 memo. **G1b:
  the values are now ours by construction and by measurement; G1c: every
  identifier is inventoried and dispositioned, drops/replacements applied.**
